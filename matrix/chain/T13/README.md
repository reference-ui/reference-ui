# matrix/chain/T13 — Parallel chains + shared layer

```
extend-library    ──▶ extend ──▶ meta-extend-library    ──▶ extend ──┐
extend-library-2  ──▶ extend ──▶ meta-extend-library-2  ──▶ extend ──┼──▶ User space
layer-library     ──▶ layer  ───────────────────────────────────────────┘
```
