# matrix/chain/T8 — Same library in extends + layers

```
extend-library ──▶ extend ──┐
                             ├──▶ User space
extend-library ──▶ layer  ──┘
```

Documents the current allow-and-duplicate policy. See
[`matrix/CHAIN_RULES.md`](../../CHAIN_RULES.md).
