# matrix/chain/T10 — Chain + layer

```
extend-library ──▶ extend ──▶ meta-extend-library ──▶ extend ──┐
                                                               ├──▶ User space
layer-library  ──▶ layer  ───────────────────────────────────────┘
```
