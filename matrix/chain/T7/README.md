# matrix/chain/T7 — Diamond

**Topology:** App extends two libraries that both extend the same base.

```
extend-library ──▶ extend ──▶ meta-extend-library          ──▶ extend ──┐
              └─▶ extend ──▶ meta-extend-library-sibling ──▶ extend ──┴─▶ User space
```
