diff --git a/packages/reference-mcp/src/pipeline/icons-search-index.ts b/packages/reference-mcp/src/pipeline/icons-search-index.ts
index afcaba7d5..d9108d3a5 100644
--- a/packages/reference-mcp/src/pipeline/icons-search-index.ts
+++ b/packages/reference-mcp/src/pipeline/icons-search-index.ts
@@ -97,6 +97,8 @@ export class IconsSearchEngine {
     if (docs && docs instanceof Map) {
       for (const entry of docs.values()) {
         if (Array.isArray(entry.categories)) {
+          // Lowercase once at load so hot filters use plain === (no per-candidate allocs)
+          entry.categories = entry.categories.map(c => (typeof c === 'string' ? c.toLowerCase() : c))
           for (const c of entry.categories) {
             if (c) catSet.add(c)
           }
@@ -172,7 +174,7 @@ export class IconsSearchEngine {
       const catLower = category.toLowerCase()
       searchOptions.filter = result => {
         const cats = (result.categories as string[]) || []
-        return cats.some(c => c.toLowerCase() === catLower)
+        return cats.some(c => c === catLower)
       }
     }
 
@@ -284,7 +286,7 @@ export class IconsSearchEngine {
     const allResults = this.miniSearch.search(MiniSearch.wildcard, {
       filter: result => {
         const cats = (result.categories as string[]) || []
-        return !category || cats.some(c => c.toLowerCase() === category)
+        return !category || cats.some(c => c === category)
       },
     })
 
