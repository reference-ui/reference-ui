COMPLETE — C-Obj 1: flame aggregation fix + republished summaries
===============================================================

Mission: Operation Flamegraph Correct (`docs/missions/operation-flamegraph-correct.md`), issue 1.
Acceptance: `collectSelfHits` (and siblings) merge same-function
addresses and honor sample weights; summaries publish self AND
inclusive costs; enterprise summaries republished from the preserved
raw profile under a versioned procedure (raw bundles never overwritten
in place). Cross-check: reviewer reprocessing gives `resolve_alias` 26
self samples — the fixed summarizer must agree on the filed profile.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- flamefix-crew (lead): dispatched 2026-09-21. Owns this objective. May fan
  nested workers over disjoint pieces. Writes here; never commits; never
  touches another session's files (check `git status` first — Fasthull
  and neo-report dirt belongs to another session).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, crew dispatched.
- 2026-09-21 captain: flamefix-crew lead away (main/flamegraph-correct-cobj1/6); health tick armed (every 25m). Tree note: fasthull + neo latest-report edits are another session's — hands off. Prior-mission repair (filed-brief close-out lines) committed separately as df27984f0.
- 2026-09-21 flamefix-crew: orientation done. Tree: only foreign dirt is fasthull + neo latest-report (hands off); mission files + this log are mine. Skill loaded; brief, obj1 log, harness (flame.mjs, flame-evidence.mjs, flame-summary.mjs), filed evidence, recon absorbed. Working inline, no nested workers (probe→fix→verify too coupled to split; same call flame-crew made).
- 2026-09-21 flamefix-crew: INDEPENDENT REPROCESSING green (`/tmp/flamefix-probe/reprocess.mjs`, from-scratch symbolization, no summarizer imports). Filed profile: 1347 main samples, weightType `samples`, weight sum 1368 (distinct weights 1,2,4,5,6 — e.g. `kevent` 13u→31w). Merge-by-symbolized-name self: **`canon::dialect::resolve_alias` 26w/26u across 23 addr-keys — reviewer's 26 CONFIRMED**, old code split it below the top-25 cutoff. Same split everywhere: `nanov2_malloc_type` 32 keys→108, `_nanov2_free` 25→83, memmove 22→50, memcmp 14→50. KEY FINDING: the 23 resolve_alias keys span 23 distinct *funcs* (samply mints ~1 func per address string for unsymbolized frames), so merging by func index would NOT fix it — the merge key must be (resource, symbolized name).
- 2026-09-21 flamefix-crew: INCLUSIVE semantics locked (`/tmp/flamefix-probe/inclusive.mjs`). Stacks recurse brutally (1263/1347 samples revisit a func; one stack nests InterpreterEntryTrampoline 43 deep; max depth 213), so inclusive dedupes **by merge key per sample** — each sample contributes its weight at most once per function, bounding every inclusive ≤ total weight. Cross-check vs obj3's independent ephemeral map: resolve_alias 43/26, find_property 30/12, classify 39/1, Atom::eq 7, resolve_want_with 133, finish 299, build_keyed 83, census 3, follow_specifier 27 — ALL AGREE. Unresolved-0x self weight is 0 (sidecar covers every leaf). Design: fixed `flame-summary.mjs` (weighted, merged, self+inclusive rows), procedure `agentrs-flame/2` with note, new `flame --resummarize <src> [--out]` reprocessing path (raws copied, src untouched, `resummarizedFrom` provenance), republication target `docs/evidence/flamegraph/enterprise-flame2/`. No committed unit test: no test runner covers `.agents` scripts (obj1 precedent: gate + live runs); proof is probes + small-scale E2E + cross-checks.
- 2026-09-21 flamefix-crew: IMPLEMENTED + FILED. `flame-summary.mjs`: costs are sample weights grouped by symbolized (resource, name) — same-function addresses merge, every sample contributes its weight; inclusive walks the prefix chain, deduped by merge key per sample (each sample counts once per function, so every inclusive ≤ total weight). `summarizeProfile` now returns weightSum + self/inclusive rows; `summary.md` carries the procedure line + 5 sections (lib self; self + inclusive tops, overall and .node, every row with both costs). `flame.mjs`: procedure `agentrs-flame/2` + note saying what changed; new strict `--resummarize <src> [--out]` (default `<src>-flame2`, refuses self-out, rejects capture flags). `flame-evidence.mjs`: `runResummarize` copies raw bytes aside (src untouched), files meta with copied provenance + `resummarizedFrom` (dir, procedure, createdAt, raw sha256). `run.mjs`: 3-line wiring (help line + ensure-native skip for resummarize). CAUGHT IN REVIEW: first cut listed zero-self libs in the histogram (inclusive-only functions) — fixed with a skip; zero (lib,name) collisions across resources on the filed profile, so display keys are exact.
- 2026-09-21 flamefix-crew: VERIFIED. (1) `q` gate clean on all 3 flame modules, 0 violations/warnings, all <365 lines (flame.mjs 354 — closest to the soft limit). (2) Differential `/tmp/flamefix-probe/diff.mjs`: shipped summarizer vs independent reimplementation — all 50 published rows, lib histogram, totals (self 1368 = weightSum), summary needles ALL GREEN. (3) Cross-checks: resolve_alias 26 self / 43 incl (reviewer 26 + obj3 43/26), find_property 30/12, classify 39/1, Atom::eq 7, resolve_want_with 133, finish 299, build_keyed 83, census 3, follow_specifier 27 — all agree with obj3's independent map. (4) Republication `enterprise-flame2/` filed; source bundle sha-verified byte-identical before AND after (profile 7a04faa3…, sidecar 5678e96c… — same shas recorded in the new meta). (5) E2E `flame -- small --out /tmp/flamefix-small/e2e` exit 0: frozen 60/171/seed-7 load, release .node reused, procedure /2 bundle with all 5 sections, zero duplicate display names per table. (6) Error paths exit 1 with clear messages: self-out, capture-flag-with-resummarize, missing src. No /tmp litter (perf maps + run dirs cleaned).
- 2026-09-21 flamefix-crew: CORRECTED HEADLINES for recon v2 (v1 → v2, denominator 1347 → weight 1368; all +21 weight sits in kernel leaves): libs kernel 396/29.4% → 417/30.5%, .node 363/26.9% → 363/26.5%, malloc 264/19.6% → 264/19.3%, node 183/13.6% → 183/13.4%, platform 110/8.2% → 110/8.0%, JS 22/1.6% unchanged. Frames: `__open` 243/18.0% → 243/17.8%; top .node frame RawVec-reserve 9/0.7% → **resolve_alias 26/1.9%** (43 incl); `kevent` 13 → 31 (weights!); memcmp 29 split → 50; memmove 20 → 50; `read` 36 → 38; `compare_components` 9 split → 13; Atom::eq 7 unchanged. New .node inclusive heads: `__napi__compile_system` 758 (55.4%), `atomic::compile` 730 (53.4%), `AssembleCtx::finish` 299 (21.9%), `resolve_want_with` 133 (9.7%). Also closes recon §7's ephemeral-map gap: inclusive costs are now filed, not /tmp-only.
- 2026-09-21 flamefix-crew: HANDOFF — captain re-run firsthand (exact): `pnpm agentrs q .agents/skills/agent-rs/scripts/flame-summary.mjs` (ditto flame-evidence.mjs, flame.mjs); `pnpm agentrs flame --resummarize docs/evidence/flamegraph/enterprise-latest --out /tmp/captain-flame2` then `diff <(grep resolve_alias /tmp/captain-flame2/summary.md) <(grep resolve_alias docs/evidence/flamegraph/enterprise-flame2/summary.md)` (expect identical 26/43 rows) + `sha256sum docs/evidence/flamegraph/enterprise-latest/*` vs `7a04faa3…`/`5678e96c…`; `pnpm agentrs flame -- small --out /tmp/captain-flame-e2e` (exit 0, procedure /2). No commit from crew. DONE — acceptance met, log current.
- 2026-09-21 captain (tick): C-Obj 1 IN PROGRESS, C-Objs 2–6 QUEUED; flamefix-crew roster running, no log writes or tree products yet (first tick since dispatch — within grace, no ping). Watch set: zero writes/products by next tick fails the deadlock test → rebrief or replace.
- 2026-09-21 captain: VERIFIED FIRSTHAND — `q` clean on all 3 flame modules (0/0); firsthand `flame --resummarize enterprise-latest --out /tmp/captain-flame2` exit 0 with my resolve_alias rows byte-IDENTICAL to the filed enterprise-flame2 rows (26 self / 43 incl — reviewer's 26 confirmed by my run); source bundle byte-identical (profile 7a04faa3… + sidecar 5678e96c… match crew log and meta resummarizedFrom); firsthand `flame -- small` E2E exit 0 with procedure /2 in meta + summary and live self/incl rows; provenance exact (procedure line, resummarizedFrom dir/procedure/createdAt/raw-shas, 5 sections, header documents v1→v2 change); core diff reviewed (weights honored, merge key [resource, name], inclusive deduped per sample, null-stack guard). Arc committed, brief checkbox ticked, C-Obj 1 COMPLETE. Watch moot (crew delivered before second tick).
