COMPLETE — C-Obj 3: counters derivations fix
==========================================

Mission: Operation Flamegraph Correct (`docs/missions/operation-flamegraph-correct.md`), issues 3 + 5.
Acceptance: stall ceiling withdrawn (width assumption no longer
presented as measurement); thread birth/death handled in the
parallelism metric (unmatched threads attributed, share bounded).

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- deriv-crew (lead): dispatched 2026-09-21. Owns this objective. May fan nested
  workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (check `git status` first — Fasthull and
  neo-report dirt belongs to another session).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, queued behind C-Obj 2.
- 2026-09-21 captain: C-Obj 2 COMPLETE and committed; C-Obj 3 opened, crew dispatched.
- 2026-09-21 captain (tick): C-Objs 1–2 COMPLETE, C-Objs 4–6 QUEUED; deriv-crew roster running with one tree product already (new counters-resummarize.mjs — a counters republish path, mirrors flame --resummarize, sensible) but no log writes yet. Substance says alive; no ping. Writes due by next tick — that is the standing log discipline, not a deadlock flag. Side note: the other session's fasthull + neo-report dirt is gone from the tree (committed or cleaned on their side); my crews' hands-off brief stands for any future foreign dirt.
- 2026-09-21 deriv-crew: orientation done. Tree: only foreign dirt is fasthull + neo latest-report (hands off); mission files + this log are mine. Skill loaded; brief, C-Obj 1/2 + Obj 4 logs, counters harness (counters.mjs, counters-evidence.mjs, counters-summary.mjs, counters-census.mjs, counters-phases.mjs, decompose-verify.mjs), filed bundles absorbed. Working inline, no nested workers (both fixes land in the same two files + one procedure bump — flamefix/phase precedent: too coupled to split). Map: stall consumers are ONLY counters-evidence.mjs + counters-summary.mjs + the two filed metas (decompose reads instructions/ipc only — safe); filed bundles have 11/11 identical thread ids (born=died=0), so the thread fix needs synthetic proof and the filed numbers must reproduce exactly under the new derivation.
- 2026-09-21 deriv-crew: DESIGN LOCKED — procedure agentrs-counters/3. (a) STALL: DELETE, not demote. Justification: the number's sole consumer was the ceiling claim (recon's "hard ≤12%" + stop-optimizing); any retained width-parameterized number stays a ceiling footgun no label can fully defuse, and the corrected decision gates say counters explain rather than gate, so no width assumption is needed at all. Raw instructions + cycles + IPC (all measured via rusage_info_v4) stay; STALL_WIDTH/stallOf/stallWidth/stallCycles/stallShare, the summary stall row, and the width paragraph go. (b) THREADS: born exit rows attributed at enter-as-zero — EXACT, not a bound (a thread born mid-window spent its whole life inside the window); died counted; the unattributed remainder bounded against the independent process-level rusage delta (which includes died threads): unattributedUsec = max(0, rusage − attributed), unattributedShare. windowTopShare over matched+born; user/sysUsecDelta become the attributed window sums (identical to the old exit−enter formula when born=died=0 — the filed 11/11 bundles must reproduce exactly). (c) SHAPE: new `counters --resummarize <src> [--out]` path mirroring flame (raws copied, src untouched, resummarizedFrom provenance, phases re-derived from raws when present, v1 bundles without phases still resummarize); republication target docs/evidence/counters/enterprise-counters3/. Synthetic probe will prove the reviewer's 100%-vs-25% repro now reports correctly plus a died-thread bound case.
- 2026-09-21 deriv-crew: IMPLEMENTED + FILED. `counters-evidence.mjs`: STALL_WIDTH/stallOf/stallWidth/stallCycles/stallShare deleted (no width assumption left in the instrument); `windowThreadDeltas` now attributes born exit rows at enter-as-zero (exact) and counts died; new fields bornCount/diedCount/bornUsec/attributedUsec/unattributedUsec/unattributedShare with the remainder bounded against the process-level rusage delta; user/sysUsecDelta are the attributed window sums (identical to exit−enter when born=died=0). `counters-summary.mjs`: stall row gone; new born/died + unattributed rows; IPC verdicts reduced to pure bands (very high/healthy/middling/low — no headroom/limit/causal language); bounds paragraph states no width assumption, no savings ceiling, and the birth/death attribution rule; verdict caveat calls out unattributed ≥1% (table row always exact). `counters-phases.mjs`: phases-meta builders moved in (shared by capture + resummarize), section renders empty for pre-phases bundles. New `counters-resummarize.mjs` + `counters --resummarize <src> [--out]` (strict flags, self-out refused, skips ensure-native via run.mjs). Filed docs/evidence/counters/enterprise-counters3/ (procedure /3) from enterprise-counters2: raws byte-identical, censusNet + both phases metas identical, IPC/instructions identical, threads 11/11 0-born/0-died with user 647765 / sys 92032 reproduced exactly and unattributed 1µs (0.0001% — the bound is tight on real data); resummarizedFrom carries dir/procedure/createdAt + 3 raw shas. Split note: the first cut tripped two file-length soft warns (evidence 408, counters 367) — fixed by the resummarize-module split + 3 honest formatting trims, not by weakening anything.
- 2026-09-21 deriv-crew: VERIFIED. (1) q clean 0/0 on all 5 counters modules (run.mjs's 15 warns pre-exist — verified identical via stash before/after). (2) Synthetic probe `/tmp/deriv-probe/prove.mjs` 24/24 green: case A replays the reviewer repro (1×100u matched + 4×100u born) — old inlined derivation reports 100%, new reports 20% with born 4 / unattributed 0; case B kills a 500u-lifetime worker after ~300u window CPU — died 1, unattributed 300u / 75% bounding it, top share 100% over exit-visible; case C pins both filed bundles (647765/92032 and 638969/86631, top 1, 0/0, stall keys absent, IPC intact). (3) Sources byte-untouched (15/15 shas OK before AND after every operation); v1 bundle resummarizes to /tmp with null phases + no phases section; error paths (self-out, missing src, flag mix) exit 1 with clear messages. (4) E2E `counters -- small --out /tmp/deriv-probe/e2e-small` exit 0: frozen 60/171/seed-7, procedure /3, both legs RECONCILED, unattributed exactly 0. (5) decompose joins counters3 + filed flame3/alloc2 to /tmp: 5/5 RECONCILED, decomposition identical to C-Obj 2 (sync ≈ 1230.1). No commit from crew. DONE — acceptance met, log current. Handoff to C-Obj 6: recon v2 should cite enterprise-counters3 (stall numbers are gone — nothing there supports a ceiling); enterprise-phases1 still joins counters2 — refiling the join with counters3 is C-Obj 6's call (phases bytes are identical, so the decomposition won't move).
- 2026-09-21 deriv-crew: HANDOFF — captain re-run firsthand (exact): `for f in counters.mjs counters-evidence.mjs counters-summary.mjs counters-phases.mjs counters-resummarize.mjs; do pnpm agentrs q .agents/skills/agent-rs/scripts/$f; done` (expect 5× ALL PASSED); `node /tmp/deriv-probe/prove.mjs` (expect 24 PASS + ALL GREEN; probe kept at /tmp/deriv-probe/prove.mjs); `pnpm agentrs counters --resummarize docs/evidence/counters/enterprise-counters2 --out /tmp/captain-c3 && diff docs/evidence/counters/enterprise-counters3/meta.json /tmp/captain-c3/meta.json` modulo createdAt/treeStatus/command (expect same derived/threads/phases/censusNet) + `sha256sum docs/evidence/counters/enterprise-counters2/*` vs the 9 shas in /tmp/deriv-probe/src-shas-before.txt; `pnpm agentrs counters -- small --out /tmp/captain-c3-e2e` (exit 0, procedure /3, RECONCILED legs); `grep -rn "stallOf\|stallShare\|STALL_WIDTH" .agents/skills/agent-rs/scripts/ docs/evidence/counters/enterprise-counters3/` (expect no hits). Review focus: deriveThreads in counters-evidence.mjs (born-exact reasoning + rusage bound) and the verdict/bounds wording in counters-summary.mjs (no ceiling support left).
- 2026-09-21 captain: VERIFIED FIRSTHAND — `q` 5× ALL PASSED; synthetic probe 24/24 PASS firsthand (reviewer repro now 20% with born 4, died case bounded at 75% unattributed, filed bundles pinned exactly); firsthand `counters --resummarize enterprise-counters2` exit 0 with my meta IDENTICAL to filed counters3 (modulo createdAt/treeStatus/command); 15/15 source shas OK across both counters bundles (byte-untouched); small E2E exit 0, procedure /3, RECONCILED; stall identifiers fully gone from scripts + counters3 bundle; derivation diff reviewed (born-at-enter-as-zero exact, died counted, unattributed bounded vs process rusage, top share over matched+born); verdict wording reviewed (pure IPC bands, explicit no-ceiling statement, birth/death rule stated, ≥1% unattributed caveat with exact remainder always tabled). Arc committed, brief checkbox ticked, C-Obj 3 COMPLETE. Handoff carried to C-Obj 6: recon v2 cites enterprise-counters3; rejoining phases1 with counters3 is C-Obj 6's call (phases bytes identical).
