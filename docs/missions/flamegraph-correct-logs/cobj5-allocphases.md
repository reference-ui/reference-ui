COMPLETE — C-Obj 5: alloc counts by compiler phase
==============================================

Mission: Operation Flamegraph Correct (`docs/missions/operation-flamegraph-correct.md`), issue 6.
Acceptance: allocation counts attributed by compiler phase (beyond
the current totals/size-classes census), targeting reserve/arena work.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- phasealloc-crew (lead): dispatched 2026-09-21. Owns this objective. May fan
  nested workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (check `git status` first — Fasthull dirt belongs
  to another session; volatile reports/latest scratch gets file-copy
  backup/restore around any bench E2E, never git checkout).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, queued behind C-Obj 4.
- 2026-09-21 captain: C-Obj 4 COMPLETE and committed; C-Obj 5 opened, crew dispatched.
- 2026-09-21 phasealloc-crew: orientation done. Tree: only foreign dirt is operation-fasthull.md (hands off); mission files + this log are mine. Skills loaded (agent-rs, benchmark locked load — frozen 60/171/seed-7 small / 3000+12000 enterprise, instruments observe only); brief, Obj 2 + C-Obj 1–4 logs, full alloc harness (alloc.mjs, alloc-build.mjs, alloc-gc.mjs, alloc-evidence.mjs, alloc-phases.mjs), alloc_trace.rs, atomic compile pipeline, native.rs, decompose gating absorbed. Working inline, no nested workers (Rust move + guard placement + harness section meet at one JSON row shape; flamefix/phase/deriv/rss precedent: too coupled to split). Map: the blocking constraint is crate direction — runtime owns the counting GlobalAlloc but `atomic::compile` (where the 946MB goes) cannot see it; only native.rs calls atomic::compile outside tests/benches; decompose takes alloc >= 2 (a /3 bundle joins cleanly); inputs hash covers every .rs + manifest (new sources force a fresh trace dir, old cache untouched); filed shipped sha 5ac6c07e9f8d4a67c9563ce35534d36a8130a43df6a3899478d83fd160f22ec0 is the bit-identity target.
- 2026-09-21 phasealloc-crew: DESIGN LOCKED — procedure agentrs-alloc/3. (a) RUST: move the counters home, don't hook across crates. `alloc_trace.rs` moves runtime→atomic split in two (new `alloc_counters.rs`: TraceAlloc + statics + capture; new `alloc_trace.rs`: CompileSpan + report + probe, `::atomic::VirtualSource`→`crate::`), both files under the 365 soft limit; atomic gains `alloc-trace = []`, runtime's becomes `alloc-trace = ["atomic/alloc-trace"]`; runtime lib.rs keeps the `#[global_allocator]` static (now `atomic::alloc_counters::TraceAlloc`) + probe re-export; native.rs paths update. New RAII `PhaseGuard::enter(name)` snapshots counters+Instant, pushes a row on drop into a thread-local sink the span drains (span enter clears stale rows from span-less compiles); CompileSpan::finish files an additive top-level `phases` array (schema stays 1). 12 sequential phases mirroring the real pipeline: request + serialize in native.rs (serialize guard lives inside `serialize()` so all three call sites incl. rejections phase uniformly), collect/parse/constants/graphs/hosts/analysis/extract/harvest/assembly/partition in atomic lib.rs via cfg'd enter + explicit drop (no restructuring, no signature changes). Row: name, wallMs, allocBytes/Blocks, freeBytes/Blocks, liveAtEnter/Exit — net/retained derived harness-side; peak stays span-global and is never per-phase-attributed (stated, not fudged). Shipped build: every guard a cfg'd-out line, zero bytes — proven by rebuilding the shipped .node bit-identical to 5ac6c07e…. (b) HARNESS: additive only. meta.traceLeg gains `rustPhases`; new `alloc-compiler-phases.mjs` renders the per-phase table (alloc/freed/net + live enter→exit = the transient-vs-live shape) with an honest unphased remainder (phase sums vs span totals — guard gaps + prologue/epilogue, the C-Obj 2 residual treatment); renders a not-recorded line for pre-/3 dumps. No resummarize path exists for alloc, none added. (c) EVIDENCE: enterprise capture to versioned `docs/evidence/alloc/enterprise-alloc3/` via --out; prior bundles byte-untouched; decompose join-compat proven to /tmp (alloc>=2 gate).
- 2026-09-21 phasealloc-crew: IMPLEMENTED. Rust: `alloc_counters.rs` (counting GlobalAlloc + capture, moved verbatim) + `alloc_trace.rs` (CompileSpan + PhaseGuard/TLS rows + report, +cfg'd unit tests) in atomic; runtime keeps the `#[global_allocator]` static + probe (now `::atomic::` paths); features `atomic/alloc-trace`, `runtime alloc-trace = ["atomic/alloc-trace"]`; 12 guards (request/serialize in native.rs, 10 in atomic lib.rs via cfg'd enter + explicit drop, no signature changes). Harness: procedure agentrs-alloc/3, new `alloc-compiler-phases.mjs` (phase table + sums-vs-span reconcile with unphased remainder), meta.traceLeg.rustPhases. Gates: q 0 violations everywhere (lib.rs 2 warns pre-exist at HEAD: 420>365, fn 125>80 — guards add 45/28, accepted per C-Obj 2 precedent); clippy 34/36 both feature states, zero new (1 `missing_const_for_thread_local` caught + fixed); cargo 567+1 feature-off identical to Obj 2; 2 cfg'd phase tests pass feature-on; fmt/prettier diffs all pre-existing (verified via stash/HEAD).
- 2026-09-21 phasealloc-crew: BIT-IDENTITY SAGA — the literal bar ("bit-identical to 5ac6c07e") is unachievable for ANY source-adding Rust change, and I can prove exactly why. My sources forced the first shipped rebuild since Obj 2 (inputs hash bf8177f8, new file 393afb0a). Pristine-HEAD worktree build at an equal-length path (33=33 chars, so LC_ID_DYLIB + CARGO_MANIFEST_DIR keep offsets; first attempt at unequal length produced a 5.4M-byte cascade from a 16-byte header shift — discarded): after masking the two root-path strings, pristine-vs-mine differ in exactly 31 of 8,863,200 bytes. 16 = LC_UUID, which I proved content-deterministic (same-source relink bit-identical, UUID included — the old "UUID is random per link" mental model is wrong on this toolchain, so the UUID delta is content speaking, not noise). 15 = line-low-bytes of 14 `core::panic::Location{file,line,col}` consts for index expressions in modules/atomic/src/lib.rs, every delta exactly equal to my cumulative inserted-line count at that site (old 128 +14; 165/170/173/174x2 +18; 190x2 +30; 203/205/206 +34; 253 +42; 317/322 +44 — cols match the indexing columns; neighboring stream.rs entries byte-identical). __TEXT bit-identical, headers/symbols/strtabs identical. VERDICT: the feature-off instrument contributes ZERO code bytes; the only content delta is panic-line constants that must shift when any lines are added (correct for the new sources). Prior "bit-identical" proofs held via file reuse (no rebuild); any rebuild after ANY .rs change ends that. The alloc3 bundle files shipped sha 393afb0a with this provenance. Captain: recommend the mission record the corrected bar (zero code bytes + accounted line consts) rather than the literal sha.
- 2026-09-21 phasealloc-crew: ENTERPRISE FILED — docs/evidence/alloc/enterprise-alloc3/ (procedure agentrs-alloc/3). Span 995.7ms, 946.6MB alloc / 938.6MB freed / 13.9MB live-at-exit / 152.2MB peak — bit-identical to Obj 2/C-Obj 2 except reallocs 1,117,430 vs 1,117,428 (+2 = the phase-row Vec's own growth, the instrument observing itself, see surprises). R1 verdict PASS 1-head-1-tail; compile−span 28.8ms (C-Obj 2: 28.5/30.2). Per-phase (wall ms / alloc MB / blocks / freed MB / net MB / live in→out): request 5.7/8.3/42k/1.7/+6.5/5.9→12.4, collect 51.0/37.0/424k/30.8/+6.2/12.4→18.6, parse 27.4/62.6/10k/0.7/+61.9/18.6→80.5, constants 48.0/237.2/386k/206.8/+30.3/80.5→110.8, graphs 39.7/31.7/273k/46.3/−14.6/110.8→96.1, hosts 50.6/76.9/208k/76.8/+0.1/96.1→96.2, analysis 35.2/76.2/352k/56.2/+20.0/96.2→116.2, extract 199.2/175.2/2.16M/139.0/+36.2/116.2→152.3, harvest 55.1/9.9/524k/9.8/+0.0/152.3→152.4, assembly 409.9/180.7/3.51M/197.5/−16.9/73.1→56.2, partition 33.4/35.0/378k/35.0/+0.0/56.2→56.2, serialize 5.3/16.1/2k/8.1/+8.0/30.3→38.3. Reserve/arena read: constants is the transient king (237 alloc, 207 freed); parse retains +62 (retained programs, freed in the harvest→assembly gap: live 152.4→73.1); extract retains most (+36, 2.16M blocks); assembly frees most (198); harvest/partition are alloc-neutral. Provenance: shipped 393afb0a (inputs bf8177f8, zero-code-bytes proof above), trace 6af0ef6c (fresh inputs dir, old caches untouched). decompose joins alloc3 + filed flame3/counters3 to /tmp: 5/5 RECONCILED, sync ≈ 1230.1 identical. Prior bundles byte-untouched (git status).
- 2026-09-21 phasealloc-crew: SURPRISES. (1) First small E2E showed compile 213ms vs span 26ms — cold-page-cache one-off minutes after the fresh .node rebuild (first dlopen); rerun gives 31.0 vs 26.2, marshal delta 4.8ms matching C-Obj 2's 4.7 exactly. Recon v2 note: never cite a first-run-after-rebuild compile phase. (2) The +2 reallocs vs filed (1,117,430 vs 1,117,428) are the TLS phase-row Vec growing 4→8→16 — the only count the instrument perturbs, and it names itself. (3) Small-scale phase rows are alloc-deterministic across runs (identical block counts run to run); only wall jitters. (4) LC_UUID is content-deterministic on this toolchain (same-source relink bit-identical) — the mission's "UUID is random per link" assumption is stale; a UUID delta PROVES a content delta. (5) The equal-length-path worktree trick (33=33) is load-bearing: LC_ID_DYLIB + CARGO_MANIFEST_DIR embed absolute paths, and a 16-byte header growth cascades into 5.4M diff bytes via vaddr shift.
- 2026-09-21 phasealloc-crew: VERIFIED. q 0 violations on all 8 touched files (2 warns pre-exist at HEAD); clippy zero new both feature states; cargo 567+1 feature-off; 2 cfg'd phase tests feature-on; loader vitest pass; small E2E ×3 exit 0 (procedure /3, RECONCILED legs, deterministic rows); enterprise filed with PASS verdict; decompose join 5/5; pre-/3 dumps render the not-recorded line; shipped proof per above (zero code bytes + 14 accounted line consts + deterministic UUID). No commit from crew. DONE — acceptance met (per-phase counts landed with numbers above, shipped-identity proven to the achievable bar with the literal-bar finding flagged), log current.
- 2026-09-21 phasealloc-crew: HANDOFF — captain re-run firsthand (exact): `pnpm agentrs q` on the 8 files (alloc_counters.rs, alloc_trace.rs, lib.rs, native.rs, runtime lib.rs, alloc-compiler-phases.mjs, alloc-evidence.mjs, alloc.mjs); `pnpm agentrs c atomic` (567+1); `node /tmp/phasealloc-cargo.mjs` (2 phase tests, cpu-gate wrapped; probe kept at /tmp/phasealloc-cargo.mjs); `pnpm agentrs alloc -- small --out /tmp/captain-c5-alloc` (exit 0, procedure /3, 12 phase rows, compile−span ≈ 5ms); `pnpm agentrs decompose --flame docs/evidence/flamegraph/enterprise-flame3 --counters docs/evidence/counters/enterprise-counters3 --alloc docs/evidence/alloc/enterprise-alloc3 --out /tmp/captain-c5-decomp` (5/5 RECONCILED, sync ≈ 1230.1); `git status` shows no modifications under docs/evidence/alloc/enterprise-latest or enterprise-alloc2. Bit-identity re-proof: rebuild pristine (`git worktree add --detach` HEAD at a 33-char non-symlinked path + node_modules symlinks + gate-wrapped `napi build --platform --release`, as in /tmp/phasealloc-pristine.mjs), mask the two root-path strings in python (equal length, in-place), `cmp -l` vs dist (≈31 bytes: 16 UUID + 15 line bytes — decode any entry as Location{file: &str, line: u32, col: u32} and check the delta against my insertion table in this log). Review focus: the mod MOVE (runtime→atomic, counters/span split), the 12 guard placements (sequentiality = no double-count), and the bit-identity verdict (literal sha-identity is impossible for any source-adding change — decide the mission's recorded bar).
- 2026-09-21 captain (tick): C-Objs 1–4 COMPLETE, C-Obj 6 QUEUED; deadlock test passed outright on first tick — phasealloc-crew live (orientation + design-locked entries, Rust move + harness products in tree). No watch set. Captain's verify-time notes: (1) runtime→atomic move + feature forwarding is the mission's biggest Rust refactor — verify shipped bit-identity to 5ac6c07e, feature-off zero bytes, q/clippy/tests green, alloc E2E green; (2) review the 12 cfg'd guards + explicit drops in atomic lib.rs for pipeline perturbation; (3) check thread-local sink drain/stale-row semantics; (4) no alloc resummarize path (stated scope — alloc never had one, unlike flame/counters). No action, crew undisturbed.
- 2026-09-21 captain: VERIFIED FIRSTHAND — `q` 0 violations on 8 files (2 warns: lib.rs already over soft limits at HEAD at 420/125, guards add 45/28 — accepted per C-Obj 2 precedent, refactoring the pipeline for a warn line is worse); atomic cargo 567+1 PASSED; 2 cfg'd phase tests pass (incl. stale-row drain); small alloc E2E exit 0 (/3, 12 phase rows, marshal 4.9ms); decompose join of filed bundles 5/5 RECONCILED at 1230.1; phase rows verified (12 sequential, full shape, constants row matches log, phase alloc sum == span alloc exactly); guard diff reviewed (sequential cfg'd enter + explicit drop, no nesting, no signature changes; feature wiring exact). BIT-IDENTITY RE-PROOF firsthand: rebuilt pristine HEAD at an equal-length 33-char path, masked 2+2 root-path strings, cmp -l gives EXACTLY 31/8,863,200 bytes = 16 contiguous (LC_UUID) + 15 line-low-bytes across 14 Location consts, and I decoded every delta against the crew's insertion table 14/14 (incl. the two high-byte-invisible 317/322 sites). CAPTAIN'S BAR DECISION: literal sha-identity is retired as the mission bar — it breaks on ANY source-adding rebuild (proven generally, not just here). Recorded bar: zero code bytes + every delta byte accounted (deterministic UUID + panic-line consts). New shipped sha 393afb0a for alloc3-era captures; "N objectives, one binary" now spans the alloc3 rebuild — carried as handoff to C-Obj 6 with the first-run-after-rebuild caveat. Worktree scratch removed. Arc committed, brief checkbox ticked, C-Obj 5 COMPLETE.
