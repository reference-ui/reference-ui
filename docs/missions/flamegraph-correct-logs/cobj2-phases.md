COMPLETE — C-Obj 2: same-run phase boundaries + reconciled decomposition
=====================================================================

Mission: Operation Flamegraph Correct (`docs/missions/operation-flamegraph-correct.md`), issue 2.
Acceptance: common same-run boundaries (startup, scanning/evaluation,
native compilation, publishing) across all legs; a reconciled
decomposition replacing recon §2, with the JS-self caveat stated.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- phase-crew (lead): dispatched 2026-09-21. Owns this objective. May fan nested
  workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (check `git status` first — Fasthull and
  neo-report dirt belongs to another session).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, queued behind C-Obj 1.
- 2026-09-21 captain: C-Obj 1 COMPLETE and committed; C-Obj 2 opened, crew dispatched.
- 2026-09-21 phase-crew: orientation done. Tree: only foreign dirt is fasthull + neo latest-report (hands off); mission files + this log are mine. Skills loaded (agent-rs, benchmark locked load, agent-neo gate for the neo touch); brief, recon §2, C-Obj 1 log/procedure precedent, Obj 1/2/4 logs + all harness legs + sync path + filed bundles absorbed. Working inline, no nested workers (one schema couples every leg; flamefix/alloc precedent).
- 2026-09-21 phase-crew: DESIGN LOCKED — procedure agentrs-phases/1. Same-run boundaries via env-gated marks in the real sync path: new `packages/reference-neo/src/sync/phases.ts` (`markPhase`, module-level ENABLED on REFERENCE_UI_PHASES_OUT, one branch per mark when unset — zero bench perturbation by construction), marks in sync/index.ts (syncStart/scan/evaluate/compile/publish/syncEnd) + worker.ts (processStart via performance.timeOrigin, workerMain, workerEnd). Phases: startup (process→sync entry, MEASURED in-run — kills the empty-process subtraction), scan, evaluate, compile, publish, syncResidual (config+rmdir+gaps, honest remainder), workerTail. Per-leg same-run join: flame buckets main-thread samples by reconstructed absolute time (meta.startTime + cumsum(timeDeltas) — verified present on the filed profile: 1347 deltas, span 1515ms vs syncMs 1242.6); census shim gains an env-gated per-call event log (COUNTERS_CENSUS_EVENTS_OUT, static buffer, mach ticks + header unix anchor, chunked destructor write — schema-1 census untouched); spans cross-check compile phase in-run. New shared `phases.mjs` (schema/bucket/reconcile + JS-self caveat text); each leg files phases.json + a phases section; new `pnpm agentrs decompose` joins the three fresh bundles into docs/evidence/phases/enterprise-phases1/ (procedure agentrs-phases/1, sums verified, caveat stated). Procedure bumps: flame/3, counters/2, alloc/2 (additive phases; old bundles without phases still resummarize). New captures go to versioned dirs via --out (enterprise-flame3/-counters2/-alloc2), never overwriting prior bundles. No new Rust features, no loader changes, no bench-load changes.
- 2026-09-21 captain (tick): C-Obj 1 COMPLETE, C-Objs 3–6 QUEUED; deadlock test passed outright on first tick — phase-crew live (orientation + design-locked entries, tree products across harness + neo sync path). No watch set. Captain's verify-time notes: (1) crew is in the REAL sync path (sync/index.ts + worker.ts marks, env-gated, claimed inert when unset) — highest-sensitivity surface yet; verify inertness firsthand (unset-run bench delta + agent-neo gate on neo files); (2) procedure bumps flame/3 + counters/2 + alloc/2 with old-bundle compat claim — verify a v1 resummarize still works; (3) new `decompose` subcommand + phases.mjs + evidence/phases/ is scope beyond bare boundaries — judge on utility at verify; (4) worker.ts coupling: C-Obj 4's sampler builds on whatever marks land here — keep the diff reviewable. No action, crew undisturbed.
- 2026-09-21 phase-crew: ALL LEGS GREEN at small scale + full decompose E2E. phases.ts + sync/worker marks (config split out after first probe showed residual 23.6ms → now 0.05ms); shared phases.mjs; flame/3 (per-phase weights ≈ ms, top libs sane: startup→node, compile→.node, scan/publish→kernel); counters/2 (event log, both legs RECONCILED 0.000ms); alloc/2; decompose joins 5 runs with re-verification from raws. q clean everywhere (0/0); neo gate 0 errors + 1 non-failing warn (sync fn 87 lines vs 80 warn — the marks tripped it, accepted: refactoring the voyage path to appease a warn line is worse). Resummarize proven both ways (old /1 → no phases section, C-Obj 1's resolve_alias 26/43 intact through my refactor; new /3 → section preserved, phases.json byte-exact). Bench small probe green, frozen 60/171/seed-7 intact (note: the probe refreshed volatile reports/latest scratch — Fasthull-owned, inherently overwritten-every-run, no further bench runs planned).
- 2026-09-21 phase-crew: SURPRISES. (1) Pre-constructor shim hole: interposition goes live before the dylib constructor, so dyld's own open/fstat/mmap/close counted in the census with no events (caught by checkEventsAgainstCensus: fstat 85 vs 87). Fix: record unconditionally, gate only the destructor's file write — all 15 calls now bit-exact, preMain bucket shows the 7 dyld calls honestly. (2) mmap lengths exceed uint32 (V8 multi-GB reservations) — widened the event struct to full uint64. (3) Small-scale GC firing (6sc/1mc) is pre-existing tree behavior, not my marks: identical counts with the env var off across toggles (Obj 2's "zero GCs at small" note has drifted with the tree). (4) Compile-phase marshal delta is real and visible: compile − span ≈ 4.4ms (counters) / 4.7ms (alloc) at small — N-API + JSON cost the old window never showed. (5) Trace-leg compile carries alloc-counting overhead (30.6 vs ~23-26) — spread column shows it, canonical stays the shipped binary.
- 2026-09-21 phase-crew: ENTERPRISE FILED. `sync ≈ 1230.1 = config 30.6 + scan 364.3 + evaluate 6.2 + compile 780.3 + publish 48.7 (+ residual 0.0)`, startup 118.1 outside sync, worker 1348.2. Bundles: flamegraph/enterprise-flame3 (weights ≈ ms every phase: 118/25/365/6/778/49; scan 295/365 kernel, compile 336/778 .node), counters/enterprise-counters2 (span 740.0 vs compile 768.4; 9.539B instr IPC 2.36; 67,601 events 0 dropped, buckets bit-exact vs schema-1 on all 15 calls), alloc/enterprise-alloc2 (14sc/2mc, PASS 1-head-1-tail; span 994.2 vs compile 1024.4; reallocs 1,117,428 + peak 152.2MB bit-stable vs Obj 2), phases/enterprise-phases1 (decomposition.md/.json + meta, caveat stated, 5/5 legs RECONCILED worst delta 0.000). Provenance: shipped .node 5ac6c07e bit-identical across all three + Obj 1/2 (four objectives, one binary); counters d54c6b3d + trace 92deddbd match the filed Obj 4/2 shas (cache reused, zero drift). v1 comparability: net open 15407→15409 (+2 = the phases.ts worker import), openat 27→27 exact, reads 30968→30969. New facts for recon v2: marshal delta (compile − span) 28.5/30.2ms ≈ 2.3% of sync; compile-phase fs work exists (3,758 calls, 9ms, stat-led); flame-leg startup 118 vs 84–101 elsewhere (profiler + first-run coldness — recon v2 should cite the range, not the top); trace-leg compile +30% counting overhead (spread shows it, canonical stays shipped). Prior bundles byte-untouched (verified via git status).
- 2026-09-21 phase-crew: VERIFIED. q 16/16 files 0/0; neo gate 0 errors + 1 accepted non-failing warn; sync.test.ts 18/18; bench small green on frozen 60/171/seed-7; GC-with/without-env probe identical (small-scale GC firing is tree drift, not marks); resummarize both ways; decompose re-derives every filed number from bundle raws before joining (any stale/unreconciled input throws). No commit from crew. DONE — acceptance met, log current.
- 2026-09-21 captain: VERIFIED FIRSTHAND — `q` 16/16 clean (0/0); neo gate 0 errors + the 1 accepted non-failing warn (sync 87 lines — crew's call stands: refactoring the voyage path for a warn line is worse); sync.test.ts suite PASSED firsthand; full E2E chain exit 0 (flame + counters + alloc + decompose, every leg RECONCILED, decompose worst delta 0.000); resummarize both ways (v1 → merged/weighted rows, no phases section — the lone 'phase' grep hit is the procedure-changelog line; v3 → section preserved, phases.json byte-IDENTICAL); prior bundles unmodified; filed decomposition matches crew log; procedures flame/3 + counters/2 + alloc/2 + phases/1 with shipped sha 5ac6c07e in all three (four objectives, one binary); v1-compat net opens 15409 = 15407 + 2 (phases.ts import, as claimed). Sync-path diff reviewed: marks-only + env-gated (module-level ENABLED, one branch when unset; worker write after sample sealed; finally-block addition behavior-neutral) — inertness accepted on code + crew bench/GC probes + my test run. Scope verdict on `decompose`: KEEP — re-derives from raws and throws on stale input, making reconciliation checkable rather than asserted. Arc committed, brief checkbox ticked, C-Obj 2 COMPLETE. Note: my E2E refreshed the volatile reports/latest scratch (Fasthull-owned, overwritten-every-run) — left for that session.
- 2026-09-21 phase-crew: HANDOFF — captain re-run firsthand (exact): `pnpm agentrs q` on the 16 files (flame.mjs, flame-evidence.mjs, flame-summary.mjs, alloc.mjs, alloc-evidence.mjs, alloc-phases.mjs, counters.mjs, counters-census.mjs, counters-evidence.mjs, counters-summary.mjs, counters-phases.mjs, phases.mjs, decompose.mjs, decompose-verify.mjs, decompose-render.mjs in .agents/skills/agent-rs/scripts/ + packages/reference-neo/src/sync/phases.ts); `pnpm agentneo q packages/reference-neo/src/sync/phases.ts packages/reference-neo/src/sync/index.ts packages/reference-neo/benchmark/measure/worker.ts`; `pnpm agentrs flame -- small --out /tmp/captain-c2-flame && pnpm agentrs counters -- small --out /tmp/captain-c2-counters && pnpm agentrs alloc -- small --out /tmp/captain-c2-alloc && pnpm agentrs decompose --flame /tmp/captain-c2-flame --counters /tmp/captain-c2-counters --alloc /tmp/captain-c2-alloc --out /tmp/captain-c2-decomp` (all exit 0, RECONCILED lines); `pnpm agentrs flame --resummarize docs/evidence/flamegraph/enterprise-latest --out /tmp/captain-c2-resum1` (exit 0, no phases section) + `pnpm agentrs flame --resummarize docs/evidence/flamegraph/enterprise-flame3 --out /tmp/captain-c2-resum3` (phases section preserved); `git status` shows no modifications under the four prior bundles; `samply load docs/evidence/flamegraph/enterprise-flame3/profile.json.gz` to view. Review focus per the captain's tick: sync/index.ts + worker.ts diffs are marks-only + env-gated (inertness: bench probe + GC toggle + 18/18 sync tests above); decompose is the join that makes the reconciliation checkable rather than asserted.
