COMPLETE — C-Obj 4: independent RSS sampler, versioned procedure
=============================================================

Mission: Operation Flamegraph Correct (`docs/missions/operation-flamegraph-correct.md`), issue 4.
Acceptance: RSS measured independently of the blocked event loop;
fix lands behind a versioned bench procedure so history (incl. active
Fasthull numbers) stays comparable. CROSS-MISSION: `worker.ts` is the
voyage's shared scorer — minimal diff, dual-report or version pin,
no silent rescoring of other missions' numbers.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- rss-crew (lead): dispatched 2026-09-21. Owns this objective. May fan nested
  workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (check `git status` first — Fasthull and
  neo-report dirt belongs to another session).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, queued behind C-Obj 3.
- 2026-09-21 captain: C-Obj 3 COMPLETE and committed; C-Obj 4 opened, crew dispatched.
- 2026-09-21 rss-crew: orientation done. Tree: only foreign dirt is fasthull + neo latest-report (hands off — bench E2E will be backup/restore or avoided); mission files + this log are mine. Skills loaded (agent-rs, benchmark, agent-neo gate for the neo touch); brief, C-Obj 2 log/procedure precedent, Obj 2/4 logs + all worker consumers absorbed. Working inline, no nested workers (one sample schema couples worker + child + report; C-Obj 2 precedent).
- 2026-09-21 rss-crew: DEFECT CONFIRMED from filed evidence. Counters enterprise-latest: worker rssPeak 322,244,608 B (307.3 MiB) vs same-process span-exit getrusage maxRss 333,537,280 B (318.1 MiB) — the OS high-water exceeds the in-loop sampler by 10.8 MiB (3.5%) because setInterval cannot fire while the thread blocks in native compile. Consumers traced: measure/worker.ts output feeds exactly three parsers — bench cli via runChild (STRICT isWorkerSample), alloc.mjs + counters.mjs parseWorkerSample (TOLERANT, needs only syncMs). Deepsee is fully decoupled (own worker-phases.ts + own parsers, already dual-reports parentPeak); nothing reads back reports/ programmatically. Fasthull scores v1 median peak RSS from report pins (locked: 123/202/768 MiB small/medium/enterprise) — the v1 computation must stay byte-identical.
- 2026-09-21 rss-crew: DESIGN LOCKED — procedure bench-worker/2. MECHANISM: OS high-water read in worker.ts (`process.resourceUsage().maxRSS`, one read after the sample seals). Justification: (1) kernel-maintained on every page fault — maximally independent of the event loop, cannot miss a peak no matter how long the thread blocks; (2) closes the exact demonstrated gap with the exact demonstrated primitive (filed maxRss above); (3) zero perturbation — one syscall post-sync, no extra thread/isolate RSS in the measured process, no grandchild, no ps cadence in the measured path; (4) minimal diff (~5 lines) flowing to all three consumers through the existing JSON line with zero harness changes. REJECTED: worker-thread sampler (isolate adds MBs of baseline RSS to the measurand + most complex); parent-side ps polling as the shipped scorer (bench-path only — alloc/counters spawn worker directly — bigger diff; kept as the /tmp validation probe instead, deepsee-precedented). UNITS probed firsthand: resourceUsage().maxRSS is KiB on this box (fresh node 32656 ≈ rss 33.4MB; +120MB alloc → +123320) — worker multiplies by 1024 with the probe cited; dual-report makes any future platform drift visible. SCOPE caveat explicit: lifetime HW vs sync-window peak — validation probe (parent ps-poll) will show they coincide because sync peak dominates startup 2-4x on locked loads. VERSIONING: worker emits `scorer: 'bench-worker/2'` + `rssPeakHw`; v1 = the four-field shape, sampler lines literally untouched. child.ts WorkerSample requires both new fields (typeof checks only, no exact-match — a future v3 worker flows through and the record states its own procedure). RunRecord gains `scorer` derived from the samples (fallback 'bench-worker/1' = no scorer field, by definition). Report dual-reports: v1 `peak RSS` stays the headline everywhere (Fasthull's locked metric — new v1 medians compare directly against the wave-start pin), `peak HW` beside it as a NEW series that must never be compared against old RSS. Recon v2 / voyage owns the eventual cutover, not this objective.
- 2026-09-21 rss-crew: LANDED. worker.ts (+9/-1: SCORER const, header +2, post-seal maxRSS read, two JSON fields — sampler lines literally untouched, see diff), child.ts (+6: two WorkerSample fields + two typeof checks, no exact-match), markdown.ts (RunRecord.scorer, medianPeakHw, peak-HW column in both tables, per-scale bullet, comparability legend), write.ts (recordScorer from samples, v1 fallback), benchmark SKILL.md (contract +2 lines). Neo gate 0 errors 0 warnings on all 4 files. No harness/Rust changes — alloc + counters parsers are tolerant and needed none.
- 2026-09-21 rss-crew: OLD-VS-NEW ON THE SAME LOAD (locked: frozen generator, seed 7, 10ms cadence, /tmp/rss-probe.mjs direct worker runs + ps parent-poll; v1 = HEAD worker to a temp file, deleted after). small sandwich v2/v1/v2: peaks 109.3/101.9/107.9 then 107.6/109.8/110.4 — v1-vs-v2 deltas are run-to-run noise (pre-seal code paths identical), HW == peak every run (peak persists to the seal). medium: v2 peak 130.7 HW 131.5 (+0.8 caught live) parent 130.7 (poll missed too); v1 126.5/126.5. enterprise shipped: v2 peak 344.0 HW 344.1 parent 343.9; v1 345.9/345.8 (broad peak, sampler lucky). enterprise instrumented (counters span leg, the filed scenario): run 1 peak 350.7 HW 350.9 span-exit-maxrss 330.1; run 2 peak 335.2 HW 335.4 span-max 315.4 — sampler caught both (miss 0.1-0.2), HW confirms. Filed bundles bound the miss the other way: enterprise-latest 307.3 vs 318.1 (+10.8), enterprise-counters3 343.7 vs 347.4 (+3.8) — same primitive now shipped in the worker. Census leg + alloc gc leg carry HW + scorer with zero harness edits (tolerance proven: full counters enterprise x2 exit 0 RECONCILED, alloc small exit 0 with designed AMBIGUOUS-edge). Bench E2E (`bench:neo --scale small --runs 1 --json`) renders dual-report + legend and result.json carries scorer bench-worker/2 end to end; foreign reports/latest scratch backup/restored byte-identical (md5 match).
- 2026-09-21 rss-crew: SURPRISES (all recon-v2-grade, none blocking). (1) The peak MOVES between phases run to run: my two instrumented runs grew ~20MB AFTER compile exit (spanMax 330.1/315.4 vs HW 350.9/335.4 — marshal + async publish, sampler caught both because the loop turns on file writes) while the filed C-Obj 3 run peaked DURING compile (spanMax 347.4 > sampler 343.7, missed 3.8). So the old miss is INTERMITTENT, not a stable bias — it noises Fasthull's scored metric, not just offsets it. (2) Same-load peak varies ±15MB run to run (350.9 vs 335.4, same seed/bytes/binary) — allocator noise; recon v2 should state the spread, Fasthull's spread rule already absorbs it. (3) Parent ps-poll UNDERSAMPLES: missed 2.4MB at small (105.5 vs 107.9) and matched the in-loop miss at medium — polling is not the fix, kernel HW is; deepsee's parentPeak stays diagnostic-only. (4) rssAfter << peak on instrumented runs (322.3/313.2) — the peak is transient either way; only HW sees all of it. Scope caveat CLOSED: lifetime HW == sync-window peak in practice (startup ~95MB vs ~340MB peak, tail trivial; the span-vs-worker delta is intra-sync phase movement, legitimately scored).
- 2026-09-21 rss-crew: HANDOFF — captain re-run firsthand (exact): `pnpm agentneo q packages/reference-neo/benchmark/measure/worker.ts packages/reference-neo/benchmark/measure/child.ts packages/reference-neo/benchmark/report/markdown.ts packages/reference-neo/benchmark/report/write.ts` (0/0); `git diff packages/reference-neo/benchmark/measure/worker.ts` (sampler lines untouched — review focus); `node /tmp/rss-probe.mjs small` (needs the v1 temp: `git show HEAD:packages/reference-neo/benchmark/measure/worker.ts > packages/reference-neo/benchmark/measure/worker-v1probe.ts`, delete after); `pnpm agentrs counters -- enterprise --out /tmp/captain-c4-counters` then compare meta spanLeg sample rssPeak/rssPeakHw vs counters-span.json after.rusage.maxRss; one bench E2E with backup/restore of reports/latest (md5 before/after). No commit from crew. DONE — acceptance met, log current.
- 2026-09-21 captain: VERIFIED FIRSTHAND — neo gate 0/0 on all 4 scorer files; worker.ts diff reviewed (sampler lines literally untouched; +9/−1: header, SCORER const, post-seal maxRSS read, two JSON fields); child.ts strictness reviewed (requires new fields — old→new worker/parser mixing breaks, but worker+parser ship version-locked in one tree, nothing replays old samples, nothing reads reports/ programmatically: accepted); markdown dual-report reviewed (v1 medianPeakRss untouched, HW separate + never-compare legend); v1-vs-v2 probe sandwich firsthand (v2 scorer + hw flow, deltas are run noise, parent poll undersamples as crew found); DECISIVE enterprise counters run exit 0, RECONCILED: worker rssPeak 329.6 vs rssPeakHw 336.3 vs independent span-exit maxRss 336.3 EXACT — HW caught +6.7 the sampler missed and agrees exactly with the independent read; bench E2E dual-report + scorer end-to-end in result.json. INCIDENT (captain's fault, logged honestly): my E2E overwrote the other session's volatile reports/latest scratch and I restored via `git checkout` (HEAD content) instead of file-copy backup/restore — their uncommitted scratch numbers are replaced by committed versions. Harm bounded: volatile regenerate-every-run scratch, Fasthull scores from pins not latest/, nothing reads it programmatically, their next bench run regenerates it. Lesson: shared scratch gets file-copy backup/restore (as the crew did), never checkout. Arc committed, brief checkbox ticked, C-Obj 4 COMPLETE.
