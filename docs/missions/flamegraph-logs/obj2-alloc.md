COMPLETE — Obj 2: allocation report on the enterprise compile
==========================================================

Mission: Operation Flamegraph (`docs/missions/operation-flamegraph.md`).
Acceptance: dhat (or equivalent) on the Rust side — reachable-live vs.
transient vs. allocator-resident during compile — reproducing the R1
post-mortem findings (zero mark-compacts, unscored reachable delta) by
measurement. Runner-backed, evidence in standard locations.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- alloc-crew (lead): dispatched 2026-09-21. Owns this objective. May fan nested
  workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (known-dirty: operation-fasthull.md — other session
  actively working it — plus neo benchmark latest/ report).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, queued behind Obj 1.
- 2026-09-21 captain: Obj 1 COMPLETE and committed; Obj 2 opened, crew dispatched.
- 2026-09-21 alloc-crew: R1 post-mortem ingested (VOYAGE-HYPERSPACE-LOG.md hunter-r death + lane notes in ../reference-ui-perf-4-r, read-only). Worked example: chunked-vs-single handoff at enterprise, `--trace-gc` whole-run 12sc/0mc BOTH paths, nulled old-space resident through scored pre-cliff peak, ~17MB reachable-live delta by byte accounting (15,122 files: contents 4.16 + paths 0.57 + request JSON 5.19 MiB + array slack) real but UNSCORED. Note: voyage/hyperspace-perf-4-r branch tip is the dispatch base (c84dd4d7a) — R1's diff survives only as uncommitted dirt in the lane tree, not on the branch. DESIGN LOCKED for `pnpm agentrs alloc` (procedure agentrs-alloc/1, evidence docs/evidence/alloc/<scale>-<pin>/): two legs over ONE frozen repo (generator + worker verbatim, worker does ~nothing but sync so whole-run GC counts ~= handoff window, same as R1's method). Leg 1 GC (AS-SHIPPED release .node via ensure-native): `node --trace-gc worker.ts` → gc.log + gc.json (scavenge vs mark-sweep/compact counts) + worker RSS. Leg 2 Rust-alloc (INSTRUMENTED release+alloc-trace .node, separate artifact under dist/native-trace/<inputs-hash>/, never clobbers dist): feature-gated counting GlobalAlloc in modules/runtime + RAII span guard at compile_system entry/exit dumping rust-alloc.json (span alloc/free/peak/live-at-end, request byte accounting, macOS malloc_zone_statistics slack) when ALLOC_TRACE_OUT is set; loader gains REFERENCE_UI_NATIVE_PATH override seam (zero behavior change when unset). No new deps, no dhat crate (builds the equivalent: totals exact, no per-callsite stacks — accepted scope). Modules mirror flame: alloc.mjs + alloc-gc.mjs + alloc-evidence.mjs, cpu-gate rs:build for trace build then rs for capture. Working inline, no nested workers (contract between Rust/loader/harness too coupled to split).
- 2026-09-21 alloc-crew: harness built, small trial green. `pnpm agentrs alloc -- small --out /tmp/alloc-trial-small` exit 0: ensure-native (shipped) → trace build (napi --features alloc-trace, hash-cached under dist/native-trace/) → frozen repo (60/171/seed-7) → GC leg + trace leg → meta.json, gc.log/json, rust-alloc.json, trace-stderr.log, summary.md. Modules: alloc.mjs (orchestrate) + alloc-build.mjs (artifacts) + alloc-gc.mjs (trace-gc parser) + alloc-evidence.mjs (meta/report), run.mjs wiring mirrors flame. Rust: alloc_trace.rs counting GlobalAlloc + span guard at compile_system (cfg'd, zero-cost off) + getAllocTrace probe; loader REFERENCE_UI_NATIVE_PATH seam + 2 tests. Gates: q 0 violations everywhere (loader.ts warns pre-existing), clippy silent on new code, loader 10/10, atomic cargo 567+1/567+1. Surprises: (1) `--trace-gc` prints to STDOUT not stderr — first enterprise file read the wrong stream and reported a bogus 0-event census; fixed + parser validated against squeezed-semispace worker output (16 events incl. Mark-Compact). (2) malloc_zone reserved can exceed RSS (region chunks, never-faulted pages) — report uses enter→exit deltas (small: +1.1/+1.1 MiB) with the caveat stated. (3) small/medium runs fire zero GCs — the census only exists at enterprise scale.
- 2026-09-21 alloc-crew: R1 VERDICT SAGA — real enterprise measurement is 14sc/2mc whole-run (shipped, 4/4 runs identical), NOT R1's 12sc/0mc. Timestamps: head MC at t≈110 (heap ~11MB, startup) + tail MC post-sync freeing ~34MB (51.7→17.8, the cliff). So the whole-run COUNT moved post-Wave-4, but R1 turns on the WINDOW: trace leg now runs --trace-gc too and the span carries unix start/end, placing each full GC from the same process against the measured window with a one-sided 60ms spawn-latency bound (calibrated 28-34ms spawn-to-first-JS; error can only shift later). Result: 1 head + 1 tail, 0 in-window, 0 edge → MECHANISM REPRODUCED. Verdict semantics (in report + console + meta.traceLeg.window): PASS = 0 in-window full GCs; edge hits are AMBIGUOUS (re-run), never silent. Cross-checks: handed ledger matches R1 exactly (15,122 files, contents 4.16MB; JSON 5.87 vs 5.19 — spec grew); Rust span counts bit-stable 4/4 (946.6MB alloc / 938.6MB freed / 13.9MB live / 152.2MB peak / 1.117M reallocs). Wave-5-grade finding: Rust span peak (152MB) is ~58% of sync-added RSS — the scored peak is Rust-transient-dominated, not JS-reachable.
- 2026-09-21 alloc-crew: BASELINE FILED — `pnpm agentrs alloc -- enterprise` exit 0. Evidence: docs/evidence/alloc/enterprise-latest/ (meta.json, gc.log/json, trace-gc.log/json, rust-alloc.json, trace-stderr.log, summary.md). Numbers: load 3000+12000 files, 7527 css() calls, 120 recipes, seed 7; gc leg syncMs 1386.3, rssPeak 355.9 MiB, rssAfter 333.9 (cliff 22.0); shipped census 14sc/2mc, V8 heap high-water 51.6MB; trace span 999.4ms, handed 11.3MB (4.2 contents + 1.2 paths + 5.9 JSON), zone compile delta +18.1 reserved / +7.3 in-use. Provenance: shipped .node sha256 5ac6c07e9f8d4a67… (BIT-IDENTICAL to Obj 1's filed sha — the feature-off instrument compiles to zero bytes in the shipped profile; ensure-native stamp fresh), trace .node release+alloc-trace sha d84a88d7…, node v24.16.0, darwin-x64, procedure agentrs-alloc/1. Pin=latest (tree dirty: my arc + other session's fasthull/neo-report edits, recorded in meta treeStatus). No commit from crew. HANDOFF — captain re-run firsthand: `pnpm agentrs alloc --list`, `pnpm agentrs q` on the 4 alloc scripts + alloc_trace.rs + loader files, `pnpm agentrs v modules/runtime/js/loader.test.ts`, `pnpm agentrs c atomic`, and `pnpm agentrs alloc -- small --out /tmp/alloc-verify`. Note: trace build reuses dist/native-trace/<hash>/ cache; dist rebuild needed once for the loader seam is already done on this box (preflight fails loud with the command otherwise).
- 2026-09-21 captain (tick): Obj1 COMPLETE, Objs 3–5 QUEUED; alloc-crew roster running, no log writes or tree products yet (first tick since dispatch — within grace, no ping). Watch set: zero writes/products by next tick fails the deadlock test → rebrief or replace.
- 2026-09-21 captain (tick): watch LIFTED — deadlock test passed. Alloc-crew live: design-locked entry (R1 ingested, two-leg agentrs-alloc/1 over one frozen repo, instrumented release+alloc-trace .node under dist/native-trace/) plus tree products (alloc*.mjs ×3, alloc_trace.rs, runtime/loader/Cargo wiring). Roster running. Captain's verify-time note: loader native-path seam + runtime feature gate touch shared surfaces — review diff closely on oracle word. No action, crew undisturbed.
- 2026-09-21 captain: VERIFIED FIRSTHAND — `alloc --list` (4 frozen scales); `q` 0 violations on 4 scripts + alloc_trace.rs + loader files (5 warnings all pre-existing loader.ts complexity, untouched functions); loader vitest 10/10; atomic cargo PASSED; firsthand `alloc -- small --out /tmp/alloc-verify` exit 0 (both legs, 8 evidence files, AMBIGUOUS-on-edge semantics firing as designed); filed report numbers match crew log (1386.3ms, 355.9/333.9 RSS, 14sc/2mc, 51.6MB heap HW, 999.4ms span PASS 1-head-1-tail, Rust 946.6/938.6/13.9/152.2); shipped .node sha bit-identical to Obj 1's filed sha (feature-off instrument = zero bytes); shared-surface diffs tight (2 cfg lines in native.rs, feature decl, 3-line loader seam + 2 tests, cfg-gated alloc_trace module/probe); run.mjs wiring mirrors flame. Drift noted: crew log prose cites trace sha d84a88d7… but filed meta records 92deddbd… (dist/native-trace/94d7a9b7177a/) — meta authoritative, likely rebuild-after-logging. Arc committed, brief checkbox ticked, Obj 2 COMPLETE.
