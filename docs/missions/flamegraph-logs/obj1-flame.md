COMPLETE — Obj 1: flame harness (`pnpm agentrs flame`) + enterprise flamegraph baseline
======================================================================================

Mission: Operation Flamegraph (`docs/missions/operation-flamegraph.md`).
Acceptance: samply harness behind `pnpm agentrs flame` (pinned procedure,
cpu-gate aware, agent-rs gates clean), output to evidence dir, plus one
enterprise flamegraph filed as the Wave 5 baseline. Profile the `.node` as
shipped (release profile through the real sync path); locked load (frozen
scales/seeds); say which is which.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- flame-crew (lead): dispatched 2026-09-21. Owns this objective. May fan nested
  workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (known-dirty at dispatch: operation-fasthull.md,
  neo benchmark latest/ report).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, crew dispatched.
- 2026-09-21 captain: flame-crew lead away (main/flamegraph-obj1-flame/1); health tick armed (every 25m). Tree note: fasthull + neo latest-report edits are another session's — hands off.
- 2026-09-21 captain: HQ check-in — crew running, no log writes yet (fresh dispatch, within grace). No ping per standing rule; next look on tick/crew word.
- 2026-09-21 flame-crew: harness built, small-scale trial green. `pnpm agentrs flame -- small --out /tmp/flame-trial-small` exit 0: ensure-native (release .node reused) → frozen repo (60 files, 171 css() calls, seed 7) → samply 0.13.1 record @1000Hz → meta.json + profile.json.gz + sidecar + summary.md. Quality gate clean on all 3 new modules (0 violations, 0 warnings). Design locked: bench generator + bench worker reused verbatim (locked load, no overrides), `--perf-basic-prof` for JS names, `--unstable-presymbolicate` sidecar for offline Rust names, cpu-gate `rs`, pin convention borrowed from bench. Surprises: (1) plain `node` under samply yields ZERO JS names — `--perf-basic-prof` is mandatory, adds `perf-<pid>.map` lib; (2) that flag also drops `isolate-*-v8.log` into CWD — harness now runs samply in a mkdtemp run dir with `--perf-basic-prof-path` + `--logfile` pointed inside, run dir removed after; (3) release `.node` ships an intact symtab (~19k entries, not stripped) so Rust names resolve with no rebuild; (4) run.mjs is 591+ lines and would fail its own gate if scanned — pre-existing, out of scope, harness kept to 3 small modules instead. Working inline, no nested workers (design and probes too coupled to split). Next: re-trial small after run-dir fix, then enterprise baseline to docs/evidence/flamegraph/enterprise-latest/ (tree dirty incl. another session's files, so pin=latest; meta records hash 68b020b2f6a1 + dirty status).
- 2026-09-21 flame-crew: DEAD END + fix — `--perf-basic-prof-path=<runDir>` silently loses ALL JS frames (samply only discovers /tmp/perf-<pid>.map; isJS funcs 145→0). Reverted to default map path with before/after-diff cleanup; v8.log stays contained via `--logfile=<runDir>/v8.log` + run-dir cwd. Small re-trial green: 145 isJS funcs, zero litter, gate clean.
- 2026-09-21 flame-crew: BASELINE FILED — `pnpm agentrs flame -- enterprise` exit 0. Evidence: docs/evidence/flamegraph/enterprise-latest/ (profile.json.gz 108260 B, profile.json.syms.json 300127 B, meta.json, summary.md). Numbers: load 3000 style files + 12000 dead, 7527 css() calls, 120 recipes, seed 7 (frozen enterprise plan, no overrides); worker syncMs 1242.6, rssPeak 360747008 (344 MiB); profile 1347 main-thread samples @1000 Hz. Lib self-share: kernel 29.4% (`__open` alone 18.0%), .node 26.9%, malloc 19.6%, node 13.6%, platform 8.2%, JS 1.6%. Top native self: RawVec reserve 9x, Atom== 7x, path compare_components 7x total, oxc add_module_request 3x. Provenance: release .node sha256 5ac6c07e9f8d4a67… via ensure-native (napi build --release), samply 0.13.1, node v24.16.0, darwin-x64, procedure agentrs-flame/1. Pin=latest (tree dirty: my uncommitted harness + other session's fasthull/neo-report edits, all recorded in meta treeStatus; hash 68b020b2f6a1). Handoff: captain re-run `pnpm agentrs flame --list`, `pnpm agentrs q <3 flame modules>`, and `pnpm agentrs flame -- small --out /tmp/flame-verify` firsthand; view via `samply load docs/evidence/flamegraph/enterprise-latest/profile.json.gz`. Optional post-commit: re-run on clean tree for a hash-pinned `enterprise-<hash>/` dir. No commit from crew. Mission-brief checkbox left for captain.
- 2026-09-21 captain: VERIFIED FIRSTHAND — `flame --list` (4 frozen scales); `q` gate clean on all 3 modules; firsthand `flame -- small --out /tmp/flame-verify` exit 0 (frozen 60/171/seed-7 load, release .node reused, real Rust symbols, evidence written); baseline meta honest (procedure agentrs-flame/1, pin latest + full dirty treeStatus, release .node sha, samply 0.13.1); run.mjs diff 20 lines wiring only. Numbers match crew's log. Arc committed, brief checkbox ticked, Obj 1 COMPLETE. Standing note: hash-pinned enterprise re-run on a clean tree remains optional.
