COMPLETE — Obj 3: criterion micro-benches for top hot functions
=============================================================

Mission: Operation Flamegraph (`docs/missions/operation-flamegraph.md`).
Acceptance: criterion benches for ≥5 hot functions (resolve, plan build,
census, line index, ladder match, …), green in CI-adjacent runs
(`pnpm agentrs c` family). Lanes must prove a win at unit scale before
paying for full bench runs.

Roster:
- captain: holds context, verifies on oracle word, commits. Crews never commit.
- bench-crew (lead): dispatched 2026-09-21. Owns this objective. May fan nested
  workers over disjoint pieces. Writes here; never commits; never touches
  another session's files (known-dirty: operation-fasthull.md — other session
  actively working it — plus neo benchmark latest/ report).

Entries: (append; newest last)
- 2026-09-21 captain: log opened, queued behind Obj 2.
- 2026-09-21 captain: Obj 2 COMPLETE and committed; Obj 3 opened, crew dispatched.
- 2026-09-21 bench-crew: DESIGN LOCKED. Inclusive-stack analysis of the filed enterprise flame (1347 main samples; map in crew notes, method: /tmp/flame-inclusive.mjs reusing flame-summary.mjs symbolication) grounds the slate. Mission names confirmed: resolve = `atomic::resolve::resolve_want_with` (133 incl); plan build = `atomic::assembly::AssembleCtx::finish` (299) / `PlanBuilder::build_keyed` (83); census = `atomic::extract::resolver::staging::StagingPlan::census` (exists, 3 incl — small but named, benched honestly); line index = `LineIndex::{for_source,line_col}` (filed self top-20); ladder match = `module_graph::SpecifierLadder::resolve` (flame: BindingWalk follow_specifier/edge 27 each). Evidence additions: `canon::dialect::resolve_alias` (43/26 — hottest our-self frame), `canon::css::find_property` (30/12), `classify_css_value` (39/1), `Atom::eq` (7 self). Conventions (none existed): criterion 0.8 (0.8.2 cached, offline build verified — crates.io is 403 from this box), `criterion.workspace` dev-dep, benches in `<crate>/benches/*.rs` with `[[bench]] harness=false test=true`. `test=true` VERIFIED on a probe crate: plain `cargo test` (the `pnpm agentrs c` path) compiles and smoke-runs each bench once ("Testing <name> / Success"); full numbers via cargo bench. No runner change needed for the acceptance path; lead additionally landed `pnpm agentrs bench <crate> [--bench <n>] [filter]` (cpu-gate `rs`, module-graph addressable — `c` never learned it) so lanes have a canonical gated invocation. Fan-out: worker-A atomic (resolve, plan build, census, line index, +Atom eq stretch), worker-B canon (alias, find_property, classify), worker-C module-graph (ladder). Workers own only their crate dir; shared edits (workspace Cargo.toml criterion line, run.mjs bench command) are lead's, already landed. Next: workers land benches + noise floors; lead integrates, runs workspace `c`, files noise floor.
- 2026-09-21 bench-crew: WORKERS GREEN. A: atomic/benches/{resolve (token/rhythm/shorthand/unit/conditioned + atom_eq hit/3 miss), plan_build (build 19.5µs / build_diet 15.5µs), census (graph_12 0.85ms / graph_48 1.35ms whole-compile), line_index (for_source 13.3µs, queries ~11ns, scan_end 10.4µs ≈900× indexed)} — `c atomic` 567+1 green, 18/18 smoke lines. Chose public `PlanBuilder::build`/`build_diet` (finish/build_keyed/resolve_entry all private). B: canon/benches/{alias (7 cases ~23–48ns), property (7 cases ~28–46ns), classify (9 cases ~10–230ns)} — `c canon` 54 green, 23/23 smoke lines. C: module-graph/benches/ladder.rs (5 cases 0.68–4.9µs over fixed 34-file MemoryFs) — full-workspace `c` 36/36 green incl. 5/5 smoke lines. Dead ends: `StagingPlan` + `census` are `pub(crate)` (verified staging.rs:138,145) so no true census micro-bench without widening visibility — NOT done: census is 3/1347 flame samples and the evidence doesn't justify API churn; the census bench is honestly a census-shaped whole-compile regression instrument (0.9–1.6% spread) + profiler attribution fixture, header says so. Unscoped `cargo bench -- --sample-size N` exits 101 (cargo forwards `--` args to lib unittests which reject the flag) — lanes must scope with `--bench <name>` first.
- 2026-09-21 bench-crew: INTEGRATED + VERIFIED FIRSTHAND. Lead fixes: `criterion::black_box` → `std::hint::black_box` in atomic's 4 files (deprecated in 0.8, C caught it); trimmed redundant `serde_json`/`smallvec` dev-deps from atomic (already main deps, benches link them). Gates: `q` on all 8 bench files 0 violations; full `pnpm agentrs c` 36/36 ok + 46/46 bench smoke Success (18 atomic + 23 canon + 5 ladder); zero warnings from bench files (remaining warnings all pre-existing src/native.rs). Firsthand `bench` reproduction in-band on all 3 crates (resolve token 1.103µs vs 1.107–1.126; alias hit_hot 25.05ns vs 25.18–25.62; ladder miss 4.91µs vs 4.92–4.98). NOISE FLOOR (3-run spreads, contention noted): plan/census ms-scale ≤1.6% → resolves ≥2%; resolve/ladder µs-scale typically ≤4.5% (ladder extensionless_probe hit 21% once under sibling-build contention) → resolves ≥5% idle, ≥10–20% under shared-CPU load; canon ns-scale 17/23 ≤5% (typical ±2%), 6 cases single-run excursions to 28% under contention → <10% noise on starred cases, <3% elsewhere. Lane rule: re-run baselines on an idle box, treat <5% as noise (<2% on plan/census). Tree: 8 bench targets / 46 cases / 9 evidence-backed functions; diff = 8 new bench files + 4 manifest deltas (workspace criterion line + 3 crate dev-dep/[[bench]] blocks) + Cargo.lock + run.mjs bench command + this log. Other session's files untouched. HANDOFF — captain re-run firsthand: `pnpm agentrs c` (46 smoke lines), `pnpm agentrs q` on the 3 benches dirs, `pnpm agentrs bench atomic --bench resolve`, `pnpm agentrs bench canon --bench alias`, `pnpm agentrs bench module-graph`. No commit from crew.
- 2026-09-21 captain (tick): Objs 1–2 COMPLETE, Objs 4–5 QUEUED; bench-crew roster running, no log writes or tree products yet (first tick since dispatch — within grace, no ping). Watch set: zero writes/products by next tick fails the deadlock test → rebrief or replace.
- 2026-09-21 captain (tick): watch LIFTED — deadlock test passed. Bench-crew live: design-locked entry (inclusive-stack slate from filed flame, 3 workers over disjoint crates atomic/canon/module-graph), tree products (benches/ ×3 crates, workspace + crate Cargo wiring, run.mjs `bench` command). Roster running. Captain's verify-time notes: (1) `pnpm agentrs bench` is lead scope-stretch beyond the `c`-path acceptance — judge on utility, likely keep; (2) criterion 0.8.2 from offline cache (crates.io 403 on this box) — confirm Cargo.lock pins it and the `c` path builds clean firsthand. No action, crew undisturbed.
- 2026-09-21 captain: VERIFIED FIRSTHAND (tick converged with verify) — full `pnpm agentrs c` exit 0 with exactly 46 Success smoke lines; `q` clean on all 8 bench files (0 violations, 0 warnings); firsthand `bench atomic --bench resolve` (token 1.1039µs in crew's band), `bench canon --bench alias` (hit_hot 25.069ns in band), `bench module-graph` (ladder cases in slate range); Cargo.lock pins criterion 0.8.2, workspace `criterion = "0.8"`, manifests exactly the claimed convention; census.rs header honestly states the pub(crate) limitation; no deprecated black_box; run.mjs bench wiring cpu-gate wrapped with help + dispatch. Scope verdict on `pnpm agentrs bench`: KEEP — canonical gated lane invocation, module-graph reachable where `c` can't go, matches the runner-backed mission spirit. Arc committed, brief checkbox ticked, Obj 3 COMPLETE. Standing note: lanes must scope `cargo bench -- --sample-size N` with `--bench <name>` (unscoped exits 101) — crew-logged.
