/**
 * Reference Icons — minimal Reference UI config for `ref sync`.
 */

import { defineConfig } from '@reference-ui/core'

export default defineConfig({
  name: 'reference-icons',
  include: ['src/**/*.{ts,tsx}'],
  extends: [],
  debug: false,
})
