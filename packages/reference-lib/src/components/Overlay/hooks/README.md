# hooks

Root lifecycle, used only by `Overlay.tsx`.

- `use-open-state` — controlled / uncontrolled `open`
- `use-part-registry` — one of each part; mixed `edge` + `anchor` is a diagnostic
- `use-overlay-layer` — push / pop the document stack
