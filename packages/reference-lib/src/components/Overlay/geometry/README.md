# geometry

Unbound writes no coordinates. Anchored uses Trigger only when isolation focus is off. `edge` binds to a viewport side.

- `reference` — resolve the floating reference
- `floating` — computePosition, autoUpdate
- `clipping` — hide flags from overflow ancestors
- `edge` — sheet insets and `--reference-overlay-index`
- `use-position` — Content layout effect
