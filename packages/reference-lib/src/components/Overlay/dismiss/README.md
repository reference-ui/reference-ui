# dismiss

Document listeners for Escape and outside press.

- `inside` — Content, Trigger, Field bezel, ignore markers, and descendant layers
- `listeners` — one bind per document; click is bubble so extension overlays can abort a deferred sequence
