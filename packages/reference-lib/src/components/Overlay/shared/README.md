# shared

Utilities used by more than one Overlay module.

- `events` — composed path, containment, primary pointer, consumed flags
- `refs` — `assignRef`, stable `useComposedRef`
- `warn` — dev-only `console.error`
