# stack

Document-scoped layer store.

- `types` — `Layer` shape
- `query` — top live layer, descendants, pointer-events enablement
- `handlers` — dismiss / escape / outside callbacks for a layer id
- `store` — zustand registration, cascade, two-frame removed tombstone
