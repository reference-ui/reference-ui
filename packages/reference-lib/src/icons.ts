/**
 * Re-exports Reference icons for apps that depend on `@reference-ui/lib` only
 * (for example the docs site), so they do not need a direct `@reference-ui/icons` dependency.
 */
export {
  BoltIcon,
  CodeIcon,
  FavoriteIcon,
  HomeIcon,
  PaletteIcon,
  SearchIcon,
  SettingsIcon,
  TerminalIcon,
} from '@reference-ui/icons'
