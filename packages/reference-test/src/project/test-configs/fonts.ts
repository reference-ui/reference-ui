/**
 * Font test configurations.
 * Stub for future expansion - MVP uses tokens only.
 */

import type { FontConfig } from './types.js'

export const minimalFonts: FontConfig = {
  fontFamily: {
    sans: { value: 'system-ui, sans-serif' },
  },
}
