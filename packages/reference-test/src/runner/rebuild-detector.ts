/**
 * Detects when watch mode completes a rebuild.
 * Stub for MVP - watch mode comes in later phase.
 */

export async function waitForRebuild(_timeoutMs?: number): Promise<void> {
  throw new Error('waitForRebuild not yet implemented for MVP')
}
