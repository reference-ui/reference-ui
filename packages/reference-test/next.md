# Project

we need to generate a project inside of sanbdo

## Running tests

If `ref sync` fails with "Virtual native addon not available", run first:

```bash
cd packages/reference-core && pnpm run build:native
```