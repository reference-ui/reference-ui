{
  "name": "@reference-ui/reference-test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test:prepare": "tsx src/prepare.ts",
    "test": "REF_TEST_WORKERS=3 pnpm run test:prepare && REF_TEST_WORKERS=3 tsx src/run-matrix.ts",
    "test:ui": "pnpm run test:prepare && REF_TEST_PROJECT=react18-vite5 playwright test --ui --project=react18-vite5"
  },
  "dependencies": {
    "execa": "^9.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "tsx": "^4.19.0",
    "@reference-ui/core": "workspace:*",
    "typescript": "~5.9.3"
  }
}
