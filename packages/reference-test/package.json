{
  "name": "@reference-ui/reference-test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test:prepare": "tsx src/prepare.ts",
    "test": "pnpm run test:prepare && playwright test",
    "test:ui": "pnpm run test:prepare && playwright test --ui"
  },
  "dependencies": {
    "execa": "^9.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "tsx": "^4.19.0",
    "@reference-ui/core": "workspace:*",
    "typescript": "~5.9.3"
  }
}
