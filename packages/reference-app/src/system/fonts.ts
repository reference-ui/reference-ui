/**
 * Font definitions for the reference app.
 * Font module removed: config only has extendPandaConfig collector (see reference-cli system/fragments.md).
 * This file is kept so styles.ts can still import it; re-add font()/extendFont when the fragment pipeline is restored.
 */
