# Build outputs
dist/

# CLI cache (eval fragments, panda-entry, etc.) — excluded so file: deps
# don't hit ENOENT when pnpm copies the package
.ref/

# Old codegen approach (deprecated)
codegen/


.virtual/