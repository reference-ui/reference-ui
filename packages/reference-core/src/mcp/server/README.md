# MCP server (standalone)

`@modelcontextprotocol/sdk` transport and tools used by **`ref mcp`** (stdio or HTTP). This runs in the **CLI process**, not in the sync worker pool.

Shared helpers (`build`, `queries`, `types`, …) live in **`mcp/pipeline/`**.
