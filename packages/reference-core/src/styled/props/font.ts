/**
 * Font prop types. Pattern registration is in styled/font/font.ts (generated).
 */

export type FontProp = 'sans' | 'serif' | 'mono'

export interface FontPropDefinition {
  font?: FontProp
  weight?: string
}
