# Test Fixtures

Clean, minimal fixtures representing realistic user code.

## Files

- **`use-function.ts`** – Calls the collector with a fragment (imports from `./setup`; setup is provided by the test or package).
- **`with-constants.ts`** – Calls the collector with a fragment built from constants.

## Design

All fixtures are **realistic user code**. No test infrastructure.
