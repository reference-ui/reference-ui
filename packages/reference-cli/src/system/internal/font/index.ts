/**
 * Font system (internal)
 * 
 * Provides runtime font generation during ref sync.
 */

export { generateFontSystem, parseFontFamily } from './generate'
export type { FontGenerationContext, FontSystemOutput } from './types'
