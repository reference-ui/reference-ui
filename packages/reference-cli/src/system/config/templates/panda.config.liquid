// Generated by reference-cli — do not edit manually
import { defineConfig } from '@pandacss/dev'

// Collector setup — each collector initialises its own globalThis slot
{{ collectorSetups }}

// Fragment bundles (each self-contained IIFE, functions intact)
{{ bundles }}

{{ deepMergeFn }}

const fragments = [{{ collectorKeys }}].flatMap(k => globalThis[k] ?? [])
;[{{ collectorKeys }}].forEach(k => delete globalThis[k])
const config = fragments.reduce((acc, frag) => deepMerge(acc, frag), {})

export default defineConfig(config)
