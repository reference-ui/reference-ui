/**
 * Public authoring surface for `@reference-ui/cli`.
 * Keep this separate from the executable CLI entry.
 */

export { defineConfig } from './config'
export type { BaseSystem, ReferenceUIConfig } from './config'
