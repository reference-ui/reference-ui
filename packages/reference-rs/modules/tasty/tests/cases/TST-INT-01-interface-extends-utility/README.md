# TST-INT-01 interface extends utility

Verifies projection of TypeScript utility types (`Omit`) appearing directly in interface `extends` clauses.
Tests member merging between utility projection and declared body, plus multi-level extends walking.
Proves primary SPEC ID anchor TST-INT-01.
