# TST-OPR-01 type operators

Verifies type operators (`keyof`, `readonly`, `unique`) on type alias definitions and interface members.
Tests extraction of operator targets, arrays, and intrinsic symbol types.
Proves primary SPEC ID anchor TST-OPR-01.
