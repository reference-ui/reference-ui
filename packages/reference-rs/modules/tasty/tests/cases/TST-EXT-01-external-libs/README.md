# TST-EXT-01 external libs

Verifies resolution of external library types (`csstype`, `json-schema`) and extends chains.
Tests flattening of interface members across external boundaries and user-owned reference collection.
Proves primary SPEC ID anchor TST-EXT-01.
