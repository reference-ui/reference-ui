# TST-INT-02 interface extends utilities

Verifies comprehensive interface `extends` support for utility types (`Omit`, `Pick`, `Partial`, `Required`, `Record`, `Readonly`).
Tests nested utility compositions, multi-clause merging, union key omitting, and generic parameters.
Proves primary SPEC ID anchor TST-INT-02.
