# TST-STY-04 key alias projection

Verifies projection of omit keys defined through aliased literal unions.
Ensures omitted keys are correctly stripped from the resulting display member list.
Proves primary SPEC ID anchor TST-STY-04.
