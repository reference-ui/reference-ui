# TST-PAR-01 default params

Verifies defaulted generic type parameters across type aliases.
Tests default values on intrinsic types, multiple type parameters, and partial default configurations.
Proves primary SPEC ID anchor TST-PAR-01.
