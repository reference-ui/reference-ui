/**
 * Complex real-world patterns test case.
 * Covers React component patterns, event handlers, builder patterns, and third-party utilities.
 */

export type {
  ReactComponentProps,
  ReactForwardRef,
  ReactContext,
  EventHandlerMap,
  BuilderPatternGeneric,
  ZodInfer,
  StyledSystemProps,
} from './real-world-types'

export type {
  ComponentExample,
  ForwardRefExample,
  ContextExample,
  EventHandlerExample,
  BuilderExample,
  ZodExample,
  StyledSystemExample,
} from './examples'
