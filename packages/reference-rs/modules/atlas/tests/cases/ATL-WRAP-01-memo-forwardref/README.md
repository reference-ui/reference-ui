# ATL-WRAP-01 memo and forwardRef wrappers

Verifies that React higher-order wrappers such as memo and forwardRef resolve canonical names and interfaces.
Asserts tracking for both named and default-exported wrapped component forms.
Proves SPEC anchor ATL-WRAP-01.
