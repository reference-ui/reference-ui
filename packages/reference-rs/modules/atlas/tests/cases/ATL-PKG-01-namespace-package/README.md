# ATL-PKG-01 namespace package usage

Verifies that direct package component usage through namespace imports is counted and mapped correctly.
Asserts call-site counts and preservation of namespace-qualified example element signatures.
Proves SPEC anchor ATL-PKG-01.
