# ATL-PAIR-01 co-usage pairs

Verifies that Atlas calculates deterministic co-usage weights for components used together.
Asserts that higher co-occurrence frequencies produce higher usage tiers in the usedWith map.
Proves SPEC anchor ATL-PAIR-01.
