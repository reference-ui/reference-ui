# ATL-REXP-01 barrel re-exports

Verifies that local components imported through barrel files are properly tracked.
Asserts that component call sites and canonical interface mapping survive barrel hops.
Proves SPEC anchor ATL-REXP-01.
