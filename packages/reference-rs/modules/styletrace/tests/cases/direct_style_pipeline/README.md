# direct_style_pipeline

Traces a component that uses the Reference style pipeline without wrapping a primitive.
splitCssProps, css(), and box() are enough to mark Panel as style-bearing.
No Div export is required.
