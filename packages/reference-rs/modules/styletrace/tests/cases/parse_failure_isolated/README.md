# parse_failure_isolated

One unparsable file does not fail the trace: the file is skipped and siblings still report.
Compile-seam twin: ATM-SITE-57 (the skip surfaces as a located warning). Sibling: `direct_wrapper` (same Card shape).

> Search terms: parse failure, isolation, skip, siblings
