# export_star_barrel

Traces a local barrel that re-exports a style-bearing wrapper with export \*.
The component lives in components.tsx; index.ts is the barrel.
Card remains the public style-bearing name.
