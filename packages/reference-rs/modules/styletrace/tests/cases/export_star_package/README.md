# export_star_package

Traces wrappers that import a package barrel which re-exports with export \*.
The mock package is committed under input/packages/fixture-style-barrel.
AppCard and PackageCard are both style-bearing.
