# ATM-RECIPE-01

`recipe({ className, base, variants, compoundVariants })` compiles each
declared variant leaf into one readable class inside `@layer recipes`.
Those classes do not appear in `@layer utilities`. Contract: [SPEC.md](../../../SPEC.md).
