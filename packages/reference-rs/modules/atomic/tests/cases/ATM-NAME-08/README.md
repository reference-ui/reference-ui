# ATM-NAME-08

The class stem passes only through explicit lexical functions: every builtin divergence carries a compiled input and a pinned outcome — refusals mint no class, `-0` folds to `0`, quoted carriage returns and non-structural marks survive byte-identical, and per-prop object declaration order follows the author, integer-keyed members included.
Contract: [SPEC.md](../../../SPEC.md).
