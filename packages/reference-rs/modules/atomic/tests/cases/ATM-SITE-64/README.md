# ATM-SITE-64

Open-test ternary arm shapes: equal branches dedupe to one class,
mixed-type arms both compile, per-arm `!` sticks to its arm. Pins the
SPEC-V2-15/16 remainder (v2 `conditional_output.rs:110` equal-branch,
atomic value arms).
