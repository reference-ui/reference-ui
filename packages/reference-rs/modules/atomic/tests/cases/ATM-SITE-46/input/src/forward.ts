import { css } from '@reference-ui/react'

export const early = spin2({ from: { opacity: 0 }, to: { opacity: 1 } })

import { keyframes as spin2 } from '@reference-ui/system'

export const q = css({ animationName: early, margin: '8r' })
