# ATM-RECIPE-05: Compound Variant Rule Emission Order

Tests that compound variant rules are emitted after simple variant rules inside `@layer recipes`, ensuring that compound variant declarations override simple variant declarations via CSS source order.
