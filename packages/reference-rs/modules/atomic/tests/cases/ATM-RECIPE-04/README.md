# ATM-RECIPE-04: Default Variants and Boolean Variant Keys

Tests that `defaultVariants` and boolean variant keys appear in the authoritative RecipeRuntimeTable and compile to closed classes inside `@layer recipes`.
