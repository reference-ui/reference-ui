# ATM-SITE-69

Dynamic responsive-array slot: `dyn` warns once and is omitted, the null
hole skips silently, and `black` keeps `base`. Pins SPEC-V2-26 GAP-26
(v2 `calls.rs:1922` stays silent; our warning is the upgrade).
