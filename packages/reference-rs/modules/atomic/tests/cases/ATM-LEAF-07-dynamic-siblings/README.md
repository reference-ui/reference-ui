# ATM-LEAF-07 dynamic siblings

A dynamic expression in one key must not erase neighbouring static leaves.
One ternary arm may be a call; extract keeps the literal arm. Inspect
`output/diagnostics.json`.
