# ATM-SITE-19

Array `css` props extract every element (`css={[{...}, {...}]}`), matching
the `css([...])` call form. Single-object `css={{...}}` is the control.
Contract: [SPEC.md](../../../SPEC.md).
