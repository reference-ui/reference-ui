# ATM-SEAM-05

`compile()` returns what it found: `tracedJsxHosts` rides the result on both the legacy and frozen request shapes while the request shape itself is unchanged.
ATM-SEAM-02's golden is byte-identical (its input has no forwarding wrapper). Siblings: ATM-SITE-56 (discovery), ATM-SEAM-02 (frozen parity).
Contract: [SPEC.md](../../../SPEC.md).

> Search terms: tracedJsxHosts, CompileResult, frozen request, seam
