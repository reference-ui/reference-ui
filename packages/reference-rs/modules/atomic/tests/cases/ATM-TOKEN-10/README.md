# ATM-TOKEN-10

A foreign BaseSystem spec (core's portable `{ name, fragment, jsxElements }` shape) is rejected with a path-bearing error diagnostic, never silently accepted as an empty system.
Contract: [SPEC.md](../../../SPEC.md).
