# ATM-SITE-74

Optional chain on an unresolvable base warns once and mints nothing while
the `padding` sibling extracts. Pins SPEC-V2-44 (v2
`optional_chaining.rs:58`; known-base fold is SPEC-V2-43).
