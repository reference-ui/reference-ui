# ATM-HARVEST-03

A sink under `_hover` mints `"_hover:color:..."` only: the pool crosses the sink carrying the sink's `when`, and no unconditioned twins leak into the map. One `ATM-I-HARVEST-SINK` names the scope and the count.
Symbols: `Sink`, `mint`, `HarvestSink`.
Siblings: `ATM-HARVEST-01` (the floor), `ATM-HARVEST-02` (holey templates mint nothing), `ATM-HARVEST-04` (zero-harvest control).
Contract: [SPEC.md](../../../SPEC.md).

> Search terms: harvest condition, sink when scope, hover harvest, conditioned twins, no unconditioned leak
