# ATM-SITE-86

A binding whose branching init drops a dynamic arm beside kept literal leaves keeps its values but never folds a test: `const isSelected = context ? context.value === value : false` (Tabs) gates `borderBottom={isSelected ? '3px solid' : '3px solid transparent'}` with both arms emitting wants and plans, and the `??` coalesce twin (`isDisabled`), truthy-tail coalesce (`dynA ?? 'blue.500'`), negated (`!isSelected`), member (`part.sel`), nested (`isLine && selA`), mirror-position (`flag ? false : pick()`), both-dynamic, and whole-`css()`-object gated twins all stay open the same way, while partial value unions (`tone = flag ? 'red.500' : pick()`, `coal`) still scoop their kept leaves.
Symbols: `scalar_residue`, `operand_leaves`, `fold_test`, `BindingInit::Scalars`.
Siblings: `ATM-SITE-23` (const ternary/logical value union), `ATM-SITE-77` (partial object entries), `NEO-SITE-30` (the indicator paints).
Contract: [SPEC.md](../../../SPEC.md).

> Search terms: tabs indicator, isSelected, isDisabled, partial scalar, dropped arm, residue, test folding, dead branch, const ternary, coalesce guard
