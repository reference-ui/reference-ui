# ATM-SITE-81

A const bound to a nested style object carries its entries: the alias lowers as a JSX condition block and spreads into `css()` exactly like the member it abbreviates, member spreads record through const object inits, and a write to the root poisons the alias instead of resolving stale.
Symbols: `member_init`, `record_spread`, `member_path_object`, `BindingInit::Object`, `Dep`, `strip_stale`.
Siblings: `ATM-SITE-29` (member depth), `ATM-SITE-28` (mutation wording), `ATM-SITE-80` (§5 call inits).
Contract: [SPEC.md](../../../SPEC.md).

> Search terms: member init, const alias, nested style object, member spread, focusRing, mutated binding, provenance
