# ATM-SITE-32 — per-shape refuse pins (doom tripwires)

Overmatch station-only (SPEC-V2-42): the highest-risk refusals pinned per
shape so the Ph3 fold table cannot regress extraction into an interpreter.
`Math.random`, `.map`/`.reduce` chains, async calls, loops, rest params,
nested unknown calls, spread args, and `f?.()` each warn and skip while
the static sibling extracts. The blanket net is HAVE (SPEC-V2-41); these
are the tripwires. Browser paint arm: NEO-SITE-21.

Panda: `scope.rs:1112` (`object_entries_factory_does_not_fold`), random
`:1020`, loops `:1249`, rest `:1123`, spread-args `:1153`, `f?.()` `:1143`.
