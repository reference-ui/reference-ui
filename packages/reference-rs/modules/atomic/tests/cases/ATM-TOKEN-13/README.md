# ATM-TOKEN-13

Every token path segment must kebab in the emitted custom property (Panda parity): `colors.ui.progress.track.mixForeground` defines `--colors-ui-progress-track-mix-foreground`, and engine-resolved uses reference the same name.
Contract: [SPEC.md](../../../SPEC.md).
