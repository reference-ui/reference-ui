# ATM-SEAM-06

The shipped runtime artifact carries no per-atom row: schema 2, no `stylePlans` key, and the namer tables both namers read. The compile-internal `stylePlans` on the result still equal the pre-cutover rows.
Contract: [SPEC.md](../../../SPEC.md).
