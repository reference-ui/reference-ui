# ATM-SITE-34

Optional-chain folding over known const objects (SPEC-V2-43/44,
Overmatch Ph3). `?.` unwraps transparently — single-hop and nested —
while an unresolvable base warns once and mints nothing (the SITE-74
shape, folded here beside its resolving twins). Calls and computed
links never fold here (SITE-32 refuse pins, SITE-48 element work).

Panda: `optional_chaining.rs:19` (known base), `:72` (nested), `:58`
(unresolvable drop).
