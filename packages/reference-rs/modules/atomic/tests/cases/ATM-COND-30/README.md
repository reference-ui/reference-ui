# ATM-COND-30

Utility-path `& + &` / `& ~ &` substitution, two-level stacks, and comma
descendant lists. Pins the SPEC-V2-47 shapes the globalCss stations prove
only in global spelling (v2 `nested_selector_parity.rs:92`, `:125`,
`:180`, `:202`).
