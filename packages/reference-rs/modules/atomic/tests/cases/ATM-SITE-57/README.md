# ATM-SITE-57

Failure is a located warning: one unparsable file under `include` yields exactly one trace warning naming the file while siblings still trace and configured hosts still gate; the compile itself does not fail.
Contract: [SPEC.md](../../../SPEC.md). Siblings: ATM-SITE-56 (discovery), ATM-SCAN-01 (include scoping), `parse_failure_isolated` (names-seam isolation).

> Search terms: parse failure, warning, isolation, TraceOutcome, diagnostics
