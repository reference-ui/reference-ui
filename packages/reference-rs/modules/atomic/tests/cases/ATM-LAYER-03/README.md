# ATM-LAYER-03

BaseSystem populates `@layer global` (stored globalCss) and `@layer tokens`
(custom properties on `:root` plus dark overrides). Empty reset/recipes stay
omitted. Contract: [SPEC.md](../../../SPEC.md).
