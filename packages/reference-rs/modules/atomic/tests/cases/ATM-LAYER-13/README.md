# ATM-LAYER-13

Breakpoint keys and conditional values in `globalCss` lower through the
scale's `@media` queries, including `*Only` / `*Down` / `*To*` ranges.
Unknown conditional keys refuse with a diagnostic. Contract:
[SPEC.md](../../../SPEC.md).
