# ATM-COND-17

Per-prop responsive objects (`width: { base, md }`) expand onto breakpoint `when` scopes with `base` unconditioned, unknown keys warn and skip, and plans carry responsive `@` slots that a later alias evicts at merge time.
Contract: [SPEC.md](../../../SPEC.md).
