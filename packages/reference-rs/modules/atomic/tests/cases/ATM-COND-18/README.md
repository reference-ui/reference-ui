# ATM-COND-18

`_placeholder` emits the `::placeholder` + `[data-placeholder]` twin
selectors, `_file` is a known condition emitting `::file-selector-button`,
and `_checked` keeps the lib twin list. No unknown-condition diagnostics.
Contract: [SPEC.md](../../../SPEC.md).
