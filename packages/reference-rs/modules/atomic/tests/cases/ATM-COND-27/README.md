# ATM-COND-27

Tag / class / BEM compounds (`&_elem`, `&html`, `html&`, `body&`,
`.foo&`), ancestors (`body &:hover b`), the three-level `.c &` tail,
comma lists (`&h1, &h2`, `&+.baz, &.qux`), tight combinators (`&>.bar`),
textual `&(:focus)`, and bare `&` (a distinct `[&]` key with a
class-only selector — v2 mints `[\&]` too). Contract:
[SPEC.md](../../../SPEC.md) (SPEC-V2-72).
