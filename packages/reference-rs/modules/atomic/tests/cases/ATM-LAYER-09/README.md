# ATM-LAYER-09

`&` substitution in the global walker distributes over comma members,
wrapping members that contain a combinator in `:is()` so sibling nests
keep the author's grouping. Contract: [SPEC.md](../../../SPEC.md).
