# ATM-COND-04 pseudo conditions

Conditions accumulate on `when`. JSX `_hover` / `_dark` and nested `css()`
objects must appear in the class map and stylesheet selectors. Also covers
the hover selector spelling from `ATM-COND-02`.
