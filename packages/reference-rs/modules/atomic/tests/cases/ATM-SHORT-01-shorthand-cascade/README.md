# ATM-SHORT-01 shorthand cascade

Composite shorthands decompose into longhands without synthesizing
`currentColor`. `borderBottom` plus `borderColor`, and `outline` plus
`outlineColor`, must coexist. Also proves `ATM-SHORT-02`.
