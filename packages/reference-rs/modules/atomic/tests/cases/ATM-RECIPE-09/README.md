# ATM-RECIPE-09

Observed-use emission gating. Recipes print plain rules only when called;
responsive `(value, breakpoint)` rules print only for observed literal
triples; dynamic selections fail closed to the full matrix; uncalled,
untraceable, and bare definitions print no rules while their runtime
tables still build. Contract: [SPEC.md](../../../SPEC.md).
