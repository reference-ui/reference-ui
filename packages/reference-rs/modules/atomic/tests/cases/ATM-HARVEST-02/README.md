# ATM-HARVEST-02

`` `2${n}r` `` and `` `#${hex}` `` are arithmetic, not values: they mint nothing, their sites still warn `ATM-W-DYNAMIC-TEMPLATE`, and each refused sink infos a zero count, sinks ordered by `(prop, when)`.
Symbols: `HarvestPool`, `Sink`, `warn_dynamic`, `HarvestSink`.
Siblings: `ATM-HARVEST-01` (the floor), `ATM-HARVEST-03` (conditioned sinks), `ATM-HARVEST-04` (zero-harvest control), `ATM-HARVEST-05` (wholesale in a hole vs `${n}px`).
Contract: [SPEC.md](../../../SPEC.md).

> Search terms: harvest refusal, template holes, authorship rule, wholesale values, zero-count sink info
