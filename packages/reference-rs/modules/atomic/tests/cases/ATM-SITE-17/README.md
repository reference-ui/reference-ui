# ATM-SITE-17

Wants that arrive through indirection — ternary arms, member access,
identifier spreads — emit one runtime style plan per want, so the plan
index the runtime resolves through never misses a leaf. Contract:
[SPEC.md](../../../SPEC.md).
