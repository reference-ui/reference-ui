# ATM-LEAF-05 responsive arrays

Array style values lower onto canonical breakpoints. Index 0 is `base`,
then `sm`, `md`. `null` holes are skipped. A ternary inside an item scoops
both arms onto that item's `when`. Look at the media queries in
`output/styles.css`.
