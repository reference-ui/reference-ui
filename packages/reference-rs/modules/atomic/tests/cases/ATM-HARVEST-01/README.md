# ATM-HARVEST-01

`'red'` lives only in an array no site reads, a dynamic `css({ color })` site exists, and `"color:red"` is in the map anyway — minted by harvest onto the refused `(color, [])` sink, with one `ATM-I-HARVEST-SINK` counting it and a runtime plan behind it. The `'4px'` pool twin does not land on the color sink (kind gate).
Symbols: `HarvestPool`, `Sink`, `warn_dynamic`, `prop_accepts`, `HarvestSink`.
Siblings: `ATM-HARVEST-02` (holey templates mint nothing), `ATM-HARVEST-03` (conditioned sinks), `ATM-HARVEST-04` (zero-harvest control), `ATM-TOKEN-14` (§9 alphabet fence).
Contract: [SPEC.md](../../../SPEC.md).

> Search terms: harvest floor, refused leaf paints, dynamic identifier sink, pool cross sinks, harvest info count, kind gate
