# ATM-SITE-75

Unresolvable specifier, missing export, and bare imported function value
each warn once and mint nothing while siblings extract. Pins SPEC-V2-54
(SUPERIOR over v2 `cross_file.rs:465`, `:488`) and SPEC-V2-58 (v2
`:1310`).
