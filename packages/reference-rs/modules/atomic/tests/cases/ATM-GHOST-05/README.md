# ATM-GHOST-05

Both at-rule `When`s on one atom must wrap the utility, nested, in author
order. Nested `sm:` object keys are not extract conditions; array slot 1
is `sm` under `_osDark`. Contract: [SPEC.md](../../../SPEC.md).
