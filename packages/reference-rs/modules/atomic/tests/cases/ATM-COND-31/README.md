# ATM-COND-31

Raw `&::` spelling lowers like the `_before`/`_after` dialect, with
single-level compound order and comma pseudo-element lists. Pins the
SPEC-V2-48 raw arm (v2 `nested_selector_parity.rs:433`, `:466`, `:477`).
