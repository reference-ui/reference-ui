# ATM-RECIPE-06: Strict Recipe Identity and Diagnostic Refusal

Tests that recipe extraction strictly enforces explicit string-literal `className`, rejects non-object arguments, spread properties, missing/dynamic class names, and duplicate `(system, className)` pairs with descriptive compiler error diagnostics.
