# ATM-LAYER-10

Keyframe bodies resolve `{token}` refs to `var()` and `r` units to the
rhythm calc, through the same passes as utilities. Unresolvable values
print verbatim with no diagnostics (spec-owned, no source location).
Contract: [SPEC.md](../../../SPEC.md).
