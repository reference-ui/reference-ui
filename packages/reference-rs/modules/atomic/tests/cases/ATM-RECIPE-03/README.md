# ATM-RECIPE-03

StyleProps on a recipe host stay utilities. `variant` is runtime-owned.
Because `@layer utilities` follows `@layer recipes`, host overrides win.
Contract: [SPEC.md](../../../SPEC.md).
