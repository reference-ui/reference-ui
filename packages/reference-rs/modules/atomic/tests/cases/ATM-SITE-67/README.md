# ATM-SITE-67

Overlapping inline-object spreads mint both atoms while the runtime merge
resolves the shared slot last-wins. Pins SPEC-V2-23 GAP-23 (v2
`calls.rs:1736` `merge_two_inline_object_spreads_second_wins`).
