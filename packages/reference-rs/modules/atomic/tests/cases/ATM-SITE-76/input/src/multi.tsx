import { css, Div } from '@reference-ui/react'

// Named import beside other names in one declaration is a live site.
export const b = css({ padding: '4px' })

export function App() {
  return <Div mt="2r" />
}
