# ATM-SITE-76

Aliased, named multi-declaration, and string-literal `css` imports are
live sites. Pins the SPEC-V2-59 alias arm (v2 `imports.rs:34`) and the
I2 string-literal arm (`bindings.rs:131`).
