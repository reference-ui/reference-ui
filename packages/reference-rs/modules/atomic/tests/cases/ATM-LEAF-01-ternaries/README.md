# ATM-LEAF-01 ternaries

Extract scoops every literal ternary branch and does not evaluate the test.
`undefined` and `void 0` are omitted. Nested and flat forms share this
station. Also proves `ATM-LEAF-02` and `ATM-LEAF-03`.
