import type { ButtonProps } from './button'

/** A documentation entry that references component props. */
export interface DocsEntry {
  /** The component props for this entry. */
  props: ButtonProps
}
