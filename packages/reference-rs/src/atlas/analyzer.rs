use crate::atlas::config::AtlasConfig;

pub struct AtlasAnalyzer;

impl AtlasAnalyzer {
    pub fn new(_config: AtlasConfig) -> Self {
        unimplemented!("AtlasAnalyzer is not yet implemented")
    }
}
