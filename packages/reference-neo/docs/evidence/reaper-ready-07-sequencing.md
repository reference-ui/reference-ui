# Reaper READY ask 7 — sequencing: what Slice 1 needs from Jettison (firsthand)

Date: 2026-09-20. Author: Reaper Phase R1 crew (read-only; this file + siblings are the only tree writes).
Mission: `docs/missions/operation-reaper.md` READY ask 7. Answered FIRST per the voyage brief — it bounds Slice 1.

## Verdict: NOT BLOCKED. Slice 1's sheet work runs now; the `react.mjs` cell is fillable now with a re-verify-after-acceptance caveat.

## Jettison state on this checkout (branch `reference-system`)

**The shipped map is gone.** Verified firsthand, generated output + source:

- `packages/reference-lib/.reference-ui/styled/runtime-data.mjs` parses to
  `{ schemaVersion: 2, namer, recipes, stylePropNames }` — no `stylePlans` key.
- `grep -c stylePlans` over `react.mjs`, `runtime-data.mjs`, `baseSystem.mjs`: **0 / 0 / 0**.
- `css.ts` cut over: `createStylePlanIndex` / `resolveScoredDeclarations` gone (0 hits in
  `packages/reference-neo/src/runtime/css/css.ts`).
- Runtime namer present: `packages/reference-rs/modules/atomic/js/namer/` (10 files incl.
  `NAMER_RULES_VERSION = 6`), namer goldens in `tests/namer-goldens/`,
  `NEO-NAMER-01..03` cases, `ATM-SEAM-06..08` + `ATM-NAME-08` case folders all exist.
- Rust and JS agree: `NAMER_RULES_VERSION = 6` on both sides
  (`modules/atomic/src/runtime/tables/mod.rs:21`, `js/namer/index.ts:30`).
- Git log shows Slices 0–4 plus fortify-2 through fortify-5 landed.

**Jettison acceptance still pends.** `docs/missions/README.md` (the status source):
"active — implementation complete (Slices 0–4 landed 2026-09-20), acceptance pending
(Doom seed brief is the captain's dispatch, plus final R1–R15 sign-off)."
Two particulars observed (not litigated — Jettison's own business):

1. The acceptance Doom seed ("run one brief without a differential break") is not recorded
   as run. Five `jettison-namer-*` doom logs exist, but those are fortify-driven breaks,
   not the clean acceptance run.
2. `docs/ATOMIC.md` harvest section still speaks map ("Map keys", "looks up",
   `docs/ATOMIC.md:75,123`) — the R12 prose pass reads incomplete.

Local hygiene note: the untracked `.reference-ui/` output on this checkout is stale
(`rulesVersion: 4` vs tree `6`) — regenerated by the next `pnpm sync`, not a repo issue.
Slice 1 re-syncs its own fixture regardless.

## What in Slice 1 needs Jettison landed, beyond the `react.mjs` cell

Nothing. Item by item against Slice 1's record list:

| Slice 1 item | Needs Jettison? | Why not |
|---|---|---|
| Fixture compile | No | `compile()` stable; SEAM-07 in-test pattern green post-cutover |
| `styles.css` raw / gzip / brotli | No | Harvest untouched by Jettison by design; sheet byte-identical pre/post cutover — **242,953 raw / 32,046 gzip verified firsthand on this checkout**, exactly the pre-cutover baseline |
| Sink census `(prop, when, kind)` | No | `extract/harvest/` untouched by Jettison (`git log` on the dir ends at Forge/Error-Correct); HarvestSink infos predate the cut |
| Pool census by kind, leaf-only vs unbound | No | `literals.rs` untouched; pool collection is pre-namer pipeline |
| Net-new vs gross | No | `Want.origin` / twin skip untouched |
| CSSOM parse + css-tree timing | No | Independent harness work |
| `react.mjs` raw / gzip | **Yes — and it is fillable now** | Needs schema 2, which has landed |

The only residual is the caveat, not a gate: Jettison acceptance could still revise
namer/tables bytes (a `rulesVersion` bump re-spells nothing, but table edits would move
`react.mjs` by kilobytes). Slice 1 should record the `react.mjs` cell now and re-verify
it after Jettison acceptance lands. The sheet numbers need no re-verify: no acceptance
outcome re-spells the sheet (that would be a new operation, not acceptance).

## Answer to the ask as written

> If nothing, HQ may let the sheet measurements run in parallel and fill the `react.mjs` cell last.

Nothing beyond the `react.mjs` cell needs Jettison, and even that cell needs only what
has already landed (schema 2). HQ need not sequence Slice 1 behind Jettison acceptance
at all — but the `react.mjs` cell gets a confirmatory re-measure after acceptance.
