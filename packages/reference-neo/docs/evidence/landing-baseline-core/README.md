# Landing baseline — core (Panda) reference output

Source: `ref sync` in `packages/reference-lib` on reverted HEAD (2026-09-18,
captain-reconstructed after the switch crew regened without saving a copy).
Kept artifacts only (HQ trim): `styled/` (Panda styled system),
`react-styles.css` (compiled sheet), `react.mjs` (JSX/css runtime backing
the L1 class-name witnesses). Oracle before/after input for the landing.
Revisit tracking at tasty re-commission.
