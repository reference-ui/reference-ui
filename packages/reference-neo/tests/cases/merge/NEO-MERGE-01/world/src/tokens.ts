// Tokens for the NEO-MERGE-01 world. It takes no inputs and emits the two
// color leaves the last-wins case paints with, so the sheet and the browser
// resolve ember and ocean without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ember: { value: '#ef4444' },
    ocean: { value: '#3b82f6' },
  },
})
