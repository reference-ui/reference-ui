// Tokens for the NEO-MERGE-02 world. It takes no inputs and emits the two
// color leaves the alias case paints with, so the sheet and the browser
// resolve sand and clay without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    sand: { value: '#f4f4f5' },
    clay: { value: '#e4e4e7' },
  },
})
