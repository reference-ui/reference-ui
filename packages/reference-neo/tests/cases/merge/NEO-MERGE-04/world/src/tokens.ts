// Tokens for the NEO-MERGE-04 world. It takes no inputs and emits the one
// color leaf the border case paints with, so the sheet and the browser
// resolve slate without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    slate: { value: '#27272a' },
  },
})
