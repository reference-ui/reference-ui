// Tokens for the NEO-MERGE-06 world. It takes no inputs and emits the one
// color leaf the miss case paints with, so the sheet and the browser
// resolve ember while the dynamic shade finds no atom at all.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ember: { value: '#ef4444' },
  },
})
