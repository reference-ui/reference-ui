// Tokens for the NEO-SITE-04 world. It takes no inputs and emits the two
// color leaves the alias and namespace calls paint with, so the sheet and
// the browser resolve cherry and ocean without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    ocean: { value: '#2563eb' },
  },
})
