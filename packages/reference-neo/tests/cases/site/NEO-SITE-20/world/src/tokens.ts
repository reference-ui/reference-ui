// Tokens for the NEO-SITE-20 world. It takes no inputs and emits the six
// color leaves the world names — one per wrap form, each a distinct paint.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    tangerine: { value: '#ea580c' },
    amber: { value: '#f59e0b' },
    forest: { value: '#15803d' },
    ocean: { value: '#2563eb' },
    plum: { value: '#a855f7' },
  },
})
