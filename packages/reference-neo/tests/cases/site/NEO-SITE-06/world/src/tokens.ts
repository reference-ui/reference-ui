// Tokens for the NEO-SITE-06 world. It takes no inputs and emits the two
// color leaves the call names — cherry for the dynamic key that must stay
// a ghost-free miss, ocean for the static sibling that paints.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    ocean: { value: '#2563eb' },
  },
})
