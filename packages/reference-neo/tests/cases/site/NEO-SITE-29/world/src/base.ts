// Base tokens for the NEO-SITE-29 world. It takes no inputs and emits
// the red color leaf the button folds in, so the painted value lives
// two files away from the css() site.
export const base = { color: 'red' }
