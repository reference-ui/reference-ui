// Global fragment for the SITE-11 world. It takes the neo fragment collector
// and emits the rhythm root the chart resolves its padding step against.
import { globalCss } from '@reference-ui/neo'

globalCss({ ':root': { '--spacing-root': '0.25rem' } })
