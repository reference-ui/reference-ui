// Tokens for the NEO-SITE-01 world. It takes no inputs and emits the two
// color leaves the ternary names — cherry for the arm the runtime picks
// by default, ocean for the sibling arm that must exist unused.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    ocean: { value: '#2563eb' },
  },
})
