// Tokens for the NEO-SITE-15 world. It takes no inputs and emits the two
// color leaves the hover ternary names — brand for the arm the runtime picks
// by default, paper for the sibling arm that must exist unused.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    paper: { value: '#ffffff' },
  },
})
