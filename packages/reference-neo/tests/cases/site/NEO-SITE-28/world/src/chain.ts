// Second hop for the NEO-SITE-28 world. It takes the wrapper's `css`
// and emits it onward, so the link node proves identity survives a
// two-hop barrel with zero config.
export { css } from './ui.js'
