// Wrapper for the NEO-SITE-28 world. It takes no inputs and emits
// Reference's `css` under its own name, so importers bind the real call
// through this file with zero config and the extractor must follow.
export { css } from '@reference-ui/react'
