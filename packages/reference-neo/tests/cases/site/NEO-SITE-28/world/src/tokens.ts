// Tokens for the NEO-SITE-28 world. It takes no inputs and emits the two
// color leaves the world paints — cherry for the direct wrapper node,
// ocean for the two-hop chain node.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    ocean: { value: '#2563eb' },
  },
})
