// Shared styles for the NEO-SITE-27 world. It takes no inputs and emits
// the color binding the entry shadows with a param plus the accent
// binding the twin paints with, so the shadowed const and the live const
// live in this file while both css() sites live in app.ts.
export const color = 'cherry'
export const accent = 'ocean'
