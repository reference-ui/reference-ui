// Tokens for the NEO-SITE-27 world. It takes no inputs and emits the two
// color leaves the world names — cherry for the shadowed key that must
// stay a ghost-free miss, ocean for the twin that paints.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    ocean: { value: '#2563eb' },
  },
})
