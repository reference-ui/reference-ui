// Tokens for the NEO-SITE-26 world. It takes no inputs and emits the three
// color leaves the world names — cherry for the folded member part, ocean
// for the fan's open arm and the refused node's sibling, plum for the fan
// arm the runtime paints.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
    ocean: { value: '#2563eb' },
    plum: { value: '#a855f7' },
  },
})
