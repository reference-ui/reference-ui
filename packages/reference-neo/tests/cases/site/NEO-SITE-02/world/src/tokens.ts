// Tokens for the NEO-SITE-02 world. It takes no inputs and emits the one
// color leaf the member access names, so the resolved cherry value paints
// through the design token instead of a raw string.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
  },
})
