# NEO-SITE-17 — Const-bound ternary color arms extract and paint

The world renders a chrome divider whose `borderBottomColor` rides a
component-body const ternary — the `BookShell.tsx` `subtleBorder` shape.
The spec checks the sheet carries exactly the two arm utilities, the dark
load paints the ink arm with a 1px solid width and style, and a light
reload paints the mist arm through the same runtime plan index.

Evidence: `[atm]` ATM-SITE-23; `[lib]` `BookShell.tsx` chrome dividers (RS-37).
