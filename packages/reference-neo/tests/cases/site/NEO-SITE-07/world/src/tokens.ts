// Tokens for the NEO-SITE-07 world. It takes no inputs and emits the one
// color leaf the cross-file binding names, so the sheet and the browser
// resolve cherry without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    cherry: { value: '#dc2626' },
  },
})
