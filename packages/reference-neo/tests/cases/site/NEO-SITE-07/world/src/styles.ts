// Shared styles for the NEO-SITE-07 world. It takes no inputs and emits
// the brand binding the entry styles with, so the const under test lives
// in this file while the css() site lives in app.ts.
export const brand = 'cherry'
