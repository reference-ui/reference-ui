// Tokens for the SITE-13 world. It takes the neo fragment collector and
// emits the ink leaf the probes share. The border macro itself lowers to
// literal width and style, so no token participates in the painted border.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ink: { value: '#111111' },
  },
})
