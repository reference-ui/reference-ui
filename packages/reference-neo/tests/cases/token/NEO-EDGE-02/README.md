# NEO-EDGE-02 — border radii resolve against radius tokens

The world calls css() with border radius props in bare and dotted form. The
spec checks the radii paint from tokens with no ghost utilities in the sheet.

> Search terms: rounded, corner, border-radius, pill, corner rounding, token/radii, ATM-TOKEN-01, ATM-TOKEN-09
