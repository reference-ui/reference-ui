// Tokens for the TOKEN-07 world. It takes the neo fragment collector and
// emits one named decimal spacing token the kebab-var probe paints with.
import { tokens } from '@reference-ui/neo'

tokens({
  spacing: {
    '0.5': { value: '0.125rem' },
  },
})
