// Global fragment for the TOKEN-07 world. It takes the neo fragment
// collector and emits the rhythm root the sugar probes resolve against.
import { globalCss } from '@reference-ui/neo'

globalCss({ ':root': { '--spacing-root': '0.25rem' } })
