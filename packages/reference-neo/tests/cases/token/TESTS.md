# TOKEN ledger

| id | claim | status | engine | host | proof | evidence |
| --- | --- | --- | --- | --- | --- | --- |
| NEO-EDGE-02 | border radii resolve against radius tokens | done | ATM-TOKEN-01/09 | — | md/full/dotted radii paint; exactly 3 utilities, no ghosts | `[atm]` TOKEN-01/09 |
| NEO-TOKEN-01 | `{colors.red.500}` inside a multi-value border shorthand resolves to `var(--colors-red-500)` | done | ATM-TOKEN-08 | — | `border: '2px solid {colors.red.500}'` → computed border-color | `[panda-v1]` `core/__tests__/style-decoder.test.ts` "should resolve references"; `[lib]` 3 leftover refs |
| NEO-TOKEN-02 | a missing `{colors.nope}` ref fails sync with a located diagnostic (no escaped literal) | done | ATM-TOKEN-12 (RS-3 landed) | `sync/index.ts` surfaces `file:line` (SYNC-11 landed) | `sync()` rejects; message names the ref and file:line | `[panda-v1]` `core/__tests__/serialize.test.ts` (contrast: Panda escapes); `[decision D13]` |
| NEO-TOKEN-03 | `red.500/40` paints `color-mix(in srgb, var(--colors-red-500) 40%, transparent)` | done | ATM-TOKEN-03/06 | — | computed background-color vs `color-mix` reference element | `[panda-v1]` `core/__tests__/color-mix.test.ts`; `[lib]` 25 mixes; `[atm]` P0 #6 |
| NEO-TOKEN-04 | `{colors.x/30}` inside a function value mixes inside the function | done | ATM-TOKEN-06/08 | — | `boxShadow: '0 0 0 3px {colors.x/30}'` computed | `[panda-v1]` `core/__tests__/color-mix.test.ts` "curly" |
| NEO-TOKEN-05 | `{ light, dark }` leaves become `:root,[data-color-mode=light]` and `[data-color-mode=dark]` islands; flipping the attribute repaints | done | ATM-COND-03/08 | — (D1 landed; engine via RS-7 done) | computed colour under `light` then `dark` on `html` and on a nested island | `[atm]` P0 #1; `[lib]` islands; `[panda-v1]` `generator/__tests__/generate-token.test.ts` (`:where([data-theme=dark], .dark)` contrast); `[decision D1]` |
| NEO-TOKEN-06 | semantic alias chains stay `var()` references, not inlined hex | done | ATM-TOKEN-11 | — | sheet has `--colors-danger: var(--colors-red-500)`; computed equal | `[panda-v1]` `generator/__tests__/generate-token.test.ts` "reuse css variable in semantic token alias"; `token-dictionary/__tests__/alias.test.ts` |
| NEO-TOKEN-07 | decimal and fraction rhythm (`0.5r`, `1/2r`) paint via calc with escaped classes (`.p_0\.5r`, `.p_1\/2r`); decimal token keys print kebab (`--spacing-0-5`) | done | ATM-RHYTHM-02/03, ATM-NAME-01..07 | — | computed 2px padding for `0.5`, `0.5r`, `1/2r` | `[lib]` L1821–1823; `[panda-v1]` `shared/__tests__/esc.test.ts`; `[atm]` P1 #8 |
| NEO-TOKEN-08 | negative rhythm/token values paint negated calc of the positive var (`calc(-1 * var(…))`) | done | ATM-TOKEN-07, ATM-RHYTHM-05 | — | computed negative margins for `-sm`, `-1r`, `-4r` | `[lib]` 368 negatives; `[panda-v1]` `token-dictionary/__tests__/spacing.test.ts`; `[atm]` P1 #8 |
| NEO-TOKEN-09 | `_private` token subtrees are usable inside the owning system | done | none (ATM-TOKEN-09 proves category breadth, not `_private`) | — (no change: owner passthrough) | computed colour from `_private.secret`; declaration present in owner types | `[core]` tokens.ts header contract; typegen golden `'_private.secret'` |
| NEO-TOKEN-10 | `font()` registry emits `--fonts-*` and `--font-weights-*` and the `font: 'sans'` macro paints family + weight | done | ATM-COND-05 | SYNC-owned `fragments/base/index.ts` merge landed via the Gap-1 easement (`buildFontWeightTokens`; no `font.ts` change needed; case hosted under `sync/`) | computed font-family + weight 400; evaluated-spec `fontWeights` pin; tokens-layer var membership | `[lib]` L2173–2191; `[core]` font registry `buildFontTokens`; `[atm]` P1 #9 |
| NEO-TOKEN-11 | `keyframes()` emits `@keyframes` in `@layer global` and `animations` tokens reference them | done | ATM-LAYER-05 | — (no change: engine prints `@keyframes` in `global`, token consumed by `var()`) | computed `animationName`; `@keyframes` present once | `[panda-v1]` `generator/__tests__/generate-keyframes.test.ts`; `[lib]` 31 keyframes; `[atm]` P1 #15 |
| NEO-TOKEN-12 | multiple refs in one value (`padding: '{spacing.1r} {spacing.2r}'`) all resolve | done | ATM-TOKEN-08 | — (no change: shorthand splits per side, each ref a `var()`) | computed padding pair | `[panda-v1]` `core/__tests__/serialize.test.ts` "expand multiple references" |
| NEO-TOKEN-13 | keyframe bodies resolve token refs and rhythm | done | ATM-LAYER-10 (RS-16 landed) | — (no change: engine resolves refs to `var()` and rhythm to calc in keyframe bodies) | `@keyframes grow` carries `var()` refs + root calcs with nothing verbatim; end state computes 32px + brand | `[panda-v1]` `generator/__tests__/generate-keyframes.test.ts` "should allow tokens" |
| NEO-TOKEN-14 | camelCase token paths define kebab vars that hardcoded uses resolve | done | ATM-TOKEN-13 (RS-41 landed) | — (no change: engine kebabs every segment at lower time) | sheet defines `--colors-ui-progress-track-mix-{foreground,background}` both modes, no camel ghosts; token-path and hardcoded-`var()` probes compute `rgb(3, 7, 18)` | `[core]` landing-baseline-core kebab defs; `[lib]` `shared.ts` `trackBackground`, `Slider.tsx` |
| RS-16 | Keyframe bodies print verbatim: input `keyframes({ grow: { from: { backgroundColor: '{colors.brand}', width: '4r' }, to: { … } } })` should emit `background-color: var(--colors-brand)` plus the rhythm calc inside `@keyframes grow`, but `compile()` prints `{colors.brand}`, `4r`, and bare token names literally with zero diagnostics (browsers drop the declarations as invalid CSS); waiting case NEO-TOKEN-13; landed as station ATM-LAYER-10 | done | ATM-LAYER-10 | — | R2 re-confirm `/tmp/n2cook-r1.mjs` green (refs→`var()`, rhythm→calc, nothing verbatim) | `[panda-v1]` `generator/__tests__/generate-keyframes.test.ts` "should allow tokens"; R1 probes `/tmp/r1token/probe.mjs`, `/tmp/r1token/probe2.mjs` |
| RS-30 | Shorthand aliases not lowered in keyframes: `h: '4'` lowers to `height: 4px` in `css()` but prints verbatim `h` inside `@keyframes` (S2 sweep K2; refs/rhythm resolve per RS-16). Keyframe values should run the alias table. Waiting: TOKEN-13 tail assertion (N3-cook extends). Station at the liaison's call. | done | ATM-LAYER-14 | — | N3b: keyframes lower via alias table + unit chain — `from { height: var(--sizes-4); }` with token (Panda `roll` parity), `height: 8px` fallback; LAYER-05 keyframes byte-identical | S2 sweep S2-F1 |
| RS-32 | Recipe + globalCss diagnostic hygiene (N3b stop-line): dedupe per-want warnings across responsive re-resolution — token10 expects 1 warning, gets 6× identical `opacity/"1"` from committed `compile_responsive_variants`; canonicalize globalCss props before token lookup so `fontFamily: 'sans'` resolves instead of warning verbatim (newly surfaced by N3a diagnostic threading; committed code sank globalCss diagnostics in a local sink). | open | token10 ext | — | atomic 188/189 with only token10 red; fix flips it | N3b RS liaison |
| RS-41 | Token-name serialization def/use mismatch (LIVE): `css_custom_property` kebabed only the category, so `colors.ui.progress.track.mixForeground` defined `--colors-ui-progress-track-mixForeground` while lib hardcodes Panda's kebab `--colors-ui-progress-track-mix-foreground` (`shared.ts` `trackBackground` with invalid `{...}` fallback) — Slider/Progress/Meter backdrops silently dropped; second victim `--colors-ui-table-row-muted-background` (button/input press ring). Fix: kebab every path segment (Panda parity, all 28 camel defs have core kebab counterparts); full def/use sweep proved the 3 kebab uses the only undefined token vars. | done | ATM-TOKEN-13 | — | NEO-TOKEN-14 paints token-path + hardcoded-var probes alike; 101 goldens pure-rename (SHORT-07 residual B37-only); atomic sole red stays RS-32 token10 | HQ Slider screenshot; `[core]` baseline; `[lib]` `tokens.ts:178-179`, `shared.ts:39-40` |
