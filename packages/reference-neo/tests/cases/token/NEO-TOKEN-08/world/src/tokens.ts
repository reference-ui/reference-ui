// Tokens for the TOKEN-08 world. It takes the neo fragment collector and
// emits the named spacing token the negative-token probe negates.
import { tokens } from '@reference-ui/neo'

tokens({
  spacing: {
    sm: { value: '0.5rem' },
  },
})
