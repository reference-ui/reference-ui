// Global fragment for the TOKEN-08 world. It takes the neo fragment
// collector and emits the rhythm root the negative-rhythm probes negate.
import { globalCss } from '@reference-ui/neo'

globalCss({ ':root': { '--spacing-root': '0.25rem' } })
