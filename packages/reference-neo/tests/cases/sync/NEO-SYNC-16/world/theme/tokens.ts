// Minimal tokens for the NEO-SYNC-16 world. It takes nothing and emits one
// brand color leaf, so the sheet and the static sibling declarations resolve.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
