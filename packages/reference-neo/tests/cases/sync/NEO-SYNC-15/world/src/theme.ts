// Global fragment for the SYNC-15 world. It takes the neo fragment collector
// and emits the rhythm root the card resolves its padding step against.
import { globalCss } from '@reference-ui/neo'

globalCss({ ':root': { '--spacing-root': '0.25rem' } })
