// Tokens for the LAYER-04 world. They take no input and emit the single
// brand leaf the utility probe paints, so the sheet carries populated
// tokens and utilities layers behind the reset layer under test.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
