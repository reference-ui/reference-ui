// Extraction wants for the LAYER-04 world. It takes the brand token and
// emits the one color utility the probe carries, so the utilities layer
// prints behind the reset layer under test.
import { css } from '@reference-ui/react'

export const brandCls = css({ color: 'brand' })
