# NEO-SYNC-01 — sync skeleton plus Rust handshake

Evidence: `[core]` handshake.

The first case-to-sync-to-browser loop: the runner syncs this world fresh,
then the spec checks the generated folder on disk and a token-driven style
in the browser.

> Search terms: smoke, round-trip, end-to-end, bootstrap, happy-path, hello sync, smoke run, sync/skeleton, sync/browser-loop
