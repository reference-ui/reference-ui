// Tokens for the GLOBAL-04 world. They take no input and emit the quote text
// colour the rule references. Collected once.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ui: {
      q: {
        foreground: { value: '#7c3aed' },
      },
    },
  },
})
