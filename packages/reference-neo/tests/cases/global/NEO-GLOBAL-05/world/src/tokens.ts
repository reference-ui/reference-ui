// Tokens for the GLOBAL-05 world. They take no input and emit the placeholder
// colour the rule references. Collected once.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ui: {
      field: {
        placeholder: { value: '#9ca3af' },
      },
    },
  },
})
