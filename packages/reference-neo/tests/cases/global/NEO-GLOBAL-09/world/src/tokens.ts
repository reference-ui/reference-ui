// Tokens for the GLOBAL-09 world. They take no input and emit the single ink
// colour the witness rule references with a brace path. Collected once.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ui: {
      plain: {
        ink: { value: '#7c3aed' },
      },
    },
  },
})
