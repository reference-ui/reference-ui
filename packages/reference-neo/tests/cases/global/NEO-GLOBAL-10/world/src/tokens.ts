// Tokens for the GLOBAL-10 world. They take no input and emit the base field
// border plus the invalid red the `:has()` compound references with brace
// paths. Collected once at sync.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    red: {
      500: { value: '#ef4444' },
    },
    ui: {
      field: {
        border: { value: '#d1d5db' },
      },
    },
  },
})
