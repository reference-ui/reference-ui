// Tokens for the COND-14 world. It takes the neo fragment collector and
// emits the three leaves the twin conditions paint with: placeholder red,
// file-button blue, and checked green. Each probe resolves one leaf.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    hint: { value: '#ef4444' },
    file: { value: '#3b82f6' },
    tick: { value: '#16a34a' },
  },
})
