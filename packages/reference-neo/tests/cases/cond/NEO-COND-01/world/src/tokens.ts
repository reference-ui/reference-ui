// Tokens for the NEO-COND-01 world. It takes no inputs and emits the two
// color leaves the hover case paints with, so the sheet and the browser
// resolve brand and ink without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    ink: { value: '#111111' },
  },
})
