// Global styles for the NEO-COND-05 world. It takes the neo fragment
// collector and emits the body ink baseline, so both paragraphs rest on ink
// until the parent key's hover arm could paint the sibling brand.
import { globalCss } from '@reference-ui/neo'

globalCss({
  body: {
    color: '#111111',
  },
})
