// Tokens for the NEO-COND-02 world. It takes no inputs and emits the two
// color leaves the triple case paints with, so the sheet and the browser
// resolve ink and paper without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ink: { value: '#111111' },
    paper: { value: '#ffffff' },
  },
})
