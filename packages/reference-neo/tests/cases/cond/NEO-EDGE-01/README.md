# NEO-EDGE-01 — pseudo props paint in their exercised state

The world calls css() with hover, focus, and disabled conditions. The spec
checks each variant rule lands in the sheet and paints via its data attribute.
