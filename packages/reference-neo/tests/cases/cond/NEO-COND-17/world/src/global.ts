// Global styles for the NEO-COND-17 world. It takes the neo fragment
// collector and emits the body ink baseline, so every control rests on
// ink until the scoped comma key paints its two members brand.
import { globalCss } from '@reference-ui/neo'

globalCss({
  body: {
    color: '#111111',
  },
})
