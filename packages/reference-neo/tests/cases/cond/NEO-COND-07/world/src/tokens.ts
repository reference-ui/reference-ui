// Tokens for the NEO-COND-07 world. It takes no inputs and emits the two
// color leaves the attribute and expanded cases paint with, so the sheet
// and the browser resolve brand and accent without any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    accent: { value: '#16a34a' },
  },
})
