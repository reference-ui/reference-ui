// Global styles for the NEO-COND-09 world. It takes the neo fragment
// collector and emits the body ink baseline, so the button rests on ink
// until either arm of the comma key paints it brand.
import { globalCss } from '@reference-ui/neo'

globalCss({
  body: {
    color: '#111111',
  },
})
