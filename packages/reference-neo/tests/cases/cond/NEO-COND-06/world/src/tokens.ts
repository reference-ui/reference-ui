// Tokens for the NEO-COND-06 world. It takes no inputs and emits the one
// color leaf the child case paints with, so the sheet and the browser
// resolve brand without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
