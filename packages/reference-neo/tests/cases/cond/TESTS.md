# COND ledger

| id | claim | status | engine | host | proof | evidence |
| --- | --- | --- | --- | --- | --- | --- |
| NEO-EDGE-01 | pseudo props paint in their exercised state | done | ATM-COND-02/10 | — | hover/focus/disabled paint via data twins; exactly 3 utilities | `[atm]` COND-02/10 |
| NEO-COND-01 | `_hover` matches both `:hover` and `[data-hover]` (one `:is()` wrap) | done | ATM-COND-02/10 | — | real `page.hover()` paints; `data-hover` paints; sheet has one selector | `[panda-v1]` `core/__tests__/atomic-rule.test.ts` "simple grouped condition"; `[lib]` `:is(:hover,[data-hover])` ×26 |
| NEO-COND-02 | `_hover`/`sm`/`_dark` composes hover × container × theme in one rule | done | ATM-COND-02/03/01 | — | paints only with all three; sheet nests `@container` outside the selector | `[panda-v1]` `atomic-rule.test.ts` "nested > property" |
| NEO-COND-03 | `_hover: { _disabled: … }` chains two `:is()` lists; `_disabled` list includes `[aria-disabled=true]` | done | ATM-COND-10 | — | paints only when both; `aria-disabled` alone + hover paints | `[panda-v1]` `atomic-rule.test.ts` "nested > nested"; `[lib]` L454 |
| NEO-COND-04 | `_light`/`_dark` utilities flip with `data-color-mode` on any ancestor | done | ATM-COND-03/08 | — (D1 already landed) | toggle attribute on `html`, then on a nested wrapper | `[atm]` P0 #1; `[panda-v1]` `atomic-rule.test.ts` "respect color mode" (four-way contrast); `[decision D1]` |
| NEO-COND-05 | Parent combinator key `'input:hover &'` paints the sibling when the input is hovered | done | ATM-COND-20 (RS-12 landed) | — | hover input; computed on sibling | `[panda-v1]` `atomic-rule.test.ts` "parent selector"; `[atm]` ATM-COND-20 |
| NEO-COND-06 | Child key `'& > p'` keeps the combinator; grandchildren do not paint | done | ATM-COND-14 | — | computed on child vs grandchild | `[panda-v1]` `atomic-rule.test.ts` nested selector; `[lib]` 14 child selectors |
| NEO-COND-07 | Attribute key with quotes `"&[data-state='open']"` plus `_expanded` list round-trips escaping | done | ATM-COND-09 | — | paints with the attribute; class string escapes `\'`/`\"` | `[panda-v1]` `rule-processor.test.ts` "css" attr test; `[lib]` `[data-slot="…"]` utilities |
| NEO-COND-08 | `_before`/`_after` with quoted `content` paint and sort after pseudo-classes | done | ATM-COND-10, ATM-LEAF-10, ATM-ORDER-03 | — | `getComputedStyle(el, '::before').content`; sheet order | `[panda-v1]` `conditions.test.ts` "pseudo-elements sort"; `[lib]` Trace D |
| NEO-COND-09 | Comma key `'&:focus, &:hover'` splits into two selectors on one class | done | ATM-COND-14 | — | focus alone paints; hover alone paints | `[panda-v1]` `atomic-rule.test.ts` "outlier" |
| NEO-COND-10 | Parent-of-self `':focus > &'` paints the child when the parent has focus | done | ATM-COND-20 (RS-12 landed) | — | focus parent; computed child | `[panda-v1]` `atomic-rule.test.ts` "outlier"; `[atm]` ATM-COND-20 |
| NEO-COND-11 | `_groupHover` / `_peerFocus` paint from an ancestor `.group` / preceding `.peer` | done | ATM-COND-09 | — | hover the group; focus the peer | `[atm]` P1 #13 |
| NEO-COND-12 | `_motionReduce`, `_osDark`, `_print` lower to the preset `@media` lists | done | ATM-COND-11 | — | `page.emulateMedia({ reducedMotion, colorScheme, media })` | `[atm]` P1 #12; `[lib]` reduced-motion in reset |
| NEO-COND-13 | Unknown `_hovr` yields a warning, no utility, no ghost class, and sync still succeeds | done | ATM-COND-12 | `sync` warns-and-succeeds by design (no change) | utility count unchanged; warning text captured | `[atm]` COND-12 |
| NEO-COND-14 | `_placeholder`, `_file`, `_checked` use the lib twin lists (`[data-placeholder]`, `::file-selector-button`, `[data-state="checked"]`) | done | ATM-COND-18 (RS-17 landed) | — | computed on input placeholder / file button / checked box + data-state twin; sheet pairs `::placeholder` with the twin and twins `_checked` four ways | `[lib]` L538, `::file-selector-button` ×3 |
| NEO-COND-15 | Mixed `@supports` + `@container` + `&:hover` in one nested key emits at-rules outside and sorts deterministically | done | ATM-COND-19 (RS-15 landed) | — | paints only when all hold (real hover + wide root; narrow and bogus-query controls stay); resync byte-identical | `[panda-v1]` `rule-processor.test.ts` "mixed vs at-rule" |
| NEO-COND-17 | Comma key `'&:not(:first-child), :only-child'` scopes the bare member to the class: classed non-first-child paints, its only-child descendant paints, a bare `:only-child` outside the class stays | done | ATM-COND-23 (SPEC-V2-68) | — | computed on scoped host + scoped kid (brand) vs unscoped only-child + first-child (ink); sheet carries `.<cls>:not(:first-child), .<cls> :only-child`, never a bare `, :only-child` | `[panda-v2]` `nested_selector_parity.rs:642` `nested_comma_group_scopes_member_without_ampersand`; `[mission]` operation-overmatch §1 entry 68 |
| RS-12 | Landed as ATM-COND-20 (unblocked NEO-COND-05, NEO-COND-10). Was: parent-combinator keys (`'input:hover &'`, `':focus > &'`) silently dropped — input `css({ 'input:hover &': { color: 'red.500' } })` should emit `input:hover .<class>` but `compile()` returned zero classes and zero diagnostics (same gap, no new row — resp RS-9 precedent) | done | ATM-COND-20 | — | R1 probe variants 05a–05d (`'input:hover &'`, nested, `'input &'`) all emit nothing; reconfirmed batch3; batch4 variants 10a–10d (`':focus > &'`, no-space, `'div:focus > &'`, nested) all emit nothing; reconfirmed `/tmp/cond10-r1/probe.mjs` (10a–10d + 05 control still zero classes/zero diagnostics, 09 comma control green); R2-resume re-confirm `/tmp/n1cook2-r1.mjs` green (both keys emit with zero diagnostics) | `[panda-v1]` `atomic-rule.test.ts:231` "[parent selector]"; R1 probes `/tmp/cond-batch2-r1/probe2.mjs`, `/tmp/cond-batch3-r1/probe.mjs`, `/tmp/cond-batch4-r1/probe.mjs`, `/tmp/cond-batch4-r1/probe2.mjs`, `/tmp/cond10-r1/probe.mjs` |
| RS-17 | `_file` is an unknown condition and `_placeholder` lacks its `[data-placeholder]` twin: input `css({ _placeholder: { color: 'red.500' }, _file: { color: 'blue.500' }, _checked: { color: 'green.500' } })` should emit `::placeholder` + `[data-placeholder]` selectors, a `::file-selector-button` rule, and the checked twin list, but `compile()` warns `Unknown condition "_file"`, emits `::placeholder` only, and only `_checked` carries twins; waiting case NEO-COND-14; landed as station ATM-COND-18 | done | ATM-COND-18 | — | R2 re-confirm `/tmp/n2cook-r1.mjs` green (`_file`→`::file-selector-button`, `_placeholder` twin, zero Unknown warnings) | `[lib]` styles-css L538 checked list, `::file-selector-button` ×3, `[data-placeholder]` ×3; R1 probe `/tmp/cond-batch4-r1/probe.mjs` |
| RS-15 | `@supports (...)` keys lower to selector fragments instead of at-rules: input `css({ '@supports (display: grid)': { sm: { '&:hover': { color: 'red.500' } } } })` should emit `@supports` outside `@container` with `:hover` on the selector, but `compile()` emits `@container` outside an invalid `…:hover:@supports (display: grid)` selector (classifier at `atomic/src/resolve/conditions/mod.rs:162` routes only `@media`/`@container` to at-rules; emitter `@supports` arms unreachable); waiting case NEO-COND-15 (+P5); landed as station ATM-COND-19 | done | ATM-COND-19 | — | R2 re-confirm `/tmp/n2cook-r1.mjs` + `/tmp/n2cook-r1b.mjs` green (`@supports` outside `@container`, `:hover` on selector) | `[panda-v1]` `core/__tests__/rule-processor.test.ts:1928` "mixed vs at-rule sorting"; R1 probes `/tmp/cond-batch4-r1/probe.mjs`, `/tmp/cond-batch4-r1/probe2.mjs` |
| RS-29 | Landed as ATM-COND-21 (PARITY-01 P5 empty arm fixed). Was: empty `@supports` query prints invalid CSS: `css({ '@supports': { color: 'red.500' } })` prints a bare `@supports {` block browsers drop, with zero diagnostics; should refuse with a diagnostic (D11 fail-closed). Waiting: PARITY-01 P5 empty arm (computed-only proof; sheet pin removed as invalid output). Station at the liaison's call. | done | ATM-COND-21 | — | R2 re-confirm `/tmp/n3cook-r1.mjs` green (`Unknown condition "@supports"` warning, no bare block, base paints) + PARITY-01 sheet carries no bare block and `#p5-empty` stays ink | N2 cook P5 |
