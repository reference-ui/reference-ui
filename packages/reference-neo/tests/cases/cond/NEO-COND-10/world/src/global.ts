// Global styles for the NEO-COND-10 world. It takes the neo fragment
// collector and emits the body ink baseline, so the child rests on ink
// until its parent's focus paints it brand.
import { globalCss } from '@reference-ui/neo'

globalCss({
  body: {
    color: '#111111',
  },
})
