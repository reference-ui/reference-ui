// Tokens for the COND-15 world. It takes the neo fragment collector and
// emits the ink base and the brand arm the mixed supports rule paints.
// Both probes share the base; only the gated arm resolves brand.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ink: { value: '#111111' },
    brand: { value: '#7c3aed' },
  },
})
