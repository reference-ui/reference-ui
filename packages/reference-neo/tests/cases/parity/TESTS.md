# PARITY ledger

| id | claim | status | engine | host | proof | evidence |
| --- | --- | --- | --- | --- | --- | --- |
| NEO-PARITY-01 | The mini-lib syncs and six representative components paint in light and dark | done | none (leans on all groups; R1 closed: `& + &`→P2, `red/abc`→F1; P4 cites RS-25, P5 cites RS-15, P8 cites RS-24, P14 cites RS-22, P15 cites RS-23) | — | sync green; six components paint; `data-color-mode` flip repaints both ways; P1 dark island as body child with no `data-panda-theme` (second root; the entry exports no `createPortal`); P2 `& + &` margin paints; P3 sheet has `:where([data-variant="primary"])`, zero bare-attr selectors; P4 skipped, cites RS-25; P5 skipped, cites RS-15; P6 inline `style="var(--x)"` consumes the globalCss root var; P7 `PrimitiveVariantRegistry` augmentation tsc-green (matrix consumer shape; Neo has no registry); P8 skipped, cites RS-24 (one face prints with size-adjust); P9 viewport-height recipe branch flips; P10 named container exposes `sidebar`; P11 `[data-state]:hover` paints only when both hold (negative halves + sheet shape; SpecPage offers no hover); P12 display/overflow/letter-spacing paint; P13 recipe first-child+responsive variant paints base + `@container` md; P14 skipped, cites RS-22; P15 skipped, cites RS-23; P16 spacing-union negatives typecheck (tsc harness with P7); P17 semantic dark color-mix redeclares per island; P18 vendor hyphenation passes through (lowercase-w Neo spelling); F1 `red/abc` passes through Panda-identical | `[lib]` lib-sheet §3; `[panda-v1]` atomic corpus; `[core]` matrix non-CHAIN; `[decision D21]`; R1 `/tmp/parity-r1/amp.test.ts`, `slash.test.ts`, `probes.test.ts`, `followup.test.ts`, `micro.test.ts` |
| NEO-PARITY-02 | Family census: every family in `lib-sheet-styles-css.md` §3 and `lib-sheet-global-css.md` §3 is present in the Neo sheet or listed as an approved absence; the absence list equals the union of group SPEC absences | done | none | — | node-side census over the Neo sheet: each of the 43 census rows (F1–F37 + T-A–T-F per oracle C §2) prints its markers or cites the checked-in 152-entry union (verified verbatim against the SPEC §union table and grounded keyword-by-keyword in the owning group SPECs); the 12 (b) rows verify still-blocked with RS owners; COND-14 cites RS-17 (relabeled from colliding RS-14 by captain ruling) | `[lib]` lib-sheet-styles-css §3, lib-sheet-global-css §3 |
| NEO-PARITY-03 | Consumer census: every specifier in `generated-folder-shape.md` §3 resolves from the world | done | none | — | node-side resolve from the world: `@reference-ui/react`, `@reference-ui/system`, `@reference-ui/system/baseSystem`, `@reference-ui/react/styles.css` resolve through the linked packages to the exports-mapped files; `react/styles.css`, `../.reference-ui/system/baseSystem.mjs` resolve as paths; zero `@reference-ui/styled` lib-src imports; D19 `types/` caveat re-checked live (11 importers, bounded 1–13, still live) | `[core]` generated-folder-shape §3 |
| NEO-PARITY-04 | No panda-isms: no `panda`, `data-panda-theme`, `--made-with-panda`, `@pandacss` in the generated folder | done | none | — | rg over the generated folder: `panda`, `data-panda-theme`, `--made-with-panda`, `@pandacss` → zero hits; `panda.config.*` absent; records the matrix `data-panda-theme` pin (`matrix/color-mode/tests/e2e/system-contract.spec.ts:103`) as the reason P1 asserts absence | PLAN §4.1 |
| RS-22 | `textGradient` emits a non-property: input `css({ textGradient: 'linear-gradient({colors.red.200}, {colors.blue.300})' })` should emit the Panda clip trio (background-image with resolved vars plus `-webkit-background-clip: text` and `color: transparent`) but `compileSync` emits `text-gradient: linear-gradient(var(--colors-red-200), var(--colors-blue-300))`, which browsers drop, with zero diagnostics; waiting case NEO-PARITY-01 (P14) | blocked-on-rs | canon `textGradient`→`text-gradient` property, no atomic expansion | — | R1-inside probe P14 (`/tmp/parity-r1/probes.test.ts`): refs resolve, property dead | `[panda-v1]` `gradient.test.ts:70` |
| RS-23 | Array `css` props extract nothing: input `<Div css={[{ color: 'blue.300' }, { backgroundColor: 'green.300' }]} />` should extract both objects (Panda `jsx.test.ts:529`) but `compileSync` emits zero utilities with zero diagnostics; the single-object control extracts and the `css([...])` call form merges, so the gap is JSX-prop-array extraction only; waiting case NEO-PARITY-01 (P15) | blocked-on-rs | ATM-SITE extraction (jsx `css` prop) | — | R1-inside probes P15 + controls (`/tmp/parity-r1/probes.test.ts`, `followup.test.ts`) | `[panda-v1]` `jsx.test.ts:529` |
| RS-24 | `fontFace` arrays fail sync: input `font('sans', { fontFace: [normalEntry, italicEntry], ... })` should print two style-distinguished `@font-face` rules (matrix font G8) but sync fails at native compile (`invalid baseSystem spec`: Rust `FontDefinition.font_face` is `Option<FontFaceDefinition>`, single only); waiting case NEO-PARITY-01 (P8) | blocked-on-rs | base-system `fonts.rs` (cf. RS-2 landed) | — | world sync failure on the array form; single-object control syncs green | matrix font `runtime.test.ts:169` |
| RS-25 | Radius pair shorthands emit non-properties: input `css({ borderTopRadius: '2r', ... })` (all six physical/logical pairs) should paint both addressed corners 8px (matrix spacing) but `compileSync` emits `border-top-radius:` / `border-start-radius:` etc., which are not CSS properties, so browsers drop them (computed `0px`) with zero diagnostics; waiting case NEO-PARITY-01 (P4) | blocked-on-rs | canon pair properties, no corner expansion | — | R1-inside probe P4 (`/tmp/parity-r1/probes.test.ts`) plus browser 0px (`NEO-PARITY-01` run) | matrix spacing `system-contract.spec.ts:143` |

RS-20 (`& + &`) and RS-21 (`red/abc`) were reserved for the R1 pair and stay
unfiled: both lowered cleanly and became the P2 and F1 probes instead.
