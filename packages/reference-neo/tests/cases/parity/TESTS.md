# PARITY ledger

| id | claim | status | engine | host | proof | evidence |
| --- | --- | --- | --- | --- | --- | --- |
| NEO-PARITY-01 | The mini-lib syncs and six representative components paint in light and dark | done | none (leans on all groups; R1 closed: `& + &`→P2, `red/abc`→F1; N2 live: P5 via RS-15/ATM-COND-19; N3 live: P4 via RS-25/ATM-SHORT-09, P8 via RS-24/ATM-LAYER-11, P14 via RS-22/ATM-SHORT-08, P19 via RS-26/ATM-UNIT-03, P20 via RS-27/ATM-LAYER-12, P21 via RS-28/ATM-LAYER-13, P5-empty via RS-29/ATM-COND-21; P15 live via RS-23/ATM-SITE-19 + NEO-PRIM-11) | — | sync green; six components paint; `data-color-mode` flip repaints both ways; P1 dark island as body child with no `data-panda-theme` (second root; the entry exports no `createPortal`); P2 `& + &` margin paints; P3 sheet has `:where([data-variant="primary"])`, zero bare-attr selectors; P4 pair shorthands expand to all eight corners at 8px, zero non-properties; P5 mixed `@supports`+container paints under the wide region only, empty `@supports` refused with a warning diagnostic (sheet carries no bare block; the diagnostic itself is unpinned — sync drops non-error diagnostics, COND-13 precedent); P6 inline `style="var(--x)"` consumes the globalCss root var; P7 `PrimitiveVariantRegistry` augmentation tsc-green (matrix consumer shape; Neo has no registry); P8 fontFace array prints the normal + italic faces, both with size-adjust; P9 viewport-height recipe branch flips; P10 named container exposes `sidebar`; P11 `[data-state]:hover` paints only when both hold (negative halves + sheet shape; SpecPage offers no hover); P12 display/overflow/letter-spacing paint; P13 recipe first-child+responsive variant paints base + `@container` md; P14 textGradient emits the clip trio with resolved refs; P15 array prop mints both utilities + both runtime plans + both paints (NEO-PRIM-11 carried arrays through the split); P16 spacing-union negatives typecheck (tsc harness with P7); P17 semantic dark color-mix redeclares per island; P18 vendor hyphenation passes through (lowercase-w Neo spelling); P19 globalCss numerics unitize (10px/42px) with unitless stays bare; P20 top-level `@media` braces and gates (matching arm paints, far arm holds); P21 breakpoint keys lower through sm/lg queries (base+sm paint narrow, lg wins wide); F1 `red/abc` passes through Panda-identical | `[lib]` lib-sheet §3; `[panda-v1]` atomic corpus; `[core]` matrix non-CHAIN; `[decision D21]`; R1 `/tmp/parity-r1/amp.test.ts`, `slash.test.ts`, `probes.test.ts`, `followup.test.ts`, `micro.test.ts`; N3 R1 `/tmp/n3cook-r1.mjs` |
| NEO-PARITY-02 | Family census: every family in `lib-sheet-styles-css.md` §3 and `lib-sheet-global-css.md` §3 is present in the Neo sheet or listed as an approved absence; the absence list equals the union of group SPEC absences | done | none | — | node-side census over the Neo sheet: each of the 43 census rows (F1–F37 + T-A–T-F per oracle C §2) prints its markers or cites the checked-in 159-entry union (verified verbatim against the SPEC §union table and grounded keyword-by-keyword in the owning group SPECs); the (b) table is empty — all six N2 rows landed (TOKEN-13, COND-14/15, SITE-13/14, RECIPE-07) | `[lib]` lib-sheet-styles-css §3, lib-sheet-global-css §3 |
| NEO-PARITY-03 | Consumer census: every specifier in `generated-folder-shape.md` §3 resolves from the world | done | none | — | node-side resolve from the world: `@reference-ui/react`, `@reference-ui/system`, `@reference-ui/system/baseSystem`, `@reference-ui/react/styles.css` resolve through the linked packages to the exports-mapped files; `react/styles.css`, `../.reference-ui/system/baseSystem.mjs` resolve as paths; zero `@reference-ui/styled` lib-src imports; D19 `types/` caveat re-checked live (11 importers, bounded 1–13, still live) | `[core]` generated-folder-shape §3 |
| NEO-PARITY-04 | No panda-isms: no `panda`, `data-panda-theme`, `--made-with-panda`, `@pandacss` in the generated folder | done | none | — | rg over the generated folder: `panda`, `data-panda-theme`, `--made-with-panda`, `@pandacss` → zero hits; `panda.config.*` absent; records the matrix `data-panda-theme` pin (`matrix/color-mode/tests/e2e/system-contract.spec.ts:103`) as the reason P1 asserts absence | PLAN §4.1 |
| RS-22 | Landed as ATM-SHORT-08 (P14 live in NEO-PARITY-01). Was: `textGradient` emits a non-property: input `css({ textGradient: 'linear-gradient({colors.red.200}, {colors.blue.300})' })` should emit the Panda clip trio (background-image with resolved vars plus `-webkit-background-clip: text` and `color: transparent`) but `compileSync` emits `text-gradient: linear-gradient(var(--colors-red-200), var(--colors-blue-300))`, which browsers drop, with zero diagnostics; waiting case NEO-PARITY-01 (P14) | done | ATM-SHORT-08 | — | R2 re-confirm `/tmp/n3cook-r1.mjs` green (clip trio, zero non-properties) + PARITY-01 P14 paints the trio | `[panda-v1]` `gradient.test.ts:70` |
| RS-23 | Landed as ATM-SITE-19 (P15 live in NEO-PARITY-01; paint landed as NEO-PRIM-11). Was: array `css` props extract nothing: input `<Div css={[{ color: 'blue.300' }, { backgroundColor: 'green.300' }]} />` should extract both objects (Panda `jsx.test.ts:529`) but `compileSync` emits zero utilities with zero diagnostics; the single-object control extracts and the `css([...])` call form merges, so the gap is JSX-prop-array extraction only; waiting case NEO-PARITY-01 (P15) | done | ATM-SITE-19 | — | R2 re-confirm `/tmp/n3cook-r1.mjs` green (both utilities, zero diagnostics) + PARITY-01 sheet mints the ink utility and the array form resolves both runtime plans | `[panda-v1]` `jsx.test.ts:529` |
| RS-24 | Landed as ATM-LAYER-11 (P8 live in NEO-PARITY-01). Was: `fontFace` arrays fail sync: input `font('sans', { fontFace: [normalEntry, italicEntry], ... })` should print two style-distinguished `@font-face` rules (matrix font G8) but sync fails at native compile (`invalid baseSystem spec`: Rust `FontDefinition.font_face` is `Option<FontFaceDefinition>`, single only); waiting case NEO-PARITY-01 (P8) | done | ATM-LAYER-11 | — | R2 re-confirm `/tmp/n3cook-r1.mjs` green (two style-distinguished faces) + PARITY-01 sheet prints both with size-adjust | matrix font `runtime.test.ts:169` |
| RS-25 | Landed as ATM-SHORT-09 (P4 live in NEO-PARITY-01). Was: radius pair shorthands emit non-properties: input `css({ borderTopRadius: '2r', ... })` (all six physical/logical pairs) should paint both addressed corners 8px (matrix spacing) but `compileSync` emits `border-top-radius:` / `border-start-radius:` etc., which are not CSS properties, so browsers drop them (computed `0px`) with zero diagnostics; waiting case NEO-PARITY-01 (P4) | done | ATM-SHORT-09 | — | R2 re-confirm `/tmp/n3cook-r1.mjs` green (eight corners, zero non-properties) + PARITY-01 P4 paints all eight at 8px | matrix spacing `system-contract.spec.ts:143` |
| RS-31 — VOID (captain S3) | Filing voided: Neo-side prim gap mis-laned as RS (RS numbers are engine-lane). Refiled as NEO-PRIM-11 (open). Original text in git history. Landed as NEO-PRIM-11. | retired | — | — | — | — |
| RS-38 | Logical border-axis shorthands emit raw reset-shorthands: input `<Div borderInline="1px solid" borderInlineColor="blue.600" />` (plus `borderBlock`/`borderX`/`borderY` twins) should decompose to width+style longhands with the sibling color winning (core `border.ts:53-68`) but `compileSync` emits bare `border-inline: 1px solid`, resetting color to currentColor with zero diagnostics; the four `*Start`/`*End` corner utilities already decompose. B37-family-audit filing (latent: no lib authorship); full text in `docs/evidence/b37-family-audit.md` §3 | open | RS liaison (suggested station ATM-SHORT-10) | — | width+style longhands, sibling color wins, no bare `border-inline:`/`border-block:` | `[core]` `border.ts:53-68`; `[audit]` b37-family-audit §2 #3 |
| RS-39 | Landed as ATM-SHORT-11 + NEO-CSS-13. Was: `flex` misrouted through border width/style/color decomposition (LIVE): `flex="1"` emitted lone `flex-grow: 1` (basis `auto`, core computes `0%`) instead of `flex: 1 1 0%`. Fix: family gate in `border.rs` (width/style/color longhand names, not len-3 count) + `flex.rs` emitting Panda's utility values (`1`→`1 1 0%`, `auto`→`1 1 auto`, `initial`→`0 1 auto`, `none`; all else raw). Provenance: Panda behavior — `@pandacss/preset-base` flex values, confirmed live on Panda 1.11.1 with `presets: []` (core ships zero flex code) | done | ATM-SHORT-11 | — | R2 re-confirm green (triples + raw passthrough, zero flex longhands) + NEO-CSS-13 paints the computed `0%`-vs-`auto` basis split | `[core]` baseline `:991,5183,5371`; `[audit]` b37-family-audit §2 #7 |
| RS-40 | Len-3 trap: non-border 3-longhand props routed through border semantics: inputs `columns="100px 3"`, `listStyle="square inside"`, `gridTemplate="1fr 1fr"`, `lineClamp="2"`, `caret="red"`, `fontSynthesis="weight style"`, `marker`/`verticalAlign` should pass through raw (core/Panda shape — no core utility) but `compileSync` emits border-classified junk (`max-lines: 2px` invalid, `grid-template-areas: 1fr`, `caret-shape: red`, `list-style-image: square`, lone `column-width`, …) with zero diagnostics; `columnRule`/`rowRule` decomposition is accidentally-correct and must stay pinned. B37-family-audit filing (latent: no lib authorship); gate `border.rs` on family, not count; full text in `docs/evidence/b37-family-audit.md` §3 | open | RS liaison (suggested station ATM-SHORT-12) | — | raw passthrough per prop; columnRule/rowRule decomposition pinned | `[audit]` b37-family-audit §2 #15-16 |

RS-20 (`& + &`) and RS-21 (`red/abc`) were reserved for the R1 pair and stay
unfiled: both lowered cleanly and became the P2 and F1 probes instead.
