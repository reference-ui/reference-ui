// Tokens for the NEO-LAYER-02 world. They take no inputs and emit the one
// downstream leaf the overriding utility paints with; brass arrives from the
// upstream extends entry instead, so the two systems each own one colour.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ink: { value: '#111111' },
  },
})
