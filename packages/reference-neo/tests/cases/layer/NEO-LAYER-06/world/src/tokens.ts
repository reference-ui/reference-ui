// Tokens for the LAYER-06 world. They take no input and emit the single
// brand token both the recipe and the utility resolve. Collected once.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
