// Tokens for the LAYER-03 world. They take no input and emit the ink text
// colour the probes paint. Collected once.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ink: { value: '#111111' },
  },
})
