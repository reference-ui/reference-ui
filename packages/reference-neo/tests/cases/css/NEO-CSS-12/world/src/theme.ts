// Global fragment for the CSS-12 world. It takes the neo fragment collector
// and emits the rhythm root the probes resolve their steps against.
import { globalCss } from '@reference-ui/neo'

globalCss({ ':root': { '--spacing-root': '0.25rem' } })
