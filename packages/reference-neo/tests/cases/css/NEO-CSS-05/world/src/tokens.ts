// Tokens for the NEO-CSS-05 world. It takes no inputs and emits the one
// color leaf the holes case paints with, so the sheet and the browser
// resolve brand without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
