# NEO-CSS-01 — runtime css() paints from compiled plans

The world script calls the synced css() bundle. The spec checks the sheet
carries exactly the wanted utilities and the classes paint in the browser.

> Search terms: smoke, baseline, stylesheet, happy path, hello world, sanity check, css/baseline, css/compiled-sheet
