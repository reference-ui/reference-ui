// Tokens for the NEO-RECIPE-09 world. It takes no inputs and emits the color
// leaves the chip recipe and its hover arm paint with, so both tones resolve
// without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    ink: { value: '#111111' },
    paper: { value: '#ffffff' },
  },
})
