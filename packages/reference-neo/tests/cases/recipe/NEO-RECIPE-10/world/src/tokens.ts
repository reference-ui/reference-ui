// Tokens for the NEO-RECIPE-10 world. It takes no inputs and emits the color
// leaves the flag recipe and the overriding utility paint with, so both the
// recipe and the utility resolve without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    ink: { value: '#111111' },
    paper: { value: '#ffffff' },
  },
})
