// Tokens for the NEO-RECIPE-11 world. It takes no inputs and emits the color
// leaves the inferred chip recipe paints with, so both tones plus the base
// resolve without reaching for any other system.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    ink: { value: '#111111' },
    paper: { value: '#ffffff' },
  },
})
