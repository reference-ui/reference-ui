// Tokens for the NEO-RECIPE-03 world. It takes no inputs and emits the one
// ink leaf the toggle base paints with; the boolean arms use literal display
// values, so no other scale is needed.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    ink: { value: '#111111' },
  },
})
