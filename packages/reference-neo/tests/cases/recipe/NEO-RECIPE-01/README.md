# NEO-RECIPE-01 — recipe variants resolve to closed classes

The world defines one recipe with base, two variant axes, and a compound. The
spec checks each selection paints its computed styles and the compound fires
only when both predicates hold.
