Smoke test: a grey page with a crimson dot centered proves the harness loop works.

This is the smallest honest neo case. The world is a single static page, and the
spec asserts the dot's computed background color plus its centering in the
viewport. If this case runs green, discovery, the local HTTP bubble, headless
execution, and artifact collection all work end to end.
