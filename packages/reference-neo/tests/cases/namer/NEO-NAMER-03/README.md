# NEO-NAMER-03 — a miss constructs its miss class and warns once in browser dev

The world paints one static ember probe and one probe whose color comes
from a function parameter, so the extractor mints no atom for it. The
spec checks the miss probe carries its constructed miss class, paints
the inherited ink, and reports exactly one browser-dev diagnostic
naming the value and the prop. Node stays silent: there is no sheet to
probe outside the browser. A miss class is not a ghost.

Evidence: `[atm]` GHOST (`docs/evidence/atomic-claims.md` §6); the miss
shape leans on `NEO-MERGE-06`.

> Search terms: miss class, unextractable, dynamic value, dev diagnostic, browser-only warning, inherited ink, no ghost, cssom probe, node silence
