// Tokens for the NEO-STATIC-02 world. They take no inputs and emit the three
// neutral leaves the wildcard expands, so the atom count must equal exactly
// three — no more, no fewer.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    n100: { value: '#f4f4f5' },
    n200: { value: '#e4e4e7' },
    n300: { value: '#d4d4d8' },
  },
})
