// Token fragment for the PRIM-03 world. It takes the neo fragment collector
// and emits the brand and ink colors the hover probes resolve against.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
    ink: { value: '#111111' },
  },
})
