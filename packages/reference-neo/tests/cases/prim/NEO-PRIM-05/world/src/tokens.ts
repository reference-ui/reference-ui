// Token fragment for the PRIM-05 world. It takes the neo fragment collector
// and emits the brand color the inner-tree probe paints with.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
