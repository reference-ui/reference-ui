// Token fragment for the PRIM-11 world. It takes the neo fragment collector
// and emits the blue and green leaves the array css probe resolves against.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    blue: {
      300: { value: '#93c5fd' },
    },
    green: {
      300: { value: '#86efac' },
    },
  },
})
