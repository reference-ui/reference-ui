// Token fragment for the PRIM-06 world. It takes the neo fragment collector
// and emits the brand color the variant recipe resolves its paint from.
import { tokens } from '@reference-ui/neo'

tokens({
  colors: {
    brand: { value: '#7c3aed' },
  },
})
