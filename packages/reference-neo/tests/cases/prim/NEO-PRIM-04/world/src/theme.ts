// Global fragment for the PRIM-04 world. It takes the neo fragment collector
// and emits the rhythm root the array probes resolve their steps against.
import { globalCss } from '@reference-ui/neo'

globalCss({ ':root': { '--spacing-root': '0.25rem' } })
