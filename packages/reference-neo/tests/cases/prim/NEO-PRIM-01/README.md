# NEO-PRIM-01 — a native primitive paints through the generated entry

The world renders one Div with style props plus a DOM prop. The spec checks
the styles paint, the DOM prop lands, and data-layer names the system.
