// Layout constants shared by the config store and the path helpers.
// This module takes nothing and emits the default generated folder name.
// It is a Neo-owned copy of the matching core constants seam.

export const DEFAULT_OUT_DIR = '.reference-ui'
