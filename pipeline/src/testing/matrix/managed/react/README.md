`react/` owns the matrix fixtures' managed browser runtime profiles.

Each profile fixes the React version set, the DOM mountpoint, and the generated
`src/main.tsx` entrypoint that setup materializes into every matrix fixture.