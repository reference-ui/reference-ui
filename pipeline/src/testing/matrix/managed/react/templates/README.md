# React Templates

This folder contains the Liquid templates used by the managed matrix React module.

- `main.tsx.liquid` renders the generated `src/main.tsx` entrypoint for matrix fixtures and synthetic consumers.
  It wires the managed React mountpoint, shared stylesheet import, and the fixture entry import path.