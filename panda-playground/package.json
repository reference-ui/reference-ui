{
  "name": "panda-playground",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "pnpm exec panda codegen && pnpm exec panda cssgen --outfile styled-system/styles.css",
    "pretest": "pnpm run sync",
    "test": "vitest run"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.10.0",
    "@types/node": "^25.1.0",
    "postcss": "^8.5.8",
    "typescript": "~5.9.3",
    "vitest": "^4.0.18"
  }
}