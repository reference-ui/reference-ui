name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: write

concurrency:
  group: changesets-main
  cancel-in-progress: false

jobs:
  changesets:
    runs-on: ubuntu-latest
    outputs:
      has_changesets: ${{ steps.changesets.outputs.hasChangesets }}
      should_publish: ${{ steps.detect.outputs.should_publish }}
      needs_rust: ${{ steps.detect.outputs.needs_rust }}
      release_packages_json: ${{ steps.detect.outputs.release_packages_json }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.29.3

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create or update release PR
        id: changesets
        uses: changesets/action@v1
        with:
          version: pnpm version-packages
        env:
          # Default Actions tokens can be blocked from PR creation by repository settings.
          # If present, use a dedicated token that is allowed to open/update release PRs.
          GITHUB_TOKEN: ${{ secrets.CHANGESETS_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Detect release targets
        id: detect
        run: pnpm run release:detect

  compile-rust:
    needs: changesets
    if: ${{ needs.changesets.outputs.has_changesets != 'true' && needs.changesets.outputs.needs_rust == 'true' }}
    uses: ./.github/workflows/rust-compile.yml
    with:
      matrix_json: >-
        [{"runner":"macos-15-intel","target":"x86_64-apple-darwin"},{"runner":"macos-15","target":"aarch64-apple-darwin"},{"runner":"windows-latest","target":"x86_64-pc-windows-msvc"},{"runner":"ubuntu-latest","target":"x86_64-unknown-linux-gnu"}]
      ref: ${{ github.sha }}

  publish:
    runs-on: ubuntu-latest
    needs:
      - changesets
      - compile-rust
    if: ${{ needs.changesets.outputs.has_changesets != 'true' && needs.changesets.outputs.should_publish == 'true' && (needs.changesets.outputs.needs_rust != 'true' || needs.compile-rust.result == 'success') }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.29.3

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download native artifacts
        if: needs.changesets.outputs.needs_rust == 'true'
        uses: actions/download-artifact@v7
        with:
          pattern: bindings-*
          path: packages/reference-rs/artifacts

      - name: Publish packages
        run: pnpm run release:publish
        env:
          GITHUB_TOKEN: ${{ secrets.CHANGESETS_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
