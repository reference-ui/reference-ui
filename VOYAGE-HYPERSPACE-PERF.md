# Voyage perf log — Hyperspace Run

Every recon map and every Fasthull cycle ends here. A disproven
hypothesis is a complete cycle — log it and move on. Terse entries;
the diff holds the detail. Append, never rewrite history.

HQ 2026-09-20 ~23:49: performance-only. Report sync, RSS, styles.css,
and runtime-data vs pin `5eda2c60b7e5` and vs Panda v2
(`/Users/ryn/Developer/panda-bench/reports/latest/`).

```text
## Wave N, recon — <one-line map>
- Avenues: <id: lever (sync|RSS|css|data), disjoint with …>
- Killed: <suspect, evidence, one line>
- Next crews: <tree-a hypothesis; tree-b hypothesis; …>

## Wave N, <lane a|b|c> — <one-line hypothesis>
- Tried: <change, files touched>
- Bench (locked load, medians): <sync / RSS / css / data vs wave-start>
- Stability: <agentrs + agentneo; churn or why skipped>
- Review: <VERIFIED or GAPS>
- Outcome: <landed | rode forward | died, and why in one line>
```

No entries yet.

[2026-09-20T22:57Z recon-1] Brief ingested (brief, perf log, fasthull, bench README/PLAN, Neo pin 5eda2c60b7e5, Panda goalpost+notes). Gap: enterprise 3.51s→645ms, 796→261 MiB, 14.3→2.7 MiB css, 3.9 MiB vs 71 KiB JS. Fanning 3 nested workers (bridge/sheets, parse-scan, bundle levers); lead runs the one enterprise bench + synthesis. No product edits.

[2026-09-21T00:05Z recon-1] Lead bench: enterprise 4.19s/810.7 MiB (box shared w/ workers; pin 3.51s/796), bundle byte-identical to pin (14.3 MiB css, 3.9 MiB data). Firsthand counts: @layer recipes = 12.47/15.0 MiB css (83%); runtime-data recipes table = 4.006/4.09 MiB (98%), of which combinations 2.50 MiB (62.7%, ~27 precomputed joins/recipe) + responsiveVariantMap 1.06 MiB (26.6%) — both mechanical derivations of variantMap (0.20 MiB). Workers A/B/C still profiling.

[2026-09-21T00:12Z recon-1] Worker A filed: S1 LIVE (11-field CompileResult, 5 discarded incl. per-atom css.classes + recipes serialized TWICE; TS seam doesn't declare dead fields) + S2 LIVE (double print shares everything but ~2 token selector lines; utilities re-sorted 2x). Worker B filed: S3 LIVE (15k arenas+ASTs whole-compile, 7 retention sites, 2x transient source Vecs) + S4 LIVE (~8 full walks x 12k dead files, zero pre-filter) + S5 LIVE (read_to_string every file THEN glob-filter; 3 reads/2 scans per sync). Awaiting worker C (bundle).

[2026-09-21T00:30Z recon-1b] Second rotation ingested: brief §1, prior 3 entries, fasthull, bench README/PLAN, pin 5eda2c60b7e5, Panda goalpost+notes. No fasthull-recon-1.md exists. Plan: 3 nested workers (A bridge/sheets S1+S2, B parse/scan S3+S4+S5, C bundle S6+S7), lead runs own bench + synthesis. Static counting first, lead enterprise bench for firsthand numbers. No product edits.

[2026-09-21T00:38Z recon-1b] Lead bench (firsthand): medium 451ms/205.2 MiB, enterprise 3.42s/808.3 MiB (pin 3.51s/796.0); bundle byte-identical to pin (14.3 MiB css/825.0 KiB gzip, 3.9 MiB data/211.3 KiB gzip). Workers A/B/C profiling. Lead now reproducing headline bundle counts firsthand.

[2026-09-21T00:55Z recon-1b] Lead firsthand counts (kept medium repo): styled/react styles.css BYTE-IDENTICAL (2,554,607 B each, cmp clean); @layer recipes = 2,302,549 B (90.1% of sheet, 28,586 rules); runtime-data recipes table 723,850/808,655 B (89.5%) across 88 entries: combinations 63.2% (sample 36 precomputed joins like "accent|sm|info|tight"), responsiveVariantMap 27.4%, variantMap 5.1% — matches dead lead's enterprise shape (62.7/26.6%). Awaiting A/B/C.

[2026-09-21T01:10Z recon-1b] All workers filed, all 7 suspects LIVE with fresh firsthand evidence. Lead spot-verified headline sites (11-field struct vs 6-field seam, lib.rs whole-compile retention comment, scanner + sources.rs read-then-filter order). Writing fasthull-recon-1.md + Wave 1 entry.

## Wave 1, recon — all 7 suspects LIVE: dead bridge payload, double sheet print, dead-file parse/walk/read tax, recipe fan-out in css + data
- Avenues: A1 slim N-API result (sync+RSS, lane a); A2 print sheet once (sync, lane a); A3 drop per-file retention (RSS, lane b); A4 pre-parse byte gate (sync, lane b); A5 filter-before-read + single collect (sync+RSS, lane b); A6 recipe responsive/grouping skip (css, lane c); A7 derive recipe tables at runtime (data, lane d). Piggybacks: A8 host-set clones (b), A9 compound dedup (c), A10 fixed namer bridge tax (future). Shared files: assembly.rs (a-internal), emitter/mod.rs (a/c diff fns), contracts (a/d diff sections), recipes/ dir (c/d disjoint files).
- Killed: none of the 7; minor: Tasty has no sync-path role; collectCompileFiles zero callers; bench dual sheets byte-identical (2-line diff is dark-token worst case).
- Next crews: perf-1-a cold payload (A1+A2); perf-1-b dead-file fast path (A3+A4+A5, architect holds harvest fence); perf-1-c recipe sheet grouping (A6b now, A6a wave 2); perf-1-d recipe tables (A7).

Wave 1 recon: COMPLETE — map filed, 7 avenues, next crews: perf-1-a (A1+A2), perf-1-b (A3+A4+A5), perf-1-c (A6b), perf-1-d (A7)
## Wave 1, lane e — deepsee observability tooling (burndown + RSS + bundle accounting)
- Tried: new deepsee/ tooling only (cli, worker-phases, sample-parse, burndown, rss, bundle-css, bundle-data, bundle, README) + evidence note fasthull-burndown-1.md; zero engine/bench/load edits
- Bench (tooling moves no numbers by design; reconciliation instead): enterprise burndown serde 23.6% / assembly 19.0% / emit 11.0% of sync; RSS worker 806.7 / parent 801.5 vs pin 796.0 MiB; bundle byte-identical to pin (15007762/4090907 raw AND gzip)
- Stability: no engine file touched (git status: log + 2 new paths only) so no agentrs smoke owed; sync deterministic (cmp-clean); agentneo q 0 errors
- Review: VERIFIED (independent agent, firsthand on small/medium + ent-2 artifact match: cross-check straddle ±, parent≈worker, residuals 0/0, raw=pin, tree clean, note matches)
- Outcome: landed — one command (`deepsee all --scale enterprise`) gives the full burndown; lanes a–d have their quantified targets

Wave 1, lane e: VERIFIED — deepsee tooling complete, enterprise burndown + byte-exact bundle accounting filed, review passed.
## Wave 1, lane b — dead-file fast path: files that cannot contain styling cost no read, parse, walk, or retain
- Tried: two byte gates (STYLING_SKIP skips W3/W4/W6/W7+resolve/bindings/host-union; STRING_SKIP skips W5 only; parse/W1/W2/records/identity/errors never skip) + A8 visitor borrows + A5a scope-before-read + A5b single-collect + A5d styletrace staged-content + A5e scanner pre-gate + A3 phase-drop. Dropped A5c (walk-memo needs module-graph interior mutability; measured target ~nil). 16 files + tests/gates.rs; module-graph untouched; harvest fence held.
- Bench (locked load, medians, box shared): small 140/121 vs 192/123.9; medium 411-421/~197 vs 460/204.0; enterprise 2873ms/~770MiB vs 3495/773.7 (-17.9% sync, ~620ms, clears 65ms spread ~10x; RSS within spread, no claim). Churn 4.84-4.96s vs 5.04s HEAD. Bytes cmp-identical all scales + churn (ent css 15007762/data 4090907 = pin). Vs Panda goalpost: ent sync 2.87s vs 645ms, RSS 770 vs 261 MiB — gap narrowed, still large.
- Stability: agentrs c atomic green (509 incl. 12 new gates); v atomic 298/299 lane AND HEAD (ATM-SITE-54 pre-existing, shown identical); styletrace failure set byte-identical to HEAD (+1 new staged pass); agentneo 173/173; quality 0 violations.
- Review: VERIFIED ([2026-09-21T00:35Z perf-1-b-reviewer], fresh agent, firsthand: 2x full suite + ent 3x + same-session HEAD stash comparison + 3 own adversarial fixtures byte-identical).
- Outcome: landed pending captain merge; no commits (crews never commit).

Wave 1, lane b: VERIFIED — dead-file fast path, enterprise sync -17.9% byte-identical, churn unregressed.
## Wave 1, lane a — cold payload: slim N-API result (proof channel) + print the utility sheet once
- Tried: native.rs serializes slim view (6 live fields) by default, full on logs:'proof'; new build_stylesheets_with shares one recipes+utilities suffix (existing builders untouched); contracts slimmed; stations re-pointed via proof flags (compileCase choke + 15 direct calls); new seam slim-pin + emitter equivalence tests. 25 files, all in-boundary; types.rs/system_layers/grouping-fns untouched.
- Bench (locked load, medians, box shared; reviewer reproduced all): small 181→142ms sync, RSS no-signal (126.6→126.1 in spread); medium 497→423ms / 208.5→199.3MiB; enterprise 3.47→3.18s / 799→~700MiB (7 post samples all < pre); churn 5.66→4.4s / 1070→~645MiB. Bundle bytes EXACT-equal base-vs-final (css+data raw+gzip, all scales + churn). Asterisk: 2 gzip values differ pin-vs-base ±1B pre-existing (identical base/final).
- Stability: v atomic 299/300 (ATM-SITE-54 pre-existing — stash-proven + reviewer causal check, bare theme-pkg rung, no fixture node_modules), v contracts 13/13, c atomic 499 green, agentneo 173/173 ok, q clean (rs 5 + neo 2). Churn RUN as guardrail: unregressed, big win.
- Review: VERIFIED (disjoint reviewer, firsthand bench + stability + diff; wart-note: wrapper stylePlans still required vs contracts optional — suggest aligning at merge).
- Outcome: VERIFIED, ready for captain merge.

Wave 1, lane a: VERIFIED — cold payload wins sync+RSS at every scale with byte-identical bundles.
## Wave 1, lane d — derive recipe tables at runtime (COMPLETE)
- Tried: serde-skip combinations+responsiveVariantMap (plan.rs), ship per-table responsiveBreakpoints (table.rs; in-memory build kept, mod.rs untouched), compose-on-read + bp-gated responsive derivation in recipe.ts with legacy fallbacks; renegotiated 5 NEO specs + 7 engine stations + fixture + census react pins + 6 doc lines to literal expectations. 26 files, no forbidden touches (reviewer-confirmed).
- Bench (locked load, medians; box shared w/ 4 siblings): small sync 150→147ms, RSS 121.8→121.4MiB, css 538.0KiB IDENTICAL, data 249.8→102.5KiB (-59%); medium 461→432ms, 207.6→198.3MiB, css 2.4MiB IDENTICAL, data 789.7→164.3KiB (-79%); enterprise (1 run) 3.51→3.35s, 796→728.7MiB, css 14.3MiB IDENTICAL, data 3.9MiB→518.2KiB (-87%). E2E: 2376/2376 combos + 3960/3960 responsive re-derived from new inputs at medium; css cmp-clean.
- Stability: cargo atomic 498 pass; atomic vitest 298/299 (SITE-54 pre-existing, proven red on stashed HEAD, wants/specifier station); neo vitest 229/229; contracts 13/13; agentneo 173/173 ok; neo gate 0 errors. Churn HOLDS: data 386.3→143.2KiB, css identical, sync/RSS delta inside run spread. RS in-memory build retained deliberately (lane c owns mod.rs; deletion = wave-2 cleanup, zero byte delta).
- Review: VERIFIED (distinct nested agent, firsthand; memo fasthull-1d-review.md). Contract renegotiation SIGNED: paint assertions byte-untouched, literals stronger than self-referential pins, legacy fallbacks tested. Caveats: bp list 52B/table (not ~35B); baseSystem co-shrink code-verified only.
- Outcome: VERIFIED — ready for captain merge. Merge flags: spec-recipes.test.ts:35-44 + SITE-15:20-21 share hunks with lane a; harvest-census EXPECTED_BYTES shares hunk with lane c; contracts/js types + fixture are disjoint-section shares with lane a.

Wave 1, lane d: VERIFIED — recipe tables derived at runtime, data -59%/-79%/-87%, css identical, paint holds.
## Wave 1, lane c — group byte-identical recipe blocks under one comma selector (A6b + A9)
- Tried: nothing — killed pre-implementation. Census (3 agents, firsthand, own kept repos): small 3,021 rules → 32 within-wrap dup pairs, ALL cross-block-instance, 0 adjacent; medium 14,501 → 422 groups, 0 adjacent. Merge-everything sim: −1.5 KB raw / gzip +71 B small, −15 KB raw / +1,382 B medium. A9 CSS exact-dupes: 0 both scales.
- Bench (locked load, medians): baseline only (own tree, box shared — wall/RSS conditions-affected, bytes pin-identical): small 472ms/128.9 MiB css 538.0 KiB, medium 452ms/217.8 MiB css 2.4 MiB. No implementation bench — no change.
- Stability: n/a (no diff). Tree holds only log + reports/latest scratch; zero product edits.
- Review: architect (≠ profiler) KILL-concurs firsthand: cross-block merge moves selectors across co-matching classes (composeClasses stacks base+variant+compound; order stations ATM-RECIPE-05/NEO-RECIPE-08), only safe variant (adjacent) has 0 sites, move (a) needs lane b's extract signal (Wave 2).
- Outcome: died — A6(b) grouping KILLED: 0 order-safe groupable sites at both scales and merge-everything is gzip-positive; the duplication is the 6× responsive fan-out, which only observed-use gating (Wave 2, move (a)) can touch.

[2026-09-21T00:25Z lane-c] Terminal: architect ruled KILL, three-agent count agreement (profiler/lead/architect all 3,021/32/0/0 small). No implementer dispatched (nothing safe to implement), no reviewer needed (no diff). Tree has zero product edits. Wave 2: gate (value,bp) on observed call-site responsive objects via lane b's signal; re-census grouping after fan-out collapses.
## Wave 2, recon — css gap fully explained in-bounds (observed-use gating + dead shake → ~2.5-2.7 MiB); sync/RSS remainder is proof-row builds, publish double-write, trace re-parse
- Avenues: B1 observed recipe emission gating+shake (css 14.3→~2.5-2.7, lane a); B2 publish-once (sync −50-100ms, lane c); B3 proof-row gating (RSS ~30-40 MiB + assembly wall, lane b); B4 styletrace entry gate (sync ~125ms hosts, lane d); B5 derive variantMap classes + stop dead-map builds (data 518→~315 KiB, lane b). B1 internal fuse (gate+shake same signal); B3+B5 one lane W1-a precedent (shared builder/table region, hunk split). Post-W1 burndown firsthand: sync 2.64 shares (assembly 612/23.2% fattest, serde 293, emit 242, prepare 345, publish 242), RSS 687 (payload 29.82, residual 511/34.6 KiB-per-file), css pin-identical, data 530,606, residuals 0/0.
- Killed: utilities 2.18 MiB = deduped floor; prepare 345ms = parallelism-only (no safe skip); slim codec ≈390ms = ship-one-sheet (OOB, self-shrinks under B1); parse co-residency 511 MiB = arena-physics floor; constants/ValueGraph gating = unsound-naive (W3 soundness ruling); A9 = 0 (two-census agree). Morning Qs: ship-one-sheet, A10 namer cache (watch-only), parse streaming, deepsee waitReady hang wart (lead-found: poll never cancels, parent parks in kevent — fix shape filed).
- Next crews: perf-2-a observed emission (B1 — architect rules strict-vs-closed + fixture call-sites BEFORE impl, determinative 3.4 vs 11.3); perf-2-b dead product (B3 then B5); perf-2-c publish once (B2); perf-2-d trace gate (B4). Merge: d → c → b → a.

Wave 2 recon: COMPLETE — map filed, 5 avenues, next crews: perf-2-a (B1), perf-2-b (B3+B5), perf-2-c (B2), perf-2-d (B4)
## Wave 2, lane c — publish once (B2) + burndown ride-along (COMPLETE)
- Tried: deferred write-once baseSystem.mjs (system leg builds + stages keyed by outDir; publishRuntimeBundle sets real runtime, serializes/writes once — same object/key order/JSON.stringify, NO signature changes so deepsee worker-phases stays byte-identical + step-for-step with zero edits) + copyFileSync for react css (styled leg already first). 4 files: publish/system.ts, publish/styled.ts (patch fn deleted), publish/react-shell.ts, publish.ts (order comment). sync/index.ts untouched. Fenced ride-along (separate hunk): burndown.ts waitReady cancels poll + kill()s worker on timeout/reject. Lane-b portableTypesSource mirror intact (absent from diff). No RS, no load, no emission changes.
- Bench (locked load, medians; box shared w/ 3 siblings; lead + independent reviewer same-box A/B agree): small 130→127ms / 118.9→117.9MiB; medium 355→344ms (spreads disjoint both sides, ~10ms daylight) / 187→179MiB; enterprise 2491→2375 final, n=5 post (2375-2400) all below n=2 pre (2450,2491) with 50ms daylight, median Δ ≈ −77ms (−3.1%, inside recon −50-100ms lever) / RSS 615→519-567 unregressed. Win grows with scale (−3/−11/−77ms) = byte-proportional shape. Reviewer caveat: its enterprise spreads edge-touch ≤18ms on one slow outlier each side; medium disjoint + replicated deltas carry the verdict. Bundle bytes EXACT all scales (css 15007762/844849, data 530606/43639). Full-folder sha256 (enterprise kept repos, lead pre=neo-bench-8GuN5s post=neo-bench-LOF1cv token=NEOBENCH + reviewer own 22/22 pre/post): 19/22 raw-identical, 22/22 after normalizing only the ephemeral tmpdir leaf; baseSystem.mjs 16258161 B both sides.
- Stability: agentneo 173/173 ok (lead + reviewer firsthand); agentneo q 0 errors (2 pre-existing burndown warns, untouched regions); sync.test.ts 18/18 via pnpm agent vt; zero .rs changed → no agentrs (diff-confirmed). Churn skipped per architect rationale (disk-write-only refactor, byte-identical by construction + proven — churn cannot observe it). Ride-along: deepsee small before/after both exit 0, bytes 550910/104921 residuals 0/0, same stage shape; negative test (injected worker crash): parent exits 1 at 120s with READY-timeout instead of kevent-parking (probe file restored, checksum-verified, no diff).
- Review: VERIFIED (independent subagent, firsthand bench/stability/diff/design; memo packages/reference-neo/docs/evidence/fasthull-2c-review.md). Boundary PASS (exactly 5 files; index.ts, worker-phases.ts, .rs untouched). Notes (not gaps): stage Map entry leaks on mid-publish throw (old code left half-written dir instead); unpaired publishRuntimeBundle writes no baseSystem (unreachable — both call sites pair).
- INCIDENT (resolved, action for lane D): reviewer's `git stash push -- <5 files>` interleaved on the shared refs/stash with a concurrent lane-D agent; reviewer's pop consumed lane-D's stash. Recovered: my diff fingerprint matches pre-stash exactly, my tree holds all 5 files intact, lane-D tree verified uncontaminated, shared stash left as found. Lane D's work survives ONLY as dangling commit 0566fae4a7f13c6c501128c255d246dc51de48ca + /tmp/lanec-foreign-rs.diff — lane D: `git stash apply` that commit in your tree, never blind pop. Captain safety order received: bare stash push/pop banned voyage-wide; this lane used /tmp captures throughout (already compliant) and passed the rule to nested agents (reviewer finished).
- Outcome: VERIFIED — ready for captain merge. Merge flags: system.ts logic hunks vs lane-b type-mirror hunks (different hunks, same file — flag at merge per brief); deepsee worker-phases needs NO changes (measures the new publish shape automatically).

Wave 2, lane c: VERIFIED — publish-once wins −77ms enterprise sync with byte-identical output plus a proven burndown hang fix.
## Wave 2, lane b — dead product: never build or ship what slim serde drops or runtime can derive (B3+B5)
- Tried: B3 reduced — wants_proof() + AssembleCtx.proof + assembly gates on css map/top-level recipe tables/wants (plan-gating TRIPWIRE-KILLED: 12/243 sweep deltas, render_session prefix; plans unconditional + render always runs; native.rs untouched). B5 full — build_shipped() + mod.rs:98 one-liner + variantMap→value-lists reshape + base/className dropped + responsiveBreakpoints hoisted (440/440 uniform) + recipe.ts derivation port + registerRecipeData 3rd param + 7 NEO specs & 12 engine stations renegotiated to stronger literals, paint untouched. 49 files + 4 evidence memos, all in-hunks.
- Bench (locked load, medians, box shared): ent sync 2.46→2.37s (reviewer drift-cancelled A/B −75/−69ms tight arms — thin win), RSS no claim (post higher in A/B); data 518.2→311.2 KiB (−40.0%, hoist step −22.3 KiB exact); css 15,007,762 B byte-identical all scales + churn; diagnostics/traced identical; churn css identical, data 143.2→110.3 KiB. E2E exhaustive: 4752/4752 variant + 528/528 base + 19800 responsive exact.
- Stability: cargo 511+1, atomic vitest 299/300 (SITE-54 proven pre-existing without stash), contracts 13/13, recipe 19/19, agentneo 173/173, q clean both sides. Churn RAN at both checkpoints. No scale/generator/sampler edits.
- Review: VERIFIED (disjoint reviewer, firsthand everything incl. own /tmp HEAD worktree + own E2E script + own A/B).
- Outcome: landed pending captain merge. Merge flags: system.ts mirror (:94-105 + :131-136) vs lane-C logic (~:222-230); harvest-census EXPECTED_BYTES; mod.rs:98 one-liner for lane-A rebase; reports/latest is TRACKED (restored post-run — captain may want it ignored). Incident: stash race repaired + captain-verified, zero contamination, voyage-wide stash ban honored (reviewer used /tmp worktree).

Wave 2, lane b: VERIFIED — dead product, data −40% with css/diagnostics/paint identical, thin sync win, B3 plan-gating killed by tripwire.
## Wave 2, lane a — observed recipe emission: gate responsive on literal call-site selections + shake uncalled part-recipes
- Tried: NEW extract/recipes/selection.rs (result-binding map + X(selection) visitor + resolve + SelectionIndex) + identity.rs trace_binding_terminal (+1, additive) + lib.rs sinks/post-pass + assembly RecipeInputs join + recipes/mod.rs gate (ResponsiveFilter + pure naming maps); fixture call-sites (01-05,07,08, SITE-03/10/15/55, ordering x3, gating, extract/tests) + NEO-11/PARITY-02/SYNC-10 worlds + NEW ATM-RECIPE-09; goldens reblessed (pure deletions)
- Bench (locked load, medians): small css 550,910->92,651 data IDENTICAL; medium css 2,554,607->348,780 data IDENTICAL; enterprise css 15,007,762->2,867,925 (2.7MiB, in-band) data IDENTICAL sync 2.41->1.76s (x3 tight 1.75/1.76/1.77) RSS 655.6->321-350MiB (conditions-affected range); churn css -430,201 (recipes layer gone) data IDENTICAL unregressed — win clears spread, bytes deterministic across runs
- Stability: cargo atomic 533 green (13 selection + 8 trace + 1 unsupported-shapes pin); vitest atomic 300/301 (SITE-54 pre-existing env, shown unrelated); agentneo 173/173 on pre-refactor binary (refactor behavior-neutral; cargo/vitest/7-neo re-verified on final binary; post-merge agentneo mandated); agentrs q 0 violations (soft warnings, justified); agentneo q clean; virtualrs/styletrace reds shown unrelated (untouched, zero coupling)
- Review: VERIFIED (nested distinct reviewer, firsthand: own medium bench byte-exact, cargo 532, atomic seam, 7 neo spots, full diff-vs-boundary; 7 non-blocking notes all addressed)
- Outcome: landed-pending-captain-merge (VERIFIED arc, no commit per crew rules)

Wave 2, lane a: VERIFIED — observed recipe emission cuts enterprise css 15,007,762→2,867,925 B (2.7 MiB, in-band) with runtime-data byte-identical, all stations green, independent review signed.
## Wave 2, lane d — gate styletrace's second parse of all 15k entries (B4)
- Tried: one in-boundary change — `hosts/entries.rs` entry_paths + `trace_skip` 7-needle predicate {import,require,<,css,box,splitCssProps,from} (fence-proved: pipeline needs bare css|box|splitCssProps or import; edges bottom at pipeline; barrels need from; require parser-ignored=conservative) + 4 unit tests; styletrace `tests/trace_gate.rs` 4 lemma tests incl. adversarial barrel→bare-css → ["BareCard"]. Census: 240/240 dead gated, 0 dead kept, 69/69 live kept (small seed-7).
- Bench (locked load; box shared w/ 3 sibs; own-tree baseline clean 59e6836d): enterprise PRE 2.65/2.53/2.49/2.43 vs POST 2.41/2.40/2.38/2.36/2.35 — no overlap, medians 2.53→2.40 (−130ms ≈ hosts 125ms lever); small 126=126, medium in noise (342.5≈341, later samples box-contended); RSS in spread (no claim). Raw bytes IDENTICAL all scales (ent css 15007762/844849, data 530606/43639). Churn HOLDS: 4.22s/611.7 vs 4.19/611.6 W1-close, bytes exact.
- Stability: cargo atomic 515/515; cargo styletrace 26 (+4) / 18 pre-existing-env (stash-proven identical set to HEAD); vitest atomic 298/300 — SITE-54 pre-existing (HEAD-binary proven + W1 a/b/d), SITE-57 BROKEN BY GATE (HEAD-binary control passes); vitest styletrace 4/28 = 24 env (missing generated StyleProps declarations, fails pre-entries); agentneo 173/173; q 3 files clean.
- Review: GAPS (independent agent, firsthand: boundary/Goodhart clean; soundness confirmed incl. require grep-clean; SITE-57 reproduced 3 runs + Broken.tsx 0/7 needles + unparsable; fresh ent 2.40s bytes-exact; no sound in-boundary fix missed — re-parse perf-voiding, heuristics unsound, renegotiation OOB).
- Outcome: GAPS — perf proven and selection sound, but SITE-57 parse-failure isolation station (committed contract: unparsable-needle-free file yields located trace warning) cannot be preserved by a content-only gate: entry_paths sees only sources, no parse-failure signal, and hosts/mod.rs plumbing is out of boundary. Fix shapes for W3: (a) thread main-phase parse-failure keep-alive into entries (mod.rs/lib.rs signatures), or (b) renegotiate SITE-57 to the authoritative main-phase error (contract decision). Diff rides in-tree. INCIDENT: repo-global stash race swapped my 2 files with lane b's 14-file B3 diff mid-verify; lane-b work preserved at /tmp/lane-d-holds-laneb.patch (sha256 7d975ede) + /tmp/lane-d-holds-laneb-files/, my tree restored byte-exact, all cited evidence predates or postdates contamination on pure binaries; no stash used after.

Wave 2, lane d: GAPS — enterprise −130ms no-overlap, bytes identical, selection sound, but SITE-57 isolation station breaks (content-only gate can't see parse-ability; fix needs out-of-boundary plumbing or renegotiation).
## Wave 3, recon — buckets lied: true codec ~15ms, true IO ~348ms, true hosts ~150ms; frontier is single-read, trace, ladder, assembly-resolve
- Avenues: C1 trace redux B4+keep-alive (sync −130 proven, lane b); C2 ladder memo + staged edges (sync ~100-130, lane c); C3 single read: parallel scan + files-share + candidate stream (sync ~400 + true-RSS −40-60M, lane a); C4 assembly resolve dedup (sync ≤100 timers-gated, lane d); C5 data reshape (data −106K css-identical, lane e); S1 end-state retention spike (scored-RSS kill-fast). C1+C2 mechanism-disjoint (dead-gate vs live-memo); C3↔C4↔C5 file-flags noted; C4 plan.rs micro rides C5. Post-W2 burndown firsthand (2 runs): sync 2.19/1.79, parent peak 397/397, payload 5.9, residual 19.0/18.9 KiB/file, css 2.87M/data 311K residuals 0/0. True-shape mined: IO 348, walks 493, prepare 355, assembly 244, hosts 150, emit 70, publish 55, codec 15.
- Killed: ship-one-sheet RETIRED (true codec ~15ms); serde-flat EXPLAINED (IO misattribution); config-215 noise; B3 plan-gating stays dead; utilities floor re-confirmed; B1 slack 0 (css DONE — defend); namer skip dead; GC lever killed (gaming); full streaming stays architecture; publish all-real. Morning Qs: scored-RSS floor if S1 fails (allocator = product arch), A10 watch-only, residual streaming after C3, true-vs-scored note (Panda 261 ≈ true peak; voyage scores bench).
- Next crews: perf-3-a single read (C3 — architect rules 4 homework items BEFORE steps 2-3); perf-3-b trace (C1 — SITE-57 + 2 new fixtures); perf-3-c ladder memo (C2); perf-3-d assembly resolve (C4 — timers-first ≥30ms gate); perf-3-e data reshape (C5); spike-s1 end-state (S1 — kill-fast ≤10MB). Merge: c → b → a → d → e. CAPTAIN PRE-FIX: deepsee sample-parse.ts 2-rule reorder (tooling-only, W1e precedent) before cutting lanes — C4's gate needs true buckets.

Wave 3 recon: COMPLETE — map filed, 5 avenues + 1 spike, next crews: perf-3-a (C3), perf-3-b (C1), perf-3-c (C2), perf-3-d (C4), perf-3-e (C5), spike-s1 (S1)
## Wave 3, lane c — ladder memo: per-compile probe memo on both masters + staged-first edge reads (C2)
- Tried: NEW module-graph/ladder/memo.rs `ProbeMemo` (RefCell path→hit/miss+text+canonical, misses cached) shared as the memo facility by both masters (one instance each per compile — a single shared instance would need lane-b/lib.rs plumbing); AtomicFs embeds it on the disk arm only (staged-first preserved, zero mod.rs touch → lane-a disjoint); styletrace `ModuleResolver` (staged-first is_file/read over the compile map, canonicalize ALWAYS disk+memoized so resolutions AND misses match an unmemoized ladder exactly incl. deleted-after-scan) owned by the Analyzer; source_files `is_file` hoist. FENCED analyzer.rs threading (1 field + init + 3 call sites + hoist, ctor signature byte-identical, surface.rs untouched) — ruled IN by architect (free fn has no persistent state; static/thread-local memos cross-compile-unsound in one N-API process; file unclaimed by all 5 lanes — reviewer AUDITED and concurred SOUND). Legacy resolver/path.rs 0 ticks — untouched. 7 .rs files total (6 tracked + memo.rs NEW untracked — captain: `git add` it with the arc).
- Bench (locked load, same-tree rebuilt-binary A/B, box shared load 3-24; medians): ent HEAD [1.68,1.85,1.86,2.17,2.45] med 1.86 vs memo [1.52,1.52,1.54,1.55,1.61,1.63,1.71,1.73,1.88,2.14] med 1.62; QUIET SUBSETS NON-OVERLAPPING (memo max 1.63 < HEAD min 1.68) — clears quiet spread. Median Δ ~-240ms is conditions-confounded (point estimate, do not etch); mechanism floor ~-60ms in-tick (tick-paired ent burndown: resolver 124t≈154ms/7.6% → 75t≈91ms/3.5%, stat/read syscall leaves ELIMINATED, remainder ladder format/hash CPU); recon band -100-130ms; CAPTAIN RE-MEASURES combined at rendezvous. Medium quiet memo 192/193/197 vs HEAD 210/211/212 (~-15-20ms, clears; reviewer: memo max 210 < pin min 226 — clears FULLY vs wave pin). Small 93 vs 106. RSS within spread, no claim. Churn RUNS: 3.02s/535.9MiB, bytes exact (css 8289806/data 112959 = W2 close) — holds. HONESTY: 35t manifest reads overlap lane-a's IO bucket — NOT additive, not double-claimed.
- Bytes IDENTICAL everything, every run, both arms: ent css 2867925/270510 gzip + data 318688/27262 (W2-close numbers to the byte); med css 348780/40672 + data 130016/20859. CSS DEFENSE holds — sheet DONE at 2.7 MiB, zero regrowth. Goldens green (300 atomic + 173 neo diff output bytes).
- Stability: module_graph 104/104 green (2 new memo tests + all ladder/walk/key/fs/graph/record integration); atomic cargo 533+1 green; styletrace 24 pass (2 new) + SAME 18 env failures as clean-HEAD /tmp worktree (16 StyleProps + 1 primitive entrypoint + 1 scratch-NotFound, all pre-resolution — shown unrelated without stash); atomic vitest 300/301, SITE-54 red PROVEN env (input/node_modules/theme-pkg gitignored-absent in every fresh worktree, present only main checkout, nothing materializes it — same red all 5 prior lanes); agentneo 173/173; q 0 violations, 6 warns byte-identical to HEAD. No scale/generator/sampler/load edits. TOOL GAP for captain: `pnpm agentrs c module_graph` (either spelling) falls back to a vacuous 0-test name filter (crate missing from agentrs map) — real result needs `cargo test -p module_graph` (both reviewers confirmed).
- Review: review #1 GAPS (one blocking TEST-ONLY: my is_dir assertion wrong + the vacuous agentrs run; everything else verified firsthand) → one-line test fix → RE-REVIEW VERIFIED (104/104 firsthand, tree intact, bytes exact). Fenced analyzer.rs + boundary + Goodhart all audited clean. Reviewer notes (not gaps): ent full-spread overlap under contention (quiet-band separation verified; rendezvous re-measure arbiter); churn RSS single-sample variance (bytes exact).
- Outcome: VERIFIED — ready for captain merge. Merge flags: styletrace same-crate as lane b (different files: b=hosts/entries,mod+lib.rs hunk+parser/mod.rs+tests; c=module_resolution+source_files+analyzer — flag at merge); merge order per recon (c FIRST — ladder-memo deepest). Captain: remember memo.rs is NEW/untracked. No commits (crews never commit).

Wave 3, lane c: VERIFIED — ladder memo wins quiet-separated enterprise sync with byte-identical output, all gates green, independent review signed.
## Wave 3, lane b — trace wall redux: B4 gate + parse-failure keep-alive (C1) [CLOSED]
- 2026-09-21T03:18Z lead heartbeat: tree voyage/hyperspace-perf-3-b @ d1b0e0549. GAPS diff fingerprinted (e020adfce, 3 files: entries.rs + trace_skip + 4 unit tests + trace_gate.rs 4 lemmas). Callers verified: collect_hosts zero, resolve only lib.rs:169, entry_paths only hosts/mod.rs:77. Entry parse = analysis/parser/mod.rs:64 (no .with_typescript); resolver/parser.rs:35 serves live-edge type resolution only, out of boundary, untouched. Baseline bench next, then seed + keep-alive + align + 2 fixtures.
- 2026-09-21T03:30Z heartbeat: baseline pinned (own tree clean d1b0e0549, box shared 5 sibs load ~11.6, medians of 3 — small 98ms/110.5MiB, medium 209/126.2, ent 1.70s/316.3, css 2.7M/264.2g data 311.2K/26.6g; report snapshot /tmp/laneb-baseline). Implementer done: GAPS seed exact + keep-alive (failed Vec<bool>, panicked||errors) + entry SourceType=main options + 5th atomic unit test + 2 new trace_gate fixtures. cargo atomic 538/538; styletrace 28 pass incl. all 6 trace_gate, 18 fail = known env set (missing primitive-declaration entrypoint, fails pre-parse); q 0 violations (10 warns pre-existing in untouched parser fns). SITE-57 GREEN on lane binary. SITE-54 red = missing color/purple theme-pkg rung (package absent tree-wide, goldens hold purple from a fixture-present past); HEAD-binary /tmp-capture control fails identically — pre-existing env, no stash touched. Architect/reviewer subagents BLOCKED (root 8/8 full) — retrying for review; recon §C1 option-(a) ruling stands as pre-concurrence.
- 2026-09-21T03:36Z heartbeat: stability + bench complete. agentneo 173/173 ok. vitest styletrace 4/28 = lane-D-identical 24-env set (missing sync-root StyleProps entrypoint, fails pre-entry). Align tripwire PROVEN: js_with_ts_syntax FAILS on HEAD options, passes on mine (/tmp-capture, cargo only, restored+re-greened 6/6). Post medians ×3 (box loaded 12-19): small 97ms (94/102/97) vs 98 (98/98/99); medium 201 (200/201/203) vs 209 (209/209/211) no-overlap; ent 1.59s (1.58/1.59/1.61) vs 1.70 (1.68/1.70/1.72) no-overlap = -110ms, clears spread. Bytes EXACT all scales raw+gzip (ent css 2867925/270510 data 318688/27262). Churn: 4.03/4.21/5.03 (box spread ±11% straddles W2-close 3.98; bytes exact 7.9M/728.2g + 110.3K/19.5g) — unregressed beyond noise; gate only removes work on zero-error loads. Reviewer subagent spawned (capacity freed); terminal on its verdict.
- 2026-09-21T03:45Z heartbeat: reviewer verification in flight (5+ min, still running cargo/vitest firsthand). All lead-side work complete; terminal entry lands on the reviewer's verdict. No further lead action until then.
- 2026-09-21T04:16Z b2-heartbeat (ingestion COMPLETE): rotation-2 lead online. Verdict recovered from reviewer log tail (GAPS: checks 1/2/3/5 PASS firsthand; check 4 FAILS — no post artifacts anywhere + medium bytes contradiction 379308 vs baseline 348780, +8.8%). Tree = 9 allowed paths, .node mtime 03:29:10Z (pre-verdict, no phantom writes). Firsthand so far: baseline artifact genuine (d1b0e05491c2 dirty:true, medium 348780/130016); generator emits ALL-.ts load (components/recipes/dead/config all .ts) so align route is a provable no-op on bench; gate/keep-alive shrink-or-neutral by code read. Reviewer ran `cli.ts --scale medium --runs 1 --seed 7` (= medium+custom rename, same seed/knobs — cosmetic per plans.ts). Same-binary-same-load determinism says one side mismeasured. Plan: verify native freshness → rebuild lane binary → locked medium ×3 + reviewer's exact command + HEAD /tmp-worktree control ×3 → adjudicate, file post artifacts, fresh reviewer or GAPS.
- 2026-09-21T04:26Z b2-heartbeat (check-4 RESOLVED — reviewer's measurement off, lane exonerated): (i) verify:native PASSES — .node 03:29Z stamp 524bf620 == current lane sources, same binary both sides used. (ii) Forensics: b1 DID measure post bytes programmatically (log line 3373: small/medium/ent ALL exact incl. gzip) — claim was real, just never filed as artifacts. (iii) REPRODUCED reviewer exactly: `--scale medium --runs 1 --seed 7` → 379308/132487/41087/20911 byte-for-byte. (iv) Same binary WITHOUT `--seed 7` → 348780/130016/40672/20859 = BASELINE EXACT. (v) MECHANISM NAILED: explicit --seed (any value) renames plan.scale → "medium+custom" (plans.ts:140) → config name 'bench-medium+custom' (config.ts:12) → namer prefixes EVERY class with system name CSS-escaped (bench-medium\+custom__…, +8B × 3816 occurrences = +30,528B exactly; data/baseSystem embed name too). Reviewer compared a different load, not a different engine. Verdict on check 4: measurement artifact, zero-byte-change HOLDS. Next: fresh post main-suite ×3 + churn ×3 (box quiet, load ~2) as filed artifacts, HEAD /tmp-worktree bytes control (medium+churn), stability re-runs, then fresh reviewer.
- 2026-09-21T04:30Z b2-heartbeat (post artifacts TAKEN, quiet box load ~2.3): post main-suite ×3 locked — small 94.4/95.1/95.4 vs base 97.6/98.5/98.7, medium 200.9/202.1/202.7 vs 208.6/209.1/211.4, ent 1575.6/1581.8/1583.5 vs 1680.0/1697.5/1718.2 (all no-overlap; ent median -116ms); bytes EXACT all 3 scales raw+gzip (ent 2867925/270510 + 318688/27262). RSS in spread, no claim. Churn ×3: 4.01/4.05/4.04s, RSS 570/582/561, bytes 8289806/745661 + 112959/19969 — unregressed vs b1 lane 4.03-5.03 (loaded) and W2-close ~4.2s. reports/latest now = post main-suite (filed in tree); churn report + baseline + comparisons in /tmp/b2-evidence/. Control worktree /tmp/w3b-head-ctl @ d1b0e0549 cut (detached); installing + building HEAD binary for the bytes control (medium+churn ×1). Stability + fresh reviewer after.
- 2026-09-21T04:45Z b2-heartbeat (HEAD control COMPLETE — triangle closed): pristine-HEAD binary (agentrs build + build:js in detached d1b0e0549 worktree, hash-pinned reports) → medium 348780/130016/40672/20859 = baseline EXACT = lane EXACT; churn 8289806/745661 + 112959/19969 = lane churn EXACT (my ×3 medians 4.04s; HEAD ×1 4.00s timing-no-claim, bytes deterministic). Check-4 sub-gaps all closed: (a) post artifacts FILED (reports/latest = post main-suite ×3; churn+both-controls+comparisons in /tmp/b2-evidence/), (b) 379308 explained as --seed config-name artifact with exact +30,528B math, (c) churn bytes now backed by HEAD control. Stabbing stability next (agentrs atomic+styletrace, seam vitest, agentneo full), then fresh reviewer with explicit --seed-trap briefing.
- 2026-09-21T04:55Z b2-heartbeat (stability CONFIRMED on current tree, all outputs in /tmp/b2-evidence/): cargo atomic 538/538; cargo styletrace 28 pass + 18 env-fail (all missing-entrypoint pre-entry, trace_gate 6/6 ok); v atomic 300/301 (SITE-54 env-red only, SITE-57 GREEN); v styletrace 4/28 (24 fail = single missing-StyleProps-entrypoint root cause, zero divergent assertions); agentneo 173/173 ok; q 0 violations/10 warns (untouched fns). Tree still exactly the 9 allowed paths. Dispatching FRESH nested reviewer (4th agent, full firsthand re-verdict incl. own locked bench) with explicit --seed-trap briefing; terminal on its verdict.
- 2026-09-21T05:05Z b2-heartbeat (reviewer IN FLIGHT): r2 reviewer (01a0c239-c226) confirmed running via roster; ~10 min elapsed, still verifying (rotation-1's took ~15 min for the same scope). Lead holding — no tree writes except heartbeats. Terminal entry lands on its verdict.
- 2026-09-21T04:41Z b2-heartbeat (reviewer IN FLIGHT, t+15min): roster still running. (Clock note: my two prior stamps 04:55Z/05:05Z used box-local UTC+1; true times ~04:3xZ. All other b2 stamps are true UTC. Still holding for the verdict.)
- Tried: B4 GAPS seed (e020adfce) + keep-alive `failed: Vec<bool>` (panicked||errors) via hosts/mod.rs + lib.rs:169 call site + styletrace SourceType align (.with_typescript(true), option-for-option ≡ main parse_source) + 5th atomic unit test + 2 trace_gate fixtures (needle-free-unparsable, js-with-ts-syntax). 6 rs files + log + 2 reports; 9 allowed paths exactly.
- Bench (locked load, medians, quiet box load ~2.3; post artifacts filed in tree + /tmp/b2-evidence/): small 98.5→95.1ms, medium 209.1→202.1, ent 1697.5→1581.8 (all runs no-overlap; ent −116ms ≈ −6.8%, in-band with C1 −130ms lever); RSS in spread, no claim. Bytes EXACT all scales raw+gzip (ent css 2867925/270510 data 318688/27262; css 2.7MiB DONE, no regrowth). Triangle closed: HEAD-pristine control == lane == baseline (medium 348780/130016/40672/20859; churn 8289806/745661 + 112959/19969). Churn 4.01/4.04/4.05s unregressed (vs b1-loaded 4.03–5.03, W2-close ~4.2). Vs Panda goalpost: ent 1.58s vs 645ms, RSS ~309 vs 261 MiB, css 2.7 = 2.7 MiB.
- Stability: cargo atomic 538/538; cargo styletrace 28 pass + 18 env-fail (all missing-entrypoint pre-entry; trace_gate 6/6); v atomic 300/301 (SITE-54 env-red only — theme-pkg rung absent, goldens hold fixture-present purple; SITE-57 GREEN); v styletrace 4/28 (24 fail = single missing-StyleProps-entrypoint cause, zero divergent assertions); agentneo 173/173 ok; q 0 violations/10 warns (untouched fns). Churn RAN (guardrail holds). Full logs /tmp/b2-evidence/.
- Review: VERIFIED (fresh r2 reviewer 01a0c239-c236, 4th agent, full firsthand: boundary/seed/tests/bench/soundness all PASS with own numbers — own locked medium ×2 199.3/200.1 + small 93.7 + churn 4.18 all bytes-exact; SITE-57 green proves keep-alive e2e; independently re-verified all-.ts load + fence). Rotation-1 GAPS check-4 adjudicated and closed: 379308 = `--seed 7` config-name artifact (plan.scale→"medium+custom"→name→per-class prefix, +8B×3816=+30,528B exact), NOT an engine delta; post artifacts filed; churn backed by HEAD control.
- Outcome: landed pending captain merge; no commits (crews never commit). Merge flags: lib.rs:169-177 keep-alive hunk vs lane-a C3 staging hunk (different regions, same file — flag at merge); styletrace parser/mod.rs+tests vs lane-c C2 module_resolution/source_files (different files — flag at merge); reports/latest is TRACKED (post-main filed — captain may want it ignored per W2b note).

Wave 3, lane b: VERIFIED — trace redux wins ent −116ms byte-identical with SITE-57 keep-alive; r1 bytes GAPS adjudicated as --seed artifact.
## Wave 3, lane a — single read: TS retention + native union-fill + byte-gated streaming (steps 1→2→3, NARROWED)
- Tried: (1) async scan shape with SERIAL reads (parallel pool +43~85ms REGRESSED on warm cache — killed by measurement, F1=280>237); (2) retain scan contents + IGNORE/ext mirror into `request.files`, native union-fill (set-equality by construction), logical `compile-request.json`; (3) stream-with-complete-staging for styling+quote-free files (transient parse, in-order errors/constants, record+bag staged, LAZY refine-on-first-import — D1 deviation, eager proven net-negative). 19 files + new stream.rs/tests/differential; lane-B hunk byte-identical; hosts/analysis/core untouched.
- Bench (locked load, medians, default invocation): ent 1684→1495ms −189 (−11%, clears spread 10×, 6-run meds, same-session clean-base scratch); medium 209→~197; small noise; lead final-tree suite small 96 medium 198 ent 1.50s. css/data raw+gzip IDENTICAL every scale (ent css 2,867,925/270,510 = 2.7MiB DONE, data 318,688; churn total 8,402,765 EXACT). Shortfall vs 350–450 floor: F1 disappointed + union 38 + A 23 (honest math 271−23−38+~10 ≈ 220 ≈ measured 200). FLAG: scored-RSS +25~45MB vs base (lane 352–359 vs base 314; relief unreproduced; true-peak −~100MB structural but sampler-blind) — rides to the captain's merge rule.
- Stability: cargo atomic 541/541 (3 union + 5 stream tests new); vitest atomic 300/301 (SITE-54 ONLY red — proven red on clean-base scratch + isolation, mechanism-impossible for this lane, main gate arbiter); contracts 13/13; neo unit 236/236 (5 differential + 10 base explicit); agentneo 173/173 on final tree; tsc clean; q 0 violations both sides (3 RS + 2 neo warnings, pre-existing/advisory); churn RAN (4.01 vs 4.11 base, bytes exact, unregressed).
- Review: round-1 GAPS (serial narrowing + match-before-filter, both fixed) → round-2 VERIFIED (independent agent, firsthand: fixes, bench, stability, diff). Soundness re-derived sound twice (union exact, D1 lazy sound + eager-cost TRUE, barrel argument airtight, artifact logical). Architect memo lead-authored after 3 rotations died on runtime failures (disclosed untrusted; reviewers re-derived).
- Outcome: VERIFIED pending captain merge, with the scored-RSS flag attached — merge rule (revert if RSS regresses) is the captain's call on quiet-box re-measure. Merge flags: lib.rs C1-call-site vs C3-parse/staging regions; contracts request-section (C3) vs types-section (C5); new stream.rs; oxc_diagnostics same-pin dep.

Wave 3, lane a: VERIFIED — single-read −189ms enterprise (−11%) bytes-identical, soundness twice-derived, scored-RSS +25~45MB flag rides to merge.
## Wave 3, lane e — data reshape: unexpanded compounds + drop dupe fields (C5)
- Tried: unexpanded compound predicates + order-sensitive dupe collapse (table.rs), manual 3-field wire Serialize + OnceLock style-prop cache (plan.rs), TS derivation (stem/key/indices/compound_class+matches ports + legacy fallbacks, recipe.ts), contracts/js/system.ts mirrors, 9 ATM/SITE stations + spec-recipes + 3 NEO specs renegotiated to literals, 2 fixtures, census react repin (+599/+171, code-only). 26 files, no assembly/scan/hosts/ladder/extract/emitter/generator touch.
- Bench (locked load; box shared w/ 5 sibs, wall/RSS noise): bytes deterministic — small data 95941->91030 (-4911), med 130016->110241 (-19775), ent 318688->214466 (-104222, -32.7%; gzip 27204->22077), churn 112959->103709 (-9250); styles.css cmp-IDENTICAL all scales (92651/348780/2867925/8289806); 3x published ent -324271 (~-317 KiB: data -104222, react -98807, baseSystem -121242); cssCalls 171/635/7527/43956 frozen. Sync/RSS: no claim beyond unregressed (uniform +22% contention inflation).
- Stability: cargo atomic 535/535; vitest atomic 300/301 (SITE-54 pre-existing specifier/env, zero recipe coupling); contracts 13/13; neo vitest 234/234 (recipe 22/22); agentneo 173/173; churn RUN (css identical, E2E exact); agentrs/neo q 0 errors (2 non-failing warns: table.rs warn band since 391, recipe.ts 366 lines). E2E exhaustive: med 88/88/308/176 + 1140 probes, ent 440/440/1540/880 + 5728 probes, churn 120/240/0 + 840 probes — ALL EXACT mod characterized dup-collapse (71/316/0 cases).
- Review: VERIFIED (2 independent agents; memo fasthull-3e-review.md). Round 1: GAPS on G1 (stale census pin — refactor postdated pin); re-check: pin-only delta confirmed, 300/301 + 4/4 census green, VERIFIED.
- Outcome: landed pending captain merge. Merge flags: contracts/types.ts (C3 request-section flag — different sections); plan.rs owned by this lane (C4 excludes); system.ts mirror hunks.

Wave 3, lane e: VERIFIED — data reshape, ent data -104.2K (-32.7%) with css byte-identical, E2E exact, 173/173 cases green.
## Wave 3, lane d — assembly resolve (C4) [OPEN 2026-09-21 ~03:10Z; times below are box wall]
- Crew: Fasthull nested (lead = profiler+architect+implementer; reviewer = disjoint subagent at close). Boundary: assembly.rs + runtime/builder.rs + resolve/mod.rs + resolve/* + runtime/serializer.rs + ride-along sample-parse.ts ONLY. plan.rs excluded (lane e).
- Hypothesis: build_atom_set resolves every want, then PlanBuilder::build re-resolves every authored decl (resolve_entry + per-decl canonicalize + to_string + unconditional per-want atom_value_to_json). Upper bound ~100ms of assembly true ≈244ms. BINDING GATE: in-lane timers split 244ms first; implement iff ≥30ms proven redundant; else KILL FAST.
- Status: setup done (install 5s, native 36s, build:js). Own-tree BASELINE (full suite, box shared w/ 5 sibs, dirty-tree reports/latest): small 482/96/97ms (run-1 cold noise, cf. recon) RSS 111.4/108.4/111.1; medium 215/210/208ms RSS 127.2/125.8/130.8; enterprise 1.68s / 311.0 MiB (1 run); css 2.7MiB (264.2 gzip) data 311.2KiB — matches W2 close (1.66/308.3). STEP 0 ride-along next, then timers.

- STEP 0 ride-along DONE (sample-parse.ts only, 2-hunk): identical-sample A/B on frozen ent sample (3428 ticks): total invariant 3428=3428; serde −305 = scan/read +305 exact; assembly −100 = constants +90/hosts +10 exact; all other buckets +0 (true assembly 190t ≈241ms vs recon ~244ms). Medium frozen sample same shape. End-to-end post-fix burndown reconciles (residuals 0/0, cross-check Δ 6.1ms). Zero engine impact.
- GATE PASSED → IMPLEMENTED. Timers (env-gated, since reverted) split ent assembly ~317ms: atom_set 78, recipes 27, plan_build 109 (keys 13 / convert 1.4 / REWANT 54 / slot 27), sheets 59, render 41. Redundancy probe: 48,566 pass-1 records, 32,281/32,281 pass-2 rewants hit (100%), 0 atom mismatches → 54ms second-resolve proven redundant. Soundness: memo key is location-free (when,prop,value,important); resolve outcomes never depend on location (message from detail only, is_duplicate compares severity+message per its own docs) → hit-skip is output-identical; miss falls back to full resolve. Fix: ResolveMemo in resolve/mod.rs (record/get) + build_atom_set returns it + PlanBuilder.with_memo (tests untouched). 3 RS files, no signature changes to pub fns.
- Stability interim: cargo atomic 534/534; vitest atomic 300/301 (SITE-54 = brief-named shared-box flake, asserts result.wants which this change cannot affect — pass-1 wants path untouched); agentneo 173/173; agentrs q 0 violations (finish 91-line soft warn pre-existing). Medium bytes exact (348780/130016).
- Bench (locked load; box shared w/ 5 sibs; PRE = /tmp/lane-d-base detached HEAD worktree, own build): full suite POST small 101/98/96 (base 482/96/97 — run-1 cold both sides) RSS ~110; medium 209/211/215 med 211 (base 210) RSS 132.5; ent 1.73/338.6 single (base 1.68/311.0 single) — singles meaningless on shared box. Enterprise batch ×5: PRE 2.09/1.72/1.76/1.71/1.69 med 1.72 vs POST 1.72/1.69/1.69/1.68/1.70 med 1.69 (Δ med −30ms, arms overlap). Interleaved same-repo A/B ×8 (drift-cancelled): base med 1451.0 vs lane med 1424.9, lane wins 8/8 rounds, paired Δ −17..−50 med −26ms (−1.8%). Bytes IDENTICAL every scale every run (ent css 2867925/264.2gzip data 318688/26.6; med 348780/130016; small 90.5/93.7KiB; churn css 7.9MiB/728.2gzip data 110.3KiB). CHURN BREACH: batch POST 4.17/4.17 RSS 648/642 vs PRE 4.13/4.11 RSS 582/569 (bytes exact); interleaved ×6: base med 3979 vs lane med 4143 (lane loses 5/6, paired med ≈ +106ms, +2.7%) + RSS +~65MiB (+11%). Mechanism confirmed: memo record+lookup ≈ 0.65µs/want flat; rewant saving ≈ 1.7µs (token/shorthand-heavy ent) but ≈ 0.5µs (cheap-hex churn) → structurally wins ent, loses churn. Lazy-key alternative sized (round-3 probe): ~16ms ent < 30 gate — dead. plan_keys 13 / convert 1.4 / slot 27 all < 30 — no second sub-step.
- Stability (final probe-free binary): cargo atomic 534/534; vitest atomic 300/301 (SITE-54 only = brief-named shared-box flake, asserts result.wants which this change cannot affect — pass-1 wants path untouched); agentrs q 0 violations (finish 91-line soft warn pre-existing, was 90); agentneo 173/173 on pre-split binary (file move after is pure move + re-export; reviewer re-runs on final). No scale/generator/sampler edits. Churn RAN (breached, see above).
- Review: GAPS CONFIRMED (disjoint subagent, firsthand): boundary PASS (exact file set, no plan.rs, probe-grep zero); ride-along PASS exact (3428 invariant, −305/+305, −100/+90/+10); memo soundness ARGUED FOR (location consumed only at 3 diagnostic-envelope sites, origin never read, new-signature unchanged, build_atom_set still 4-arg); own ent A/B ×6 lane wins 6/6 Δ −24.0ms (−1.7%); own churn A/B ×4 lane loses 4/4 Δ +67.3ms (+2.0%) — BREACH REPRODUCED (magnitude lower on quieter box); sha256-identical bytes all outputs both scales; cargo 534/534, vitest 300/301 (SITE-54 only), agentneo 173/173 on final binary (pre-split debt CLOSED), q 0 violations; Goodhart PASS.
- Outcome: GAPS (W2d precedent) — hypothesis TRUE (54ms second-resolve proven 100% redundant, 0 mismatches; enterprise −26ms paired 8/8, bytes identical, suite green) but implementation BREACHES the churn guardrail (+~106ms sync, +65MiB RSS, tight arms) with a confirmed-structural cause. Diff PRESERVED in-tree (uncommitted): assembly.rs + runtime/builder.rs + resolve/mod.rs + NEW resolve/memo.rs. W4 handoff: a churn-neutral second-resolve skip needs zero-per-want lookup — extract-recorded decl↔want index pairing (extract/* out of this lane's boundary; new evidence = this lane's 100%/0-mismatch proof + cost model). SEPARABLE: the STEP 0 ride-along (sample-parse.ts 2-hunk bucket fix, proven identical-sample + e2e 0/0) is independent tooling with zero engine impact — captain may cherry-pick it regardless of the memo's fate. Captain: restore reports/latest after last bench (tracked scratch, currently POST-churn); /tmp/lane-d-base + /tmp/lane-d-timers/* + frozen samples preserved for re-proof.

Wave 3, lane d: GAPS — second resolve proven redundant (ent −26ms 8/8, bytes identical) but memo breaches churn (+106ms/+65MiB, structural); diff preserved, W4 handoff filed, ride-along separable.
## Wave 3, spike s1 — end-state retention: V8 attribution + allocator-residue experiment
- Tried: nothing in-engine (read-only + reverted temp probe). /tmp probes: memsplit/phased/heap-snapshot pre-post diffs (snapshot-forces-GC split trick) + temp macOS-only s1_pressure_relief napi export (measured 4x, reverted, binary rebuilt clean).
- Bench (no scored claim — kill-fast): V8 end-state = ~2 MiB sync-retained reachable (1.9 esbuild buffer + ~1 misc) + 8-19 MiB GC-only garbage (sheets/payload/spec, GC-timing ±11 identical work); relief returns -24.1/-24.1/-25.9/-24.1 MiB same-process 4/4, but like-for-like end-state spread is 286-378 (n=5, σ≈36, V8 bit-identical across the swing = 100% allocator-side lottery) so no BENCH-scored ≥10 MiB demonstration is possible on this box.
- Stability: n/a (zero engine diff; memo + log only). Tree verified clean except memo + this entry; probe export confirmed gone from rebuilt binary.
- Review: n/a — spike died by kill-fast rule, no product shape to review. Attribution memo is the deliverable.
- Outcome: died — leg (i) KILLED (reachable 3x below bar, garbage needs GC = gaming); leg (ii) KILLED as a lane (-24 mechanism proven but unverifiable inside ±36 spread; feeds morning allocator-strategy Q1 with the new bound that only ~24 of ~150-190 dead-resident MiB respond to zone pressure).

Wave 3, spike s1: died — V8 end-state unattainable (GC-only) and allocator relief unverifiable inside run spread; attribution filed.
## Wave 4, recon — wall is five rooms: want line/col ~40ms, plan verdicts ~50-110 tiered, union re-stat ~40-90 gated, resolver staging ~30-40 gated; RSS hunter is chunked handoff ~10-12MB
- Avenues: D1 lazy line index (sync ~35, lane a); D2 plan-verdict diet A-always/B-gated (sync ~50-110, lane b, subsumes C4-pairing); D3 trust-staged union/file_type-fallback (sync ~40-90, lane c); D4 unstage unimported (sync ~30-40, lane d, gated); R1 chunked handoff (RSS ~10-12MB structural, hunter r). Flags: sync/index.ts (D3+R1), lib.rs (D4+R1), render.rs (D1+D2-steps). All lanes: RSS unregressed-or-claimed, bundle exact, churn RUNS.
- Killed: prepare-reads floor (~283 syscall, parallelism twice-negative); fg-micro (weak evidence, W5); parse/emit/hosts/walk physics; render-standalone folded into D2; map-Vec (~5-10, piggyback); pool fusion (coupling, W5); C4-pairing subsumed by D2 (diff preserved); B3-gating stays dead (D2 is a new shape); GC-timing unavailable (S1); bundle: ZERO lanes (home, defend exact).
- Next crews: perf-4-a (D1); perf-4-b (D2 — architect rules emptiness+subset day-0, 243-sweep gate); perf-4-c (D3 — architect rules enumeration, fallback if unprovable); perf-4-d (D4 — architect rules External soundness, 60% kill-fast); hunter-4-r (R1 — standing-method proof). Merge: a → d → c → b → r.

Wave 4 recon: COMPLETE — map filed, 4 avenues + 1 hunter, next crews: perf-4-a (D1), perf-4-b (D2), perf-4-c (D3), perf-4-d (D4), hunter-4-r (R1)
## Wave 4, lane c — union trust: (a) KILLED by extglob proof, fallback (c) file_type lands (COMPLETE)
- Tried: (c) ONLY — sources.rs: WalkEntry (d_type+path) + sorted_entries + entry_is_dir (d_type decides plain files/dirs; symlink/unknown/refused → is_dir() stat fallback); both walks (scan_dir + collect_candidate_paths) share the helper; sort identical; new unix symlink pin test (linked dir descends, linked file carries target bytes, dangling drops, union backfill completes). (a) files_complete flag KILLED pre-implementation: firsthand extglob counterexample (pattern src/a+(b).ts — fg hits [ab.ts] nonempty, today union [blue] vs simulated skip []; /tmp/d3-glob-probe2.mjs, reviewer-reproduced) proves native-hit ⊆ retention FALSE; mid-sync races second strict kill. No TS touched → R1 sync/index.ts merge flag EVAPORATES.
- Bench (locked load; box shared w/ 4 sibs throughout — paired/adjacent only): enterprise paired Δms lead 6/6 (-70/-70/-40rev/-60/-60/-10, med -60) + reviewer 3 (-183/-221rev/+126) = pooled 8/9 same-sign, med -60, wins in both orders; claim TUNED per review: ~40ms TYPICAL (conservative central estimate, NOT a per-run floor). Levels (conditions-affected): final filed suite small 113ms/113.1MiB, medium 215/138.3, ent 1.32/363.6. RSS HELD (overlapping GC-noise bands both operators: lead lane 339-368 vs base 330-345; reviewer lane 349-371 vs base 339-381; fewer syscalls by design, KB-scale transients). Bundle EXACT every leg every scale (css 92651/348780/2867925 raw, 264.2KiB ent gzip; data 91030/110241/214466; calls 171/635/7527 frozen). Churn HOLDS (lead: 2.94s/564.7MiB vs 2.96/527-604 close, bytes exact; reviewer: not independently re-run, covered by shared walk path + 6 exact ent legs + 1022 green tests).
- Stability: cargo atomic 549/549 (lead + reviewer firsthand, incl. new symlink pin); v atomic 300/301 (SITE-54 ONLY — node_modules-absent env mechanism confirmed by BOTH operators independently, main gate arbiter); agentneo 173/173 (both operators own runs); q 0 violations (+1 advisory line-warn 405>365, tests in-module by necessity). No scale/generator/sampler edits.
- Review: VERIFIED (disjoint subagent, firsthand everything: boundary/diff/stability/probe/bench + own from-scratch rebuild bit-identical md5 8d18≠base d930; one required fix — "floor"→"typical" — APPLIED here; race-window caveat ruled same-class-as-existing).
- Outcome: VERIFIED — ready for captain merge. Merge flags: NONE (single-file RS diff; no sync/index.ts, no lib.rs, no render.rs touch). Captain: reports/latest is TRACKED scratch (reviewer-regenerated full-suite lane); binaries provenanced (lane md5 8d18 in tree, baseline d930 at /tmp/lanec-baseline.node).

Wave 4, lane c: VERIFIED — fallback (c) file_type kills the per-entry stat (~40ms typical), bytes exact, RSS held, (a) dead on extglob proof.
## Wave 4, lane a — want locations (D1): lazy per-file line index [LIVE 2026-09-21T07:03Z]
- Brief read (VOYAGE-HYPERSPACE §2, fasthull, recon-4 §D1, W3 close + HQ amendment, bench frozen load, Panda notes). Tree clean on voyage/hyperspace-perf-4-a; no node_modules yet — install → base build → own-tree baseline, then implement.
- Architect pre-concurrence (day-0): recon shape CORRECTED on one point — ExpressionWalk is constructed per-property (entries.rs:79, object/mod.rs:234, call_lower.rs:114), so a lazy field on ExpressionWalk alone rebuilds O(file) per property (no win). Index owner must be per-file ExtractContext (new() ×3, zero literals), shared down as Option<&LineIndex> through ObjectWalk + ExpressionWalk; ensure-at-first-walk-construction preserves the laziness that matters (12k dead files construct zero walks → never pay). span_position keeps a scan fallback (None → line_col) so behavior is identical on every path. SourceCatalog render-path piggyback needs zero render.rs changes (change sits in site.rs SourceCatalog) → no D2 merge flag at all. Boundary: site.rs + walk/mod.rs + extract/mod.rs (ExtractContext field/ensure/3 walk ctors) + object/mod.rs (field/ctor/span_position) + diagnostics/mod.rs export line. No other lane touches these files.
- [07:40Z] Implementer: diff complete (5 files: site.rs LineIndex+locate cache+fuzz, diagnostics/mod.rs export, extract/mod.rs ExtractContext field/ensure/3 ctors, object/mod.rs + walk/mod.rs field/ctor/span_position). cargo atomic 550/550 (+2 fuzz); q 0 violations (3 pre-existing warns unchanged); native rebuilt. vitest atomic 300/301 — SITE-54 only (specifier-resolution wants-presence; hasWant matches prop/value/when ONLY, line/col not consulted; fuzz proves line/col identical; resolver region untouched by this diff) — brief-named shared-box flake, re-ran, main gate arbiter per brief. Baseline (base binary, shared box): small 90/108.1, med 180/123.0, ent 1312ms/334.7MiB; css 92651/348780/2867925, data 91030/110241/214466.
- [08:30Z] A/B v1 (with catalog cache): lane wins 6/6 pairs, paired Δ −12..−128 med −30ms, bytes 12/12 identical — but paired RSS med +9 (spreads overlap, R4 lane 383 outlier) and mechanism read shows the catalog HashMap is end-of-compile-retained inside the peak window (≤~4MB real). Architect ruling: DROPPED the render-path piggyback (recon said "if clean" — it wasn't RSS-clean). Final diff = pure core: transient per-file index, nothing alive at peak that wasn't before. cargo 550/550, q clean; final binary rebuilt. Re-running A/B ×6 on the final binary.
- [09:05Z] A/B v2 FINAL (pure core, no catalog): 4/6 pairs, paired Δ −110/+25/+14/−22/−97/−36 med −29ms; pooled v1+v2 10/12 wins med −29ms (both orders 5/6 — no order confound). Bytes 24/24 identical across every A/B run (css 2867925, data 214466). RSS: band medians identical (lane 355 vs base 356), paired spreads −39..+20 = documented S1 lottery; mechanism = zero retained at peak (transient per-file index only) → HELD. Lane binary restored in-tree (md5 e60a…, stamp consistent). Churn + neo next.
- [09:40Z] Stability final: cargo atomic 550/550; vitest atomic 300/301 (SITE-54 only, brief-named flake, hasWant ignores line/col); agentneo 173/173; q 0 violations (3 pre-existing warns). Full vitest: 532/567 — red = virtualrs 10 (voyage-log-R1-documented pre-existing goldens) + styletrace 24 (PROVEN pre-existing: identical 24/4 on base binary; missing gitignored .reference-ui entrypoint in this tree, module untouched) + SITE-54. Churn: pair base 3.40s/630.1 vs lane 3.37s/619.4 (bytes exact 8289806/103709 both) — no breach. Final suite: small med 94 (base 90, cold-noise), med 177 (base 180), ent 1264 (base 1312); bytes exact every scale+churn; RSS 109.7/129.9/326.0 held. Spawning disjoint reviewer.
- [RVW 07:30Z] Review: VERIFIED (stamp = box `date -u`; implementer Z-stamps run ~2h ahead of this box clock). (1) Boundary EXACT: `git diff --name-only` = 5 RS files (site.rs LineIndex+fuzz, diagnostics/mod.rs 1-line export, extract/mod.rs field/ensure/3 ctors, object/mod.rs + walk/mod.rs field/ctor/span_position) + VOYAGE log + reports/latest scratch; zero render/builder/assembly/sources/resolver/stream/bridge/TS/generator/scale/sampler/harness touches. Catalog piggyback fully gone: no RefCell/HashMap in site.rs, SourceCatalog::locate back on scan line_col. (2) Equivalence: `pnpm agentrs c atomic -t "line_"` → 8 passed incl. both line_index_matches_scan fuzz tests + 3 pre-existing line_col tests; read both — assert_index_matches_scan compares index vs scan on EVERY offset 0..=len+1 (past-end + all mid-char byte positions) over 9 edge shapes + 400-token LCG text with \\n/emoji/\\r\\n/é. (3) Stability: `pnpm agentrs c atomic` → 550/550 lib (+1 integ); `pnpm agentrs q` on 5 files → 0 violations, 3 warns pre-existing in kind (git show HEAD: base extract/mod.rs 559>365, walk/mod.rs 372>365; walk_fallback untouched); `pnpm agentrs v atomic` → 300/301, sole red ATM-SITE-54 at spec.ts:43 hasWant(prop,value) — read helpers.ts:255-268, matches prop/value/when ONLY, line/col never consulted; `pnpm agentneo run` → 173/173 (last-run.json total=173 ok=173). Styletrace-24: no base-side vitest log in /tmp, no swap run — mechanism conclusive from evidence: vall.log error is `missing StyleProps declaration entrypoint in .../styletrace/fixtures/sync-root` thrown from styletrace native before atomic code matters; sync-root/ lacks .reference-ui which IS gitignored (.gitignore:52) → binary-independent env failure; module untouched by diff; lane-C tree independently styletrace-red. Virtualrs-10: semicolon-format golden diffs in an untouched module (code-identical across binaries by construction); prior-wave line 113 already documents virtualrs reds as unrelated. Full-log tally matches: 532/567 = 10+24+1. (4) Bench round 7 via /tmp/lane-a-ab.sh "7": base 1386ms/362.0MiB → lane 1320ms/332.2MiB, Δ −66ms lane win, direction-consistent with v2 med −29ms; bytes 2867925/214466 BOTH sides exact. NOTE: brief said round 7 is lane-first — script runs odd rounds base-first (confirmed via r1/r2 log mtimes); round 7 ran base→lane and ends on lane. In-tree .node after = e60a40b24c8ec5d03c3530bca87399b0 (lane, verified, no restore needed). (5) Sweep `pnpm bench:neo -- --scale small,medium --runs 1` on md5-verified lane binary: small 92651/91030, medium 348780/110241 — EXACT vs /tmp/lane-a-base.json. (6) Goodhart clean: neo-side diff is reports/latest scratch numbers only (incl. my own runs), no source/harness edits. No commits/pushes/stash; lane tree only.
- [07:31Z box] Close-out: reviewer round 7 base 1386 → lane 1320 (Δ −66, bytes exact) → v2 final 5/7, pooled v1+v2 11/13, med −29ms. Tree verified: lane .node e60a… in place, reports/latest restored to HEAD; diff = 5 RS files + this log only (+79/-0 core: LineIndex+fuzz, export, ExtractContext field/ensure/3 ctors, 2 walk field/ctor/span_position). Evidence preserved: /tmp/lane-a-base.json, /tmp/lane-a-base.node, /tmp/lane-a-lane.node, /tmp/lane-a-ab.sh + 14 round logs, /tmp/lane-a-vall.log. No commits/pushes/stash; captain merges.

Wave 4, lane a: VERIFIED — lazy per-file line index, ent −29ms med (11/13 paired, bytes exact all scales+churn, RSS held, 173/173 neo, 550 cargo).
## Wave 4, lane b — plan verdicts: tier-A diet (placeholders + carried keys + fact memo); B killed day-0, insert-skip killed by sweep
- Tried: tier (A) per architect ruling — diet plans on !proof (resolve + emptiness gate + inserts run; per-atom slot/class strings replaced by one zero-alloc placeholder), carried canonical keys (build_with_keys/build_diet sidecar + render_session_with_keys, no re-serialization), render.rs-internal fact-key memo (exact serializes once). Proof path byte-identical legacy (build + render_session untouched). 4 files, +317/-31: runtime/builder.rs, assembly.rs, diagnostics/proof/plans.rs (tests only), diagnostics/proof/render.rs. facts.rs/policy/analysis.rs untouched. Tier (B) KILLED day-0 (sound predicate = ~200-line shadow-resolve, uncensused, divergence risk + $r-fenced plan-pass-unique diagnostic). Insert-skip KILLED by the sweep (ATM-LEAF-11: plan pass sole producer of a leaf-`!` rule, −48B css, rule-level proven; inserts restored). No C4-pairing (retired), no Goodhart.
- Bench (locked load; box shared w/ 4 siblings, adjacent lane-vs-HEAD pairs, loads recorded): enterprise reviewer R3 (loads 4.40/4.45, best matched) POST 1255.7/1274.8/1257.1 vs HEAD 1291.4/1284.5/1291.9 — NO OVERLAP, median −34.3; R1 −30 touching; R2 +15 load-5.2 noise; pooled medians 1280 vs 1300 (−20). Implementer pair: −60 separated (loads 4.38/4.35), −30 touching. Medium −4…−7 shape; churn −100…−155 median/single arms-touch (suggestive, guardrail holds). Below the 50-65 prediction but direction + separation + linear-in-wants shape match the lever. Bundle EXACT raw+gzip every scale + churn (ent css 2867925/270510 + data 214466/22110; med 348780/40672 + 110241/19674; churn 8289806/745661 + 103709/19339). RSS interleaved every sample (no claim, no regression; strictly-less-materialization by design).
- Stability: q 0 violations (4 files); cargo atomic 551/551; v atomic 301/302 (SITE-54 sole red — proven red on pristine HEAD control, asserts pass-1 wants, unrelated); 244-sweep cmp-IDENTICAL pre/post/rpost (implementer + reviewer own runs); agentneo 173/173. Churn RAN (bytes exact, sync suggestive-negative). No scale/generator/sampler edits.
- Review: VERIFIED (disjoint reviewer, firsthand 6/6: boundary/diff, KILL-B re-proof, LEAF-11 re-proof at rule level, own sweep, own enterprise pairs, own stability; memo fasthull-4b-review.md). Architect R1 died at runtime (zero residue); R2 filed.
- Outcome: landed pending captain merge. Merge flags: none shared (all 4 files D2-exclusive per recon §D2(d); channels/render.rs untouched so D1's one-liner is safe). Captain: re-run the 244-sweep post-merge (temp tests/w4b-sweep.test.ts + procedure in its header), then DELETE it; restore tracked reports/latest after last bench. PRE snapshot /tmp/w4b-sweep-pre.json (244 cases, 0 errors); HEAD control /tmp/w4b-head-ctl @ c84dd4d7a left for re-proof.

Wave 4, lane b: VERIFIED — tier-A plan diet wins separated enterprise sync (−34ms reviewer / −60ms implementer, matched pairs) with byte-exact bundle, zero-delta 244-sweep, and both kills rule-proven.
