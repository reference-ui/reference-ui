# @fixtures/extend-library

Reusable fixture library for `extends`-based tests.

## Exports

- `@fixtures/extend-library`: exports `baseSystem`, `DemoComponent`, and `LightDarkDemo`

## Usage

```bash
pnpm run sync
pnpm run build
```
