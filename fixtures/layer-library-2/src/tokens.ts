/**
 * layer-library-2 tokens. The accent color stays inside the layer scope when
 * a consumer adopts this library via `layers[]`.
 */

export const layerPrivateAccent2 = '#9f1239'
export const layerPrivateAccent2Rgb = 'rgb(159, 18, 57)'
