# @fixtures/layer-library

Reusable fixture library for `layers`-based tests.

## Exports

- `@fixtures/layer-library`: exports `baseSystem` and `LightDarkDemo`

## Usage

```bash
pnpm run sync
pnpm run build
```
